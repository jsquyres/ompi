# Open question issues older than 6 months

Triage of open `open-mpi/ompi` issues opened more than 6 months ago (created before 2025-12-20), as of 2026-06-20. Classification and status are a **reasoned determination** from each issue's text, discussion, labels, and linked/merged PRs, plus spot-checks of current `main` — not a per-branch build-and-reproduce test. Treat "Close" as *recommended for maintainer confirmation*, not automatic.

Issues are grouped by **why they are still open**. See [issues-overview.md](issues-overview.md) for totals and cross-tabs, and the companion files: [bugs](issues-bugs.md) · [feature requests](issues-feature-requests.md) · [questions](issues-questions.md) · [other](issues-other.md).

These are user/usage questions, not defects — "why still open" focuses on whether they were answered, abandoned, or left hanging.


**148 issues** — 🗑️ Close 120 · ❓ Needs info 28 · ✅ Keep open 0.


## Unresolved, still valid (1)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#7334](https://github.com/open-mpi/ompi/issues/7334) — Oddity: configure and dlopen checks for CUDA support | Design question asking why Open MPI does configure-time CUDA feature checks (e.g. GDR) yet dlopens libcuda at runtime, suggesting runtime dlsym checks instead. | n/a | 🗑️ Close | This is a stale design/usage question that was never answered; the CUDA stack has been substantially reworked since 2020 (accelerator framework), so the specific concern is largely moot and it can be closed as an unanswered design discussion. |

## Answered (95)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#862](https://github.com/open-mpi/ompi/issues/862) — Non-standard behavior in MPI_File_open | Reporter claimed MPI_File_open with MPI_MODE_WRONLY should truncate the file like fopen("w"); maintainers explained MPI IO flags map to POSIX open() (not fopen), so not truncating is correct. | n/a | 🗑️ Close | Discussion concluded the behavior is standard-compliant (flags map to POSIX open semantics, truncation requires MPI_File_set_size); the affected ROMIO backend has since been removed in favor of ompio. Not a defect; answered. |
| [#1453](https://github.com/open-mpi/ompi/issues/1453) — suppress debug printouts in malloc.c | User asked how to suppress 'malloc debug: Request for 0 bytes' messages from opal/util/malloc.c. | n/a | 🗑️ Close | The output is gated behind OPAL_ENABLE_DEBUG (still true in opal/util/malloc.c on main), so it only appears in debug builds; question was answered (use a non-debug build). Close. |
| [#3781](https://github.com/open-mpi/ompi/issues/3781) — MPI_Gather Performance Issue (ompi_coll_base_gather_intra_linear_sync) | First call to MPI_Gather with the linear_sync algorithm (algo 3) was ~500x slower than subsequent calls; the user asked how to reduce the initial startup cost. | n/a | 🗑️ Close | bosilca explained the slowdown is one-time connection establishment and the user confirmed mpi_preconnect_all resolves it; the questions about the algorithm were answered fully and no defect remains. Recommend Close. |
| [#4866](https://github.com/open-mpi/ompi/issues/4866) — Error running MPI hello-world with IB on 16 machines (2 or more threads per node) | Hello-world segfaults in MPI_Init via the openib BTL at scale because mixed IB/Ethernet Mellanox ports are auto-selected; excluding the Ethernet-mode HCAs avoids the crash. | n/a | 🗑️ Close | A usage/config issue answered in-thread (use btl_openib_if_include/exclude to pick the right HCAs). The openib BTL itself has been removed from current main (replaced by UCX/OFI), so the specific component and behavior are obsolete; the question was answered. Recommend closing. |
| [#5763](https://github.com/open-mpi/ompi/issues/5763) — Performance issues with memory/patcher | User observed a 1-5% performance penalty from the memory/patcher component for a CFD code and asked what it does and whether it is safe to disable. | n/a | 🗑️ Close | This is a support/usage question (memory/patcher still exists on main); the reporter self-reported no issues after disabling it and the thread has been idle since 2018 with no defect established. Recommend closing as an answered/stale question. |
| [#5808](https://github.com/open-mpi/ompi/issues/5808) — --MCA flag does not activate btl framework to work with specified components (TCP/IB) | User trying to benchmark TCP vs IB BTLs saw identical/poor alltoallv latency; gggouaillardet explained btl tcp/openib are only used with pml ob1 and that the correct mca selection (pml ob1 + btl tcp,self / openib,self / vader) was needed. | n/a | 🗑️ Close | This was a usage/component-selection misunderstanding that was answered; the mxm/openib components involved are now removed and the thread has been idle since 2018. Recommend closing as answered. |
| [#5976](https://github.com/open-mpi/ompi/issues/5976) — Failed to convert worker name to MPI index: ps:0:0 | TensorFlow grpc+mpi distributed training aborts with 'Failed to convert worker name to MPI index' when workers/ps are launched manually instead of via mpirun. | n/a | 🗑️ Close | This is a usage problem in TensorFlow's contrib/mpi layer (not launched under a single mpirun); a commenter correctly noted mpirun must launch all workers/ps, and TensorFlow's MPI contrib is itself defunct. Not an Open MPI defect; recommend closing as answered. |
| [#6113](https://github.com/open-mpi/ompi/issues/6113) — Not enough slots when upgrading from 2.0.2 to 4.0.0 | User confused why Open MPI 4.0.0 refuses to launch more procs than physical cores (no longer oversubscribing by default), unlike 2.0.2. | n/a | 🗑️ Close | This is intended behavior change (default no oversubscribe / hyperthreads not counted); maintainers explained the --oversubscribe / --use-hwthread-cpus flags and the rmaps_base_oversubscribe MCA param. A usage question that was answered; recommend closing. |
| [#6390](https://github.com/open-mpi/ompi/issues/6390) — MPI_Bcast hangs with version 4.0.0 when opal_progress_set_event_flag is set | Calling the internal opal_progress_set_event_flag(1) from a user app to yield CPU while blocked causes a hang in 4.0.0; user wants the old low-CPU-usage behavior. | n/a | 🗑️ Close | opal_progress_set_event_flag is an internal API not meant for end-user apps; the supported equivalent is mpi_yield_when_idle, and the hang was traced to events from btl/vader (now btl/sm), worked around with --mca btl ^vader. Usage question that was answered with workarounds; recommend closing. |
| [#6447](https://github.com/open-mpi/ompi/issues/6447) — horovod distributed failed run in k8s with kube-router | MPI job (Horovod TensorFlow) hangs under Kubernetes with kube-router due to IP masquerade / NAT confusing TCP wireup (peer reports 0.0.0.0 as its address). | n/a | 🗑️ Close | Same class as referenced issue 6103; reporters confirmed it is an IP-masquerade/NAT problem solved by disabling masquerade. An environment/usage issue, effectively answered (a 2024 drive-by question merely asked which parameter); recommend closing. |
| [#6847](https://github.com/open-mpi/ompi/issues/6847) — --use-hwthread-cpus changes behaviour of program | User on Windows 7/Cygwin with a 2-core i3 reports that adding --use-hwthread-cpus makes a simple Barrier program appear to hang (never finish). | n/a | 🗑️ Close | Maintainers explained the program is not hung but running far slower due to oversubscription of hyperthreads on very old hardware/OS (awlauria measured ~5x slowdown), i.e. working as expected. Answered; suggested closing as 'working as intended'. Close. |
| [#6868](https://github.com/open-mpi/ompi/issues/6868) — UCX (SW) RMA Atomics Performance | Benchmarking shows MPI RMA accumulate/fetch-op latencies ~4x higher under osc/ucx than MVAPICH/openib, attributed to SW atomic emulation on an old MOFED stack and unnecessary locking in osc/ucx fetch-and-op. | n/a | 🗑️ Close | A support/investigation thread; the high latency was explained (SW atomics on old MOFED plus extra accumulate locks), and the reporter (an OMPI member) confirmed removing the locks fixed performance and said he would submit a PR. The diagnostic question was answered; any code improvement is tracked via the follow-up PR work, so close. |
| [#6916](https://github.com/open-mpi/ompi/issues/6916) — Error using MPI_Comm_connect/MPI_Comm_accept | Connect/accept between processes launched by separate mpirun commands fails with 'Not supported / accept/connect functionality' unless a rendezvous server is provided. | n/a | 🗑️ Close | rhc54 answered: separately-launched jobs need a rendezvous server, achievable via ompi-server or by running under a PRRTE/prte DVM (now the standard model in v5.0+). This is the expected usage and the question was answered; close. |
| [#7088](https://github.com/open-mpi/ompi/issues/7088) — How to reduce Open MPI's memory footprint? | User compared Open MPI 3.1.3 (UCX/openib) memory consumption against IBM Platform MPI on ConnectX-3 and asked how to reduce the footprint at low-to-medium node counts. | n/a | 🗑️ Close | This is a tuning/support discussion, not a defect; maintainers provided extensive recommendations (disable OOB/ud, use UD transport, ob1+vader for single node, various UCX env vars) and the remaining gap is UCX-side tuning, not an Open MPI bug. Largely answered and stale since 2019; close. |
| [#7090](https://github.com/open-mpi/ompi/issues/7090) — Is it possible to use different OFI mtl for inter- and intra-node transfers? | User asks whether Open MPI can use different OFI/libfabric providers for intra-node vs inter-node communication with the OFI MTL. | n/a | 🗑️ Close | Maintainer answered clearly: the OFI MTL (CM philosophy) uses a single libfabric provider for all peers and assumes the provider handles on-node transfers internally; this is a design answer, not a defect. The question was fully answered and is stale since 2019; close. |
| [#7106](https://github.com/open-mpi/ompi/issues/7106) — mpiexec not allowing more cpus per process in more than one node (master or slave) | User tries to request more cpus-per-proc (28) than slots available on a node (24) and gets a 'not enough slots' error, asking how to span multiple nodes for a single multithreaded process. | n/a | 🗑️ Close | Maintainer answered that requesting more cpus-per-proc than slots on a single machine cannot work even with overload-allowed, since a process's PEs cannot span nodes; this is a usage answer, not a defect (and uses the deprecated --cpus-per-proc syntax). Answered and stale since 2019; close. |
| [#7130](https://github.com/open-mpi/ompi/issues/7130) — [btl_tcp_component.c:966:mca_btl_tcp_component_create_listen] bind() failed: no port available in the range | User restricted TCP ports for a firewall via oob/btl port-range MCA params and got 'bind() failed: no port available in the range', with mpirun itself occupying the whole 8445-8455 range. | n/a | 🗑️ Close | This is a usage/configuration question; maintainers explained the port range was too small (oob and btl both need ports, plus FIN_WAIT/stale processes consuming them) and advised killing leftover processes, widening the range, and restricting interfaces with btl_tcp_if_include. No defect identified and stale since 2021; close. |
| [#7164](https://github.com/open-mpi/ompi/issues/7164) — Unable to run mpiexec in an MPI initialized environment | User reports that spawning mpiexec from within a process that has already called MPI_Init fails (exit code 256). | n/a | 🗑️ Close | A maintainer answered that this is expected behavior and pointed to MPI_Comm_spawn with MPI_Info for launching non-MPI apps; the question was answered and no defect exists, so close. |
| [#7165](https://github.com/open-mpi/ompi/issues/7165) — Run openmpi on docker containers on different hosts | User asks for help configuring Open MPI to run across Docker containers on different Google Cloud VMs, with orted failing to start daemons. | n/a | 🗑️ Close | The reporter themselves acknowledged this is a Docker networking setup question, not an Open MPI defect; it is a usage/support request with no maintainer follow-up needed, so close. |
| [#7461](https://github.com/open-mpi/ompi/issues/7461) — Error initializing an OpenFabrics device using Mellanox InfiniBand | openib device-attribute errors and missing UCX libraries on a compute node; user had inconsistent library/MOFED installs across nodes. | n/a | 🗑️ Close | Discussion identified it as a node-specific library/MOFED installation problem, not an Open MPI defect; the openib BTL has since been removed (gone on main), so the path no longer exists. Close as answered/obsolete. |
| [#7532](https://github.com/open-mpi/ompi/issues/7532) — MCA settings required to run a windowing query | RMA with MPI_Win_create fails with CMA 'Wrote -1, errno=14' errors; switching to MPI_Win_allocate works. | n/a | 🗑️ Close | Diagnosed as a CMA/ptrace permission limitation with externally-provided buffers; maintainers recommended MPI_Win_allocate, which the reporter confirmed works. Question answered and resolved by workaround; close. |
| [#7533](https://github.com/open-mpi/ompi/issues/7533) — openmpi seems to give priority to low ranks if there is multiple messages pending | User asks how to control MPI_ANY_SOURCE matching fairness in a master/slave pattern; observed low ranks favored in old 1.6.4 but balanced in 4.0.0. | n/a | 🗑️ Close | The reporter's own output shows 4.0.0 already balances work across all slaves (4 each), unlike 1.6.4; the concern is effectively resolved by newer versions and is a usage question with no defect. Close. |
| [#7567](https://github.com/open-mpi/ompi/issues/7567) — some question and thinking about TenosrFlow and OpenMPI's usnic btl | User asks about usNIC BTL queue-pair usage and how to lay out processes for TensorFlow distributed training to improve performance. | n/a | 🗑️ Close | jsquyres answered both questions in detail (vader handles intra-node, recommended 4-process layout, btl_usnic_rd_num tuning) and the reporter followed up; the support question is answered. usNIC is also a niche/legacy transport. Close. |
| [#7630](https://github.com/open-mpi/ompi/issues/7630) — Error on cluster over WAN: ORTE does not know how to route a message to the specified daemon | Two-node launch over WAN fails; initially a version mismatch (unknown --tree-spawn), then ORTE cannot route to the remote daemon. | n/a | 🗑️ Close | First error was a stale/mismatched OMPI install on a node; the residual routing failure is a network/interface reachability issue over WAN, not an Open MPI defect. ORTE was replaced by PRRTE in v5+. Environment/config question; close. |
| [#7733](https://github.com/open-mpi/ompi/issues/7733) — synchronize cuda-aware mpi streams | Request/discussion about making CUDA-aware MPI synchronize with user CUDA streams (e.g. wait on a user event or accept user-provided streams) so MPI calls honor stream ordering. | n/a | 🗑️ Close | Maintainers explained at length that CUDA-aware MPI does not adopt CUDA stream semantics by design and the correct pattern is to cudaStreamSynchronize before MPI calls (standard MPI/CPU semantics); the original asker's follow-up questions were answered and acknowledged. Stream-oriented MPI is effectively wontfix/out-of-design-scope and the usage question is answered; recommend closing. |
| [#7841](https://github.com/open-mpi/ompi/issues/7841) — WARNING: There was an error initializing OpenFabric device --with-verbs | User sees an OpenFabrics (openib BTL) init warning when configuring 4.0.4 --with-ucx and --with-verbs, and asks what 'verbs' means and how to avoid the warning. | n/a | 🗑️ Close | Answered in-thread: openib/verbs is unmaintained/deprecated, removed in 5.0, and the fix is to use UCX with --without-verbs (which the user confirmed worked); openib no longer exists on main. Usage question fully answered and the underlying component is gone; recommend closing. |
| [#7857](https://github.com/open-mpi/ompi/issues/7857) — Device binding according to process | User wants per-process IB device selection and found a 10-20% gain by manually setting UCX_NET_DEVICES; investigation showed UCX picks both rails for one socket instead of the closest device. | n/a | 🗑️ Close | Discussion concluded the device-affinity selection is a UCX behavior (UCX picks devices by CPU locality, and the suboptimal multi-rail choice is internal to UCX), making this a UCX configuration/issue rather than an Open MPI defect or feature gap. Effectively answered and out of OMPI scope; recommend closing. |
| [#7861](https://github.com/open-mpi/ompi/issues/7861) — UCX iWARP support does not provide the same performance as the openib counterpart? | User saw very poor (TCP-level) latency running pml/ucx over an iWARP (Chelsio) card and asked how to make UCX work well with iWARP. | n/a | 🗑️ Close | Thoroughly answered: UCX does not support iWARP RDMA (only plain POSIX sockets), so users should use the openib BTL (v4.x) or the OFI MTL/BTL via libfabric verbs+rxm. Discussion concluded with working configs; this is a usage question with no further action needed. |
| [#7929](https://github.com/open-mpi/ompi/issues/7929) — Cross Memory Attach | User asked how to check whether their Open MPI was built with CMA enabled when they no longer have the configure line. | n/a | 🗑️ Close | Fully answered: run ompi_info --param btl sm -l 3 (or vader in v4.0.x) and inspect btl_(sm\|vader)_single_copy_mechanism. Simple support question, resolved; nothing left to do. |
| [#8058](https://github.com/open-mpi/ompi/issues/8058) — Problem with MPI_Init_thread | Singleton MPI_Init_thread fails with ess_singleton errors and missing help files on Ubuntu 16.04 with Open MPI 4.0.1, due to inconsistent install paths/environment. | n/a | 🗑️ Close | A user environment/installation issue (mismatched install prefix in .bashrc vs the binary's path); commenters diagnosed it and a later user confirmed setting LD_LIBRARY_PATH and OPAL_PREFIX resolves it. ORTE-era singleton path in v4.0.x; the question is answered, so close. |
| [#8072](https://github.com/open-mpi/ompi/issues/8072) — Where to install and configure OpenMPI in a cluster ? | A new user asked general setup questions about where to install Open MPI on a MOAB/Torque cluster and whether passwordless ssh is required. | n/a | 🗑️ Close | Pure usage/support question with no comments and no reproducible defect, six years old; this is general support best handled by docs/mailing list. Recommend closing as stale/answered. |
| [#8112](https://github.com/open-mpi/ompi/issues/8112) — Unexpected behaviour with passive One side communication MPI_Win_lock/MPI_Win_unlock | User observed much slower passive-target RMA lock/unlock when using MPI_Win_create (with MPI_Alloc_mem or malloc) vs MPI_Win_allocate on an Omni-Path system, and asked whether this is expected. | n/a | 🗑️ Close | A maintainer explained the behavior is expected (MPI_Win_allocate is preferred and faster, and the deprecated osc/pt2pt path used on OPA is slow and lacks THREAD_MULTIPLE), advising btl/ofi + osc/rdma. osc/pt2pt is now removed on main and osc/rdma/ucx are the supported paths; the question is answered. Recommend closing. |
| [#8157](https://github.com/open-mpi/ompi/issues/8157) — coll_tuned_dynamic_rules_filename option | User asked how to use coll/tuned dynamic rules files for fine-grained collective algorithm selection; the thread answered the format and later surfaced a counter-intuitive message-size definition (msg size scaled by comm size). | n/a | 🗑️ Close | Maintainers fully answered the usage question (file format, algorithm IDs, that all collectives need a size-0 rule). The remaining design concern about the confusing message-size semantics is already tracked in the still-open #7672, so this thread itself is answered/duplicated. Recommend closing in favor of #7672. |
| [#8193](https://github.com/open-mpi/ompi/issues/8193) — orte_ess_init failed while trying to run Horovod application | Running a Horovod (Python+threaded MPI) application with mpirun under Open MPI 4.1.0rc2 fails during MPI_Init with 'orte_ess_init failed', while OSU benchmarks run fine. | n/a | 🗑️ Close | Maintainer diagnosed it as missing/inconsistent PMIx libraries or an OMPI version mismatch on backend nodes (an environment problem), labeled 'question'; a later commenter confirmed the same error from mixing OMPI versions. ORTE no longer exists on v5.0+/main. The issue is answered as a setup problem; recommend closing. |
| [#8436](https://github.com/open-mpi/ompi/issues/8436) — How to use MPI_Iallreduce with OpenMPI | User asks which environment variables/flags enable asynchronous progress for non-blocking collectives like MPI_Iallreduce in Open MPI, analogous to Cray MPICH settings. | n/a | 🗑️ Close | This is a usage question with no comments; Open MPI handles async progress differently (e.g., async-progress thread / coll components) and no defect is involved. Recommend closing as a question. |
| [#8806](https://github.com/open-mpi/ompi/issues/8806) — NCCL and CUDA-aware MPI? | User asks how to safely use NCCL concurrently with CUDA-aware Open MPI given NCCL's documented deadlock risk requiring global synchronization. | n/a | 🗑️ Close | Maintainers answered that Open MPI issues no GPU kernels without active MPI communication, so completing all outstanding MPI requests (MPI_Wait) before switching contexts is sufficient to implement safe epochs; the question was answered and the reporter never came back, so close. |
| [#9079](https://github.com/open-mpi/ompi/issues/9079) — Questions about SRQ depletion | Asks about default SRQ counts, Subnet-Manager behavior on SRQ exhaustion, and how to address poor collective performance attributed to SRQ depletion when using the OpenIB BTL. | n/a | 🗑️ Close | Another user answered with the btl_openib_receive_queues MCA parameter and tuning values, which the reporter confirmed improved performance; this is a usage question, and the openib BTL is itself deprecated/removed in favor of UCX on current branches. Answered; recommend closing. |
| [#9508](https://github.com/open-mpi/ompi/issues/9508) — Can the target service RMA MPI_Get requests in parallel threads? | User asks how Open MPI implements RMA and whether the target can service multiple MPI_Get passive-synchronization requests in parallel as scaling worker count increases walltime. | n/a | 🗑️ Close | A maintainer thoroughly answered (RDMA hardware handles parallel transfers; use MPI_Win_allocate and UCX/IB for best results; the slowdown is likely a bandwidth/window-type issue), and the thread tapered off with the user planning further tests. This is a usage question that was answered. Recommend closing. |
| [#9690](https://github.com/open-mpi/ompi/issues/9690) — cuda-aware build fails cuda runtime linking with undefined symbol cuIpcOpenMemHandle_v2 | User building CUDA-aware Open MPI 4.1.1 gets a runtime dlsym failure for cuIpcOpenMemHandle_v2 (undefined symbol in libcuda.so.1), disabling CUDA-aware support. | n/a | 🗑️ Close | Maintainers explained this is a CUDA library/LD_LIBRARY_PATH mismatch (configure built against /usr/local/cuda but a different libcuda.so resolved at runtime); workaround mpirun -x LD_LIBRARY_PATH was given. This is a usage/environment question, not an Open MPI defect. Recommend closing. |
| [#9751](https://github.com/open-mpi/ompi/issues/9751) — Failing to build an RPM for openmpi 1.10.7 | User cannot build an RPM of the ancient Open MPI 1.10.7 because buildrpm.sh misdetects the RPM top dir and errors on /usr/src/redhat. | n/a | 🗑️ Close | rhc54 explained the fix (edit the offending line in buildrpm.sh) and the reporter confirmed it worked; follow-up about openmpi-devel was also answered. v1.10 is EOL. Recommend closing as an answered support question. |
| [#9838](https://github.com/open-mpi/ompi/issues/9838) — [openmpi-4.1.2] No components were able to be opened in the pml framework | On an OPA/HFI fabric, user forced pml/osc ucx and got pml framework open failures; UCX is not appropriate for OPA and mtl/ofi should be used. | n/a | 🗑️ Close | Discussion concluded UCX correctly disqualifies itself on OPA in recent versions (per #8496 backports) and mtl/ofi is the correct path for OPA; this is a v4.1.x-era usage/config question that was answered and needs no code change. |
| [#9858](https://github.com/open-mpi/ompi/issues/9858) — How do I use OpenMpi in AWS EFA | User could not run multi-node jobs over AWS EFA; their EasyBuild Open MPI 4.0.5 was not configured with libfabric so the ofi mtl could not select the efa provider. | n/a | 🗑️ Close | AWS engineers diagnosed that the user's Open MPI was built without libfabric and advised using the EFA-installer Open MPI; this is a v4.0.x build/usage support question that was answered and is not an Open MPI defect. |
| [#9918](https://github.com/open-mpi/ompi/issues/9918) — ORTE_ERROR_LOG: Data unpack had inadequate space in file base/plm_base_launch_support.c at line 1213 | Launching across two machines running different Ubuntu versions failed with a data-unpack error during daemon startup, attributed to mismatched/differently-configured Open MPI installs and PATH/LD_LIBRARY_PATH not propagating over non-interactive SSH. | n/a | 🗑️ Close | This is an ORTE-era (v4.1.x) cross-version/environment setup question; maintainers explained the likely cause (mismatched library versions and non-interactive-login env not set) and advised aligning installs, so it is answered and not an Open MPI defect. |
| [#10025](https://github.com/open-mpi/ompi/issues/10025) — Orted hanging(?) while importing TensorFlow. | Importing TensorFlow intermittently hangs in orted startup; root cause was hwloc's GL/X11 backend probing TCP port 6006 (TensorBoard) which never replies. | n/a | 🗑️ Close | Diagnosed as an hwloc/X11 issue (port 6000+ assumed to be an X11 server); workarounds (move TensorBoard port, HWLOC_COMPONENTS=-gl) confirmed fixing it, and PMIx later added component blacklisting. Question is fully answered, not an OMPI bug; close. |
| [#10045](https://github.com/open-mpi/ompi/issues/10045) — Cannot use SHARP with Singularity container | User saw no SHARP debug output when running NVIDIA's HPL Singularity container, whose embedded Open MPI was built with UCX but without HCOLL. | n/a | 🗑️ Close | Maintainers explained SHARP is enabled only through HCOLL and the container's MPI lacked HCOLL, plus HPL does not exercise SHARP; this is a configuration/usage question (not an OMPI defect) and the reporter went silent, so close. |
| [#10261](https://github.com/open-mpi/ompi/issues/10261) — Mpi4.1.0 How to check what allreduce algorithm was used and 'comm too small' message meaning | User asks how to determine which allreduce algorithm/coll component is selected and what the 'intercomm, comm is too small, or not all peers local; disqualifying myself' verbose message means. | n/a | 🗑️ Close | bosilca gave a complete explanation of coll component selection, priorities, and per-call algorithm selection; the question was fully answered with no remaining defect; close. |
| [#10303](https://github.com/open-mpi/ompi/issues/10303) — Unnormal Performance of MPI_Irecv() (GPUDirect RDMA asymmetry) | User asks why osu_bw Host-to-Device vs Device-to-Host GDR bandwidth is asymmetric without device affinity using HPC-X UCX on DGX A100. | n/a | 🗑️ Close | An UCX maintainer explained PCIe read vs write asymmetry and outstanding-transaction limits for GDR; the user thanked and accepted the answer, no OMPI defect; close. |
| [#10417](https://github.com/open-mpi/ompi/issues/10417) — Is there any Scientific or Engineering Application dominated by the performance of LARGE message MPI_Allreduce? | User asks which scientific/engineering applications are dominated by large-message MPI_Allreduce performance for benchmarking a new algorithm. | n/a | 🗑️ Close | Pure usage/discussion question; maintainers answered with HPCG and the deep-learning/Mini-AMR apps the user already knew. Not a defect; recommend closing as answered. |
| [#10474](https://github.com/open-mpi/ompi/issues/10474) — Point to point protocols sometimes take a long time to complete | User sees occasional multi-second spikes in halo-exchange Sendrecv/Isend timings on a single node and asks for the cause. | n/a | 🗑️ Close | Diagnosed in discussion as OS/scheduling noise (confirmed by another user's netgauge measurements) plus a suboptimal unexpected-message communication pattern; no Open MPI defect identified and detailed tuning advice was given. Recommend closing as answered/support. |
| [#10535](https://github.com/open-mpi/ompi/issues/10535) — A process or daemon was unable to complete a TCP connection | User's self-built Open MPI 4.1.2 failed rsh launch with a TCP connection error to a worker, while the distro package worked with the identical firewall setup. | n/a | 🗑️ Close | User self-diagnosed it as a local misconfiguration (distro package worked fine), so this is a usage/environment question, not an Open MPI defect, on an EOL v4.1.x. Close as answered/self-resolved. |
| [#10649](https://github.com/open-mpi/ompi/issues/10649) — The mpirun -n 20 program was run 20 times with one command | User running an ancient Open MPI 1.10.7 saw each process run the program independently instead of as one MPI job, symptomatic of mpirun/libmpi version mismatch. | n/a | 🗑️ Close | Maintainers diagnosed a likely mpirun/libmpi mismatch and advised upgrading from the 2017-era 1.10.7; reporter never followed up and the version is long unsupported. Close as answered (usage/environment issue on EOL version). |
| [#10717](https://github.com/open-mpi/ompi/issues/10717) — Question about how OMPI handles OFI HMEM and completion semantics | A support question asking how the OFI MTL ensures CUDA/HMEM buffers are fully synced at transfer completion since (unlike the BTL) it does not request FI_DELIVERY_COMPLETE. | n/a | 🗑️ Close | Discussion concluded that completion semantics rely on the libfabric provider setting CU_POINTER_ATTRIBUTE_SYNC_MEMOPS and on apps following GPUDirect-RDMA rules, with NCCL-specific flush behavior being a separate concern; the question was thoroughly answered and is a discussion rather than an open defect, so it can be closed. |
| [#10735](https://github.com/open-mpi/ompi/issues/10735) — [Question] ORTE daemon behavior on non-leader machines | A series of usage questions about ORTEd daemon lifetime, ssh fanout/tree-spawn, detecting mpirun completion, and 'lost communication with a remote daemon' errors (likely flaky TCP or OOM killer). | n/a | 🗑️ Close | rhc54 and jsquyres answered all questions in detail (daemon lifetime, tree spawn, keepalive MCA params, OOM-killer/TCP causes); this is support, not a defect, and uses the legacy ORTE (now PRRTE), so it can be closed as answered. |
| [#10841](https://github.com/open-mpi/ompi/issues/10841) — Device vendor ID: 0x02c9, part ID: 4123, WARNING: No preset parameters were found for the device that Open MPI detected | On 4.1.1 with ConnectX-6, the deprecated openib BTL emits 'WARNING: error initializing an OpenFabrics device'/'No preset parameters' and segfaults with --mca btl openib, while UCX is the correct transport. | n/a | 🗑️ Close | Maintainers explained UCX is the supported path and recommended --mca btl ^openib, confirming UCX was in fact being selected and working; openib is removed in 5.0.x+, so the warning/segv concern is tied to the deprecated/removed BTL and the question was answered, recommend closing (a nicer error message is a minor wish, not a tracked defect here). |
| [#10926](https://github.com/open-mpi/ompi/issues/10926) — Connect to the wrong network interface using Slurm | User running over Slurm/pmix saw Open MPI's TCP BTL try to connect over the docker0 interface (172.17.0.1) instead of the intended NIC, causing hangs/failures unless btl_tcp_if_include was set. | n/a | 🗑️ Close | This is expected behavior: a maintainer explained Open MPI tries all available interfaces and users must include/exclude as appropriate; the workaround (btl_tcp_if_include/OMPI_MCA_btl_tcp_if_include) was confirmed working. Usage question, answered, recommend close. |
| [#10940](https://github.com/open-mpi/ompi/issues/10940) — PMIX ERROR: NOT-FOUND in file ptl_usock.c at line 175 , fails when running 2 cmds inside bash script | User direct-launching (srun --mpi=pmix_v3) two python MPI commands in a bash script inside an enroot/pyxis container on Open MPI 4.1.2; the second command's MPI_Init fails with PMIX NOT-FOUND / 'OMPI not built with SLURM PMI support'. | n/a | 🗑️ Close | Classic mismatch between Slurm's PMIx plugin version and Open MPI's PMIx; a 4.1.2 site-configuration/launch issue with no project defect identified, no comments, and stale since 2022. Recommend close (needs reporter info but practically obsolete on EOL 4.1.x). |
| [#11063](https://github.com/open-mpi/ompi/issues/11063) — Problems when running examples hello_c | On Open MPI 4.1.2 with ConnectX-4 Lx ports configured as Ethernet (not InfiniBand), the openib BTL prints 'No OpenFabrics connection schemes' warnings; the job still runs and works once openib is disabled (UCX is selected). | n/a | 🗑️ Close | A misconfiguration/usage issue with the now-removed openib BTL on Ethernet-mode mlx5; workaround '-mca btl ^openib' confirmed working and UCX handles communication. openib no longer exists on main (replaced by ofi/uct), so it is moot for current releases. Recommend close. |
| [#11098](https://github.com/open-mpi/ompi/issues/11098) — openmpi build error | A new user following a decade-old (Open MPI 1.4.4, 2011) tutorial hit a build error involving the 'ar' tool while compiling Open MPI. | n/a | 🗑️ Close | Maintainer (jsquyres) answered: the failure is a missing binutils/core developer-tools install, and the user was advised to use a modern version rather than the 2011 release. Usage question, answered, stale since 2022; recommend close. |
| [#11136](https://github.com/open-mpi/ompi/issues/11136) — A problem about hostfile | User got hostfile and ORTE errors running mpirun on Open MPI 4.1.1, caused by mixing slot-count specification styles in the default hostfile. | n/a | 🗑️ Close | Maintainer pointed out the error message stated the exact problem (mixing slot=N with repeated node listing) and asked for the hostfile; reporter never responded. Usage/configuration question on EOL v4.1.x. Recommend closing as answered. |
| [#11139](https://github.com/open-mpi/ompi/issues/11139) — [Q] MPI_THREAD_MULTIPLE and multiple workers / endpoints | User asks whether Open MPI supports per-thread endpoints / multiple UCX workers on InfiniBand for MT scalability, noting the older CRI/uct-btl path seemed abandoned. | n/a | 🗑️ Close | This is a support/usage question about feature availability with no maintainer follow-up; no defect is reported and it has sat untouched since 2022. The uct btl still exists but the per-thread-endpoint capability the user asked about is not a tracked OMPI feature. Recommend closing as a stale unanswered question. |
| [#11174](https://github.com/open-mpi/ompi/issues/11174) — OMPI fails to connect in a WAN setting | User running across AWS regions over public IPs finds the wired-up TCP connection uses a private/local IP, breaking cross-region MPI; asks for extra WAN setup. | n/a | 🗑️ Close | Maintainer answered with the fix (use oob_tcp_if_include and btl_tcp_if_include MCA parameters to restrict interfaces), which is the standard configuration answer for this networking scenario; reporter never followed up. This is a usage/configuration question, not a defect. Recommend closing as answered. |
| [#11345](https://github.com/open-mpi/ompi/issues/11345) — P vs E cores in Open MPI | User asked how to restrict Open MPI to only the P-cores on a 12th-gen Intel hybrid (P+E) CPU; under WSL2 the topology was reported as 12x2 cores so E-cores could not be distinguished, while native Ubuntu saw the hybrid layout. | n/a | 🗑️ Close | Fully answered: maintainers explained the topology limitation is in WSL/hwloc (not Open MPI), and recommended using --use-hwthread-cpus --bind-to none and letting the OS schedule, or --cpu-list/pe-list to pick P-cores. User said the explanations helped; no Open MPI defect. Recommend close. |
| [#11357](https://github.com/open-mpi/ompi/issues/11357) — Kernel's NUMA balancing vs. OpenMPI's affinity | User sought guidance on how Linux automatic NUMA balancing interacts with Open MPI process affinity/binding and how to benchmark the combinations. | n/a | 🗑️ Close | Answered in detail: maintainer explained that explicit binding overrides autonuma (kernel honors sched_setaffinity), recommended --map-by/--bind-to socket-or-core, and provided sched_getaffinity sample code to monitor migration. Usage/support question, no defect; user thanked and will experiment. Recommend close. |
| [#11366](https://github.com/open-mpi/ompi/issues/11366) — Open MPI make fails with UCX, undefined reference to ucp_tag_recv_nbx | On ppc64le RHEL 8.4, building 5.0.0-rc9 against a custom UCX failed at link with undefined UCX references unless LDFLAGS pointed at the UCX libdir; later, pml ucx runs segfaulted at MPI_Init with UCX 1.13.x while ob1 and UCX 1.12.1 worked. | n/a | 🗑️ Close | User-environment build/link and UCX-version-specific issue against an old rc; workaround (set LDFLAGS / --with-ucx-libdir) made the build succeed and UCX 1.12.1 ran fine. The runtime segfault was never root-caused and points to the user's UCX/GCC/CUDA combo on a now-EOL rc; no actionable Open MPI defect confirmed. Recommend close. |
| [#11386](https://github.com/open-mpi/ompi/issues/11386) — How do I set the value of the "coll_tuned_alltoall_algorithm_segmentsize" parameter? | User asks how to tune coll_tuned alltoall segmentsize/fanout MCA parameters for best collective performance. | n/a | 🗑️ Close | Maintainers (gkatev, bosilca) thoroughly explained the parameter ranges, that fanout params are unused by alltoall, and tuning strategy; this is a usage question that was fully answered and the reporter acknowledged it, so it can be closed. |
| [#11471](https://github.com/open-mpi/ompi/issues/11471) — PMIX - SLURM OpenMPI | srun --mpi=pmix fails to launch MPI jobs across multiple nodes (works on a single node) in a Slurm+PMIx Kubernetes setup. | n/a | 🗑️ Close | Discussion identified this as a Slurm/environment problem (firewall, or a shared NFS slurmd spool dir needing %n), not an Open MPI defect; rhc54 noted it is outside OMPI's PMIx integration. Root causes were found in the environment, so close as answered/out of scope. |
| [#11594](https://github.com/open-mpi/ompi/issues/11594) — is there a GPU-aware build on Frontier? | User asks whether a GPU-aware Open MPI build is available on the Frontier supercomputer. | n/a | 🗑️ Close | A site-specific support question, not an Open MPI defect; a maintainer pinged an ORNL contact but no defect exists. Stale since 2023; recommend closing as a support/usage question. |
| [#11669](https://github.com/open-mpi/ompi/issues/11669) — PML UCX error | User cannot select the UCX PML ('No components were able to be opened in the pml framework') with module-loaded Open MPI 4.1.4 + UCX 1.13.1 running OpenFOAM, plus assorted openib/threading warnings. | n/a | 🗑️ Close | A usage/support issue: UCX was built without multi-thread support while OpenFOAM requests MPI_THREAD_MULTIPLE; contributors suggested -fileHandler uncollated and verifying the UCX build. Not an Open MPI defect; answered. Close as a support question. |
| [#11765](https://github.com/open-mpi/ompi/issues/11765) — Performance issue of MPI_Allgatherv compared to Cray MPICH | User reports MPI_Allgatherv (40MB per rank, 32 nodes over UCX) is several times slower than Cray MPICH, using a custom single-shot benchmark. | n/a | 🗑️ Close | Maintainer explained the methodology issues (no warmup, first-collective connection setup) and suggested proven benchmarks plus tuning coll/tuned vs coll/han algorithms; reporter never followed up with proper measurements. Recommend close as answered / stale; a generic collective being slower than vendor-tuned Cray MPICH is not in itself an actionable defect. |
| [#11785](https://github.com/open-mpi/ompi/issues/11785) — How could i debug openmpi 3.0.5 | User hangs/segfaults in MPI_Init with very old OMPI 3.0.5 (and 4.0.5), with stack traces in btl_openib/grdma; asks how to debug. | n/a | 🗑️ Close | Maintainer advised upgrading from EOL 3.0.5 to v4.1.x; reporter self-resolved by disabling openib (--mca btl ^openib), which is the legacy IB path long superseded by UCX. Both versions are EOL. Recommend close as answered/self-resolved on unsupported versions. |
| [#11832](https://github.com/open-mpi/ompi/issues/11832) — How to have multiple docker containers communicating through MPI? | User asks how to set up several Docker containers (one server, multiple clients) so MPI processes can communicate, proposing static IPs plus a hostfile with mpiexec. | n/a | 🗑️ Close | A maintainer confirmed the proposed hostfile/TCP approach is viable and gave general guidance (same Open MPI version everywhere, mutual TCP reachability); this is a usage question, not a defect, so it can be closed. |
| [#11970](https://github.com/open-mpi/ompi/issues/11970) — MPI attribute MPI_TAG_UB not available on sub-communicators | Reporter observes MPI_Comm_get_attr(MPI_TAG_UB) returns flag=false on a communicator created by MPI_Comm_split, only working on MPI_COMM_WORLD. | n/a | 🗑️ Close | Maintainers explained the MPI standard only mandates MPI_TAG_UB on MPI_COMM_WORLD (world model) or comms created from groups; the behavior is standard-compliant, so this is an answered question and can be closed. |
| [#12092](https://github.com/open-mpi/ompi/issues/12092) — Issue configuring openmpi 5.0.0 with ifort | User could not find mpifort after building Open MPI 5.0.0 with ifort/icx; turned out the build failed early due to a permission error on .libs/pmix_globals.o and a missing space between configure arguments. | n/a | 🗑️ Close | Maintainers diagnosed a filesystem permission problem plus a dropped space in the configure line (--disable-shared--enable-mpi-fortran) and advised a clean rebuild; root cause was user-environment, not an Open MPI defect, and jsquyres already asked to close. Recommend Close as answered. |
| [#12209](https://github.com/open-mpi/ompi/issues/12209) — MPI_TYPE_INDEXED + MPI_SEND/RECV slow with older infiniband network? | User observes MPI_TYPE_INDEXED (non-contiguous) sends are 2-5x slower on a 40G InfiniBand system but not on 100G, and asks how Open MPI/UCX handles indexed types. | n/a | 🗑️ Close | A UCX maintainer (brminich) explained that non-contiguous data limits the protocols UCX/network can use, answering the question; the user's follow-up auto-linearize idea overlaps the CUDA datatype work in 12202. Recommend close as answered. |
| [#12225](https://github.com/open-mpi/ompi/issues/12225) — What is the recommended way of using OpenMPI compiler wrappers for CUDA applications? | User asks the recommended way to compile CUDA (.cu) applications, including a top-level main.cu, with the Open MPI compiler wrappers. | n/a | 🗑️ Close | The usage question was answered (build .cu with nvcc, link with the MPI wrapper; or use --showme/pkg-config); the documentation/feature aspect spun off into separate issue 12228. Recommend close as answered (the wrapper feature is tracked in 12228). |
| [#12314](https://github.com/open-mpi/ompi/issues/12314) — OpenMPI 5.0.1 seems to break compatibilities with CUDA and create memory allocation errors | After upgrading Arch's openmpi to v5 without rebuilding ParaView, the user saw 'unable to open mca_accelerator_cuda: libcuda.so.1 file too short' warnings and an OptiX memory-allocation crash. | n/a | 🗑️ Close | Crash originates in ParaView/OptiX/VisRTX, not Open MPI; the libcuda warning is from a bad libcuda.so.1 on the custom LD_LIBRARY_PATH and the v4->v5 upgrade required a ParaView rebuild. Question answered (not an Open MPI defect), recommend close. |
| [#12422](https://github.com/open-mpi/ompi/issues/12422) — Is there an MCA way to specify server URI in v5.0? | User asks whether v5.x has an environment-variable equivalent to the old --mca pmix_server_uri for specifying the server/DVM URI. | n/a | 🗑️ Close | In v5.x connecting to an external DVM uses PRRTE's --dvm-uri CLI option or the PMIX_SERVER_URI environment variable (present in the bundled PRRTE/PMIx); the thread was never formally answered but the answer exists. Recommend answering and closing. |
| [#12511](https://github.com/open-mpi/ompi/issues/12511) — Scaling issue run openmp on a cluster | User sees no speedup running a hybrid MPI/OpenMP program under salloc, suspecting improper salloc settings. | n/a | 🗑️ Close | Members answered that the salloc request was for 1 cpu so threads were bound to a single core, plus the workload was too small to scale; this is a usage question fully answered with no Open MPI defect. Recommend close. |
| [#12520](https://github.com/open-mpi/ompi/issues/12520) — Errors when running mpi programs | Multi-node TCP/libfabric run on Hetzner hosts with /32 public-IP subnets fails with 'different subnet' connection errors; works when forcing '--mca pml ob1 --mca btl tcp,self'. | n/a | 🗑️ Close | The /32 subnetting confuses interface matching and libfabric was the trigger; a member gave a working workaround and redirected the libfabric-specific part to the libfabric community, which the reporter agreed to. Effectively answered/configuration issue, no Open MPI fix pending; recommend close. |
| [#12567](https://github.com/open-mpi/ompi/issues/12567) — libmpi.so is linked with the wrong libopen-pal.so | User's freshly built libmpi.so resolves to a stale system libopen-pal.so.40 instead of the newly built .so.80, causing undefined-symbol link/run errors. | n/a | 🗑️ Close | Members showed they could not reproduce with a correct environment and traced it to a conflicting system-installed Open MPI plus an unset/incorrect LD_LIBRARY_PATH; a second commenter's pile-on was a different misconfiguration and was redirected to a new issue. This is an environment/usage problem, effectively answered; recommend close. |
| [#12578](https://github.com/open-mpi/ompi/issues/12578) — ImportError: libopen-pal.so.80: cannot open shared object file: No such file or directory | User hits an ImportError for libopen-pal.so.80 and asks what libopen-pal is. | n/a | 🗑️ Close | Members explained libopen-pal is an internal Open MPI support library and the error means the loader cannot find it, typically requiring LD_LIBRARY_PATH to point at the install lib dir; question answered, marked Stale, reporter never followed up. Recommend close. |
| [#12595](https://github.com/open-mpi/ompi/issues/12595) — small array of derived data type(in Fortran) can be sent by MPI_Isend and MPI_Irecv but it ran into errors when I augment the array | Fortran nonblocking send/recv of a derived-type subarray crashes for large arrays; works for small ones. | n/a | 🗑️ Close | Members explained the program is erroneous (no MPI_Wait, same buffer for send and recv) and, crucially, that Open MPI does not support Fortran subarrays with nonblocking comms (MPI_SUBARRAYS_SUPPORTED is .false.), so the runtime frees the flattened temporary before transfer completes. Fully answered and reporter satisfied; recommend close. |
| [#12625](https://github.com/open-mpi/ompi/issues/12625) — Remove script wrappers in v6.0.x | Maintainer asked whether the (not-installed-by-default) script compiler wrappers can be removed in v6.0.x and beyond, prompted by occasional support work. | n/a | 🗑️ Close | Conda-forge confirmed they may use the script wrappers for cross-compilation, so the consensus was to keep them; the discussion concluded and the script wrappers still exist in the tree. The question was effectively answered, so it can be closed. |
| [#12778](https://github.com/open-mpi/ompi/issues/12778) — symbol lookup error: undefined symbol ompi_mpi_errors_throw_exceptions (OpenLB cylinder3d) | An OpenLB user hit a runtime symbol-lookup error and later application assertions/OOM while running a CFD example with Open MPI 5.0.5. | n/a | 🗑️ Close | Maintainers identified this as the user mixing Open MPI 4.x and 5.0 (the missing symbol exists only in 4.x) plus downstream OpenLB application issues; it is a usage/linking problem in the user's application, not an Open MPI defect. Answered; can be closed. |
| [#12848](https://github.com/open-mpi/ompi/issues/12848) — Is having more than one GPU initialized UB in standard cuda-aware MPI? | User asks whether mapping more than one CUDA device per MPI rank (calling cudaSetDevice for an unused device then the used one) is valid or undefined behavior for CUDA-aware MPI. | n/a | 🗑️ Close | A maintainer answered that the MPI standard says nothing about GPUs and that multi-GPU-per-process is a quality-of-implementation matter where most MPIs are optimized for one GPU per process; the question was answered and can be closed. |
| [#12884](https://github.com/open-mpi/ompi/issues/12884) — CPU core duplication in OpenMPI 5.0.5 when running multiple tasks | Two independent mpirun jobs launched on the same node bind their ranks to the same cores, causing core overlap; user expected distinct cores per job. | n/a | 🗑️ Close | Maintainers explained this is expected behavior since independent mpirun invocations cannot know each other's placement, and gave several workarounds (cpu envelope/cpu-set, persistent DVM, srun --mpi=pmix, cgroups); the question is answered and can be closed. |
| [#12948](https://github.com/open-mpi/ompi/issues/12948) — Inquiry About OpenMPI Support for NVSwitch in Inter-Node GPU Communication | User asks whether Open MPI extends GPU peer-to-peer support beyond intra-node NVLink to inter-node peer-to-peer via NVSwitch. | n/a | 🗑️ Close | Pure usage/support question with no defect; no action required from the project. Recommend closing as a question (the inquiry can be answered via mailing list / docs). |
| [#12968](https://github.com/open-mpi/ompi/issues/12968) — openmpi5.0.6 - Multiple nodes cannot run (Error: Connection timed out (110)) | TCP connect() between nodes times out (errno 110) on a two-node directly-connected IB setup, causing multi-node jobs to fail/hang. | n/a | 🗑️ Close | Classic environment/networking misconfiguration (TCP BTL trying to reach an unreachable interface on directly-connected NICs); no Open MPI defect and no comments/diagnosis followed. Recommend closing as a support/config question. |
| [#12990](https://github.com/open-mpi/ompi/issues/12990) — How to specify hostfile using environment variable? | User cannot pass mpirun CLI args (third-party app controls launch) and asks how to set a default hostfile and allow-run-as-root purely via environment variables. | n/a | 🗑️ Close | Both have env-var answers: a default hostfile can be set via the PRTE MCA param (e.g. PRTE_MCA_prte_default_hostfile), and PRTE_ALLOW_RUN_AS_ROOT / _CONFIRM are documented; the docs reference these. A support question with no defect; recommend answering and closing. |
| [#12993](https://github.com/open-mpi/ompi/issues/12993) — Program Hangs When Number of Process Exceeds 308 | Under srun --mpi=pmix on an 8-node cluster, jobs hang once process count exceeds ~308, with PMIX_ERR_NOMEM errors from the PMIx gds/shmem component; launching with mpirun works fine. | n/a | 🗑️ Close | Reporter confirmed mpirun works and only srun/Slurm-PMIx fails, with NOMEM in PMIx gds/shmem (an external PMIx/Slurm integration issue, workaround PMIX_MCA_gds=hash). Not an Open MPI defect; recommend closing as a Slurm/PMIx support question. |
| [#13118](https://github.com/open-mpi/ompi/issues/13118) — Help with launching MPI application | User asks how to bind rank 0 to 2 cores (for a file-I/O helper pthread) while binding all other ranks to 1 core each, using Open MPI 4.0.7 under PBS. | n/a | 🗑️ Close | A usage/support question with no defect, achievable via a rankfile or per-rank PE mapping; no reporter follow-up. Recommend closing as a support question (also EOL 4.0.x). |
| [#13128](https://github.com/open-mpi/ompi/issues/13128) — MPI One-sided applications only work via Infiniband adapter as expected, but not via Ethernet adapter | User cannot force MPI one-sided (RMA) communication over Ethernet/TCP instead of InfiniBand; without UCX they hit osc/pt2pt hangs and MPI_Win_allocate errors on a 4.1.x build. | n/a (osc/pt2pt removed in 5.0; EOL 4.1.x) | 🗑️ Close | A configuration/support question; maintainer explained osc/pt2pt is unmaintained (removed in 5.0) and suggested workarounds, after which the user hit further setup errors on EOL 4.1.4. Recommend closing as answered, pointing to 5.0.x with a TCP-capable osc. |
| [#13196](https://github.com/open-mpi/ompi/issues/13196) — mpiexec 4.1.3, how to avoid transfer PMIX_INSTALL_PREFIX to the child process of orted | User with per-node-differing Open MPI install prefixes asks how to stop PMIX_INSTALL_PREFIX being inherited by orted children when relocating into per-node sandboxes. | n/a | 🗑️ Close | Maintainers explained PMIX_INSTALL_PREFIX is internal and that OPAL_PREFIX/PMIX_PREFIX are the right relocation knobs, plus suggested launch/fork agents, chrpath, static builds, or a uniform sandbox path; the question was fully answered, so recommend Close. |
| [#13346](https://github.com/open-mpi/ompi/issues/13346) — openpmix 6.0.0 and openMPI 5.0.8 configure problem | User hit a configure error (PMIX_CAP_LTO incompatible with PRRTE) trying to build Open MPI 5.0.8 against the new external PMIx 6.0.0. | n/a | 🗑️ Close | Maintainers fully explained that PMIx v6 / PRRTE v4 form an intentional compatibility break, so OMPI 5.0.x must use external PMIx v5 + PRRTE v3 (or the embedded versions); the reporter confirmed PMIx 5.0.8 works, so the question is answered. |
| [#13544](https://github.com/open-mpi/ompi/issues/13544) — Why does UCC consistently insist on using software to simulate PUT/GET operations? | User on a custom shared-memory UCX transport asks why MPI_Allreduce (via UCC/OSU benchmark) appears to use AM/software-emulated PUT/GET rather than RDMA. | n/a | 🗑️ Close | Maintainer explained OMPI internal allreduce uses pt2pt send/recv (not RMA), and UCC choosing AM for small allreduce is expected benchmark behavior; reporter acknowledged 'that makes sense.' Not an Open MPI defect. Answered; recommend close. |

## Awaiting reporter (16)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#9087](https://github.com/open-mpi/ompi/issues/9087) — IB communication speed per process | User asks why single-process InfiniBand bandwidth peaks around 20 Gbps while aggregate across many processes is much higher, and how to increase per-process speed. | n/a | 🗑️ Close | This is a UCX/InfiniBand performance tuning question, not an Open MPI defect; reporter posted ibv_devinfo but never followed up after 2021. Stale support question with no actionable bug; recommend closing. |
| [#7085](https://github.com/open-mpi/ompi/issues/7085) — illegal node name | User's hostname contained an underscore, causing the regex node-name parser to reject it; after changing $HOSTNAME and rebuilding, the error persisted. | n/a | ❓ Needs info | This is a usage/configuration question (underscores are not valid in hostnames per RFC); a maintainer asked how the hostname was set and what 'uname -n' returns, with no reply from the reporter since 2019. Recommend a final ping/close-if-no-response. |
| [#7120](https://github.com/open-mpi/ompi/issues/7120) — SSH not execute when some node is same with the host performs mpirun | User launches mpirun on host.a with a custom hostfile script; the script runs on the remote host.b but fails on the local host.a with 'unable to find the specified executable', because mpirun does not SSH to (or source .bashrc on) the local node. | n/a | ❓ Needs info | This is a usage issue (PATH/.bashrc not sourced for the local node since mpirun does not ssh to itself); several maintainers asked clarifying questions (does the script exist locally, is '.' in PATH, source .bashrc first) with no reply from the reporter since 2019. Recommend a final ping/close-if-no-response. |
| [#8548](https://github.com/open-mpi/ompi/issues/8548) — PMIX ERROR: BAD-PARAM in file base/bfrop_base_unpack.c at line 692 | PMIX BAD-PARAM warnings appear (non-fatal) when running WRF under SLURM srun --mpi=pmix with Open MPI 4.1.0, attributed to a likely PMIx version mismatch between SLURM and Open MPI. | n/a | ❓ Needs info | Maintainers diagnosed a probable PMIx version mismatch (recommend building OMPI --with-pmix matching SLURM's PMIx) and asked for follow-up; reporter never confirmed and the thread stalled in 2021, so this is a stale support question on an EOL version that should be pinged and closed. |
| [#8819](https://github.com/open-mpi/ompi/issues/8819) — Low scalability | User reports poor single-node speedup with a legacy MPI code (vs. results from ~2010) on old distro-packaged Open MPI (1.10.x and 3.1.3) and separately a TCP-connect failure across two nodes. | n/a | ❓ Needs info | Maintainers asked the user to compare against MPICH to isolate whether Open MPI is at fault and the multi-node failure was diagnosed as a wrong-interface/firewall issue (resolved via btl/oob_tcp_if_include); the user said they were still working on it and never reported back, so this stale support question should be pinged and closed. |
| [#9452](https://github.com/open-mpi/ompi/issues/9452) — orterun error code 911 / process not bound running GAMESS LIBCCHEM | User cannot run a CUDA GAMESS/LIBCCHEM job under SLURM with orterun; the app aborts via MPI_ABORT with DDI error code 911 after reporting the process is not bound. | n/a | ❓ Needs info | This is an application-level abort (DDI error 911) in third-party software, not a demonstrated Open MPI defect; a maintainer asked where it fails and the reporter never followed up. Recommend pinging the reporter and closing for lack of response if none comes. |
| [#10601](https://github.com/open-mpi/ompi/issues/10601) — An error occurred in MPI_Init (seems the network initialization has some bugs) | MPI_Init failures on Omni-Path/PSM2 (assign_context busy, can't open hfi unit) when oversubscribing HFI contexts; OFI BTL opening an extra context per process disabled PSM2 context sharing. | v4.1.x (EOL) | ❓ Needs info | Cornelis maintainer explained the HFI-context exhaustion cause and requested an opcapture/support case, but the reporter never supplied the diagnostic data; this is a PSM2/Omni-Path configuration issue on EOL v4.1.x. Awaiting reporter; close if no response. |
| [#11876](https://github.com/open-mpi/ompi/issues/11876) — How to enable NVLink support with OFI for Slingshot 11 | User asks how to enable NVLink GPU support when building Open MPI 5.x with libfabric/OFI and CUDA on Perlmutter (Slingshot 11). | n/a | ❓ Needs info | This is a build/usage configuration question with no replies and no defect identified; with no response from the reporter and an old 5.0.0rc12 build, ping for current status and close if stale. |
| [#12048](https://github.com/open-mpi/ompi/issues/12048) — MPI CUDA USAGE | User reports that ompi_info shows mpi_built_with_cuda_support:value:false despite configuring with --with-cuda, and separately hit a PMIx-headers configure error. | n/a | ❓ Needs info | A usage/support question, not a confirmed defect; maintainers repeatedly requested the full configure/build data per getting-help and the reporter only uploaded a zip at the end with no follow-up since Nov 2023. Recommend Needs info; close if no response. |
| [#12452](https://github.com/open-mpi/ompi/issues/12452) — Q: preferred way to implement GPU communication | User asks which approach (indexed datatype with Isend vs MPI_Pack to GPU/CPU buffer then Isend) is most efficient for unstructured GPU-to-GPU data exchange. | n/a | ❓ Needs info | Pure usage/performance-advice question with zero comments or maintainer reply since April 2024. Recommend answering or pinging the reporter; close if no further interest. |
| [#12473](https://github.com/open-mpi/ompi/issues/12473) — Problem when running mpirun on an Ubuntu computer cluster error (ORTE does not know how to route a message...) | Multi-node mpirun hangs with an ORTE 'does not know how to route a message to the specified daemon' error on a small Ubuntu cluster. | n/a | ❓ Needs info | Classic v4.1.x ORTE routing/connectivity setup problem; a maintainer asked for the full question template and the reporter only replied 'ok' with no further info. Recommend closing for lack of information (it is also effectively duplicated by #12476). |
| [#12999](https://github.com/open-mpi/ompi/issues/12999) — OpenMPI combined with RDMA | User running Open MPI 4.1.5 over UCX/RDMA gets unspecified errors when extending from local RDMA to cross-node communication via a hostfile. | n/a | ❓ Needs info | No reproducer, error text, or environment details were provided; jsquyres asked the reporter to supply the required getting-help information and there was no follow-up. Awaiting reporter; close if no response. |
| [#13317](https://github.com/open-mpi/ompi/issues/13317) — Running openmpi with different rank on two docker on same host | User running NCCL all_reduce_perf across two Docker containers on one host sees both processes as rank 0; maintainer notes each appears to be its own size-1 MPI_COMM_WORLD. | n/a | ❓ Needs info | Maintainer explained the symptom (two independent jobs each MPI_COMM_WORLD size 1, likely a launch/hostfile/networking setup problem) and asked for the actual mpirun command, which was never provided; awaiting reporter so ping and close if no response. |
| [#13326](https://github.com/open-mpi/ompi/issues/13326) — Error string: /lib64/libcuda.so.1: undefined symbol: cuIpcOpenMemHandle_v2 CUDA-aware support is disabled | User building CUDA-aware Open MPI 4.1.8 + UCX 1.19 sees an undefined-symbol error for cuIpcOpenMemHandle_v2 and asks whether the missing nv_peer_mem/nvidia-peermem driver module is the cause. | n/a | ❓ Needs info | Maintainer suggested it is likely an LD_LIBRARY_PATH/CUDA-driver-version environment issue and asked the user to retry with mpirun -x LD_LIBRARY_PATH; the user had not yet tested, so this is an environment question awaiting reporter feedback, ping and close if no response. |
| [#13340](https://github.com/open-mpi/ompi/issues/13340) — Performance regression in atmospheric model when using OpenMPI traced to slow memset | User reports a 2-4x slowdown vs Intel MPI with time attributed to memset, suspecting an Open MPI defect. | n/a | ❓ Needs info | Maintainers explained the likely cause is differing default mapping/binding policy (OMPI maps by NUMA, IMPI tightly packs) and asked the user to compare with --display-map; no reproducer or follow-up was provided, so this is awaiting reporter and likely a config/usage matter rather than a defect. |
| [#13371](https://github.com/open-mpi/ompi/issues/13371) — Q: MPI-IO hint to disable/enable data sieving | Darshan developers ask whether/how data sieving can be disabled via an MPI_Info hint on a per-file basis to avoid a sieving bug present in Open MPI <= 5.0.5. | n/a | ❓ Needs info | This is a usage question with no maintainer answer; the underlying sieving bug was fixed (PR 12759) so the workaround need is reduced, but the specific hint question was never answered, so ping the reporter and close if moot. |

## Cannot reproduce / needs info (20)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#7263](https://github.com/open-mpi/ompi/issues/7263) — OpenMPI+Open MP: The available threads decreased over iterations | User reports that combining OpenMPI processes with OpenMP threading causes available threads to decrease each iteration until only one remains. | n/a | 🗑️ Close | No reproducer, version details, or code were provided and there were no responses; this is almost certainly a user OpenMP threading/affinity issue rather than an Open MPI defect, so close (or needs info) given it is stale since 2019. |
| [#7378](https://github.com/open-mpi/ompi/issues/7378) — Cannot find an UCX device after updating to CentOS 7.7? | Mixed CentOS 7.6/7.7 jobs fail with UCX endpoint-unreachable errors after a driver update renamed the IB device (mlx4_0 to rdmap3s0) on ConnectX-2/QDR hardware. | n/a | 🗑️ Close | UCX maintainer noted ConnectX-2/QDR is unsupported old hardware and requested debug logs that were never provided; this is a UCX/driver support question on EOL hardware, stale since 2020, so close. |
| [#9218](https://github.com/open-mpi/ompi/issues/9218) — OpenMPI periodically reports connection reset by peer | Intermittent 'recv() failed: Connection reset by peer (104)' from btl/tcp on multi-interface (docker0/IB) Ethernet clusters with Open MPI 3.1.6/4.0.x, sometimes causing jobs to hang or fail. | n/a | 🗑️ Close | Maintainer could not reproduce; likely an environment/interface-selection or network issue rather than a clear Open MPI defect, and the affected versions (3.1.6/4.0.x) are EOL. No reproducer was provided and the thread went stale in 2021; recommend closing as unreproducible. |
| [#10251](https://github.com/open-mpi/ompi/issues/10251) — Calling python script with mpirun within a pytest does not work | User reports mpirun fails (exit 256) only when invoked from inside a pytest, with stray PMIX/OMPI singleton env vars set, on Open MPI 4.0.3. | n/a | 🗑️ Close | A maintainer could not reproduce; analysis showed pre-set PMIX/OMPI env vars (effectively nested launch) in the user's pytest setup, an environment issue not an OMPI defect, on long-EOL 4.0.x; close as not reproducible. |
| [#10730](https://github.com/open-mpi/ompi/issues/10730) — Try to install mpi4py+openmpi using conda on Ubuntu 20.04 but failed | opal_init fails with opal_shmem_base_select failed when running mpiexec/mpirun from a conda-forge openmpi+mpi4py environment on Ubuntu 20.04 (likely a packaging/environment issue). | n/a | 🗑️ Close | ggouaillardet could not reproduce with miniconda and the reporter worked around it by using pip instead of conda; this is an environment/conda-forge packaging matter not reproduced by maintainers and effectively resolved for the user, so recommend closing. |
| [#10955](https://github.com/open-mpi/ompi/issues/10955) — Processes can't communicate with infiniband on lsf cluster | On an LSF cluster, an Open MPI 4.1.4 job runs fine over TCP but fails over InfiniBand (UCX cannot connect between nodes; openib falls back and aborts) when submitted through LSF. | n/a | 🗑️ Close | Maintainer (jjhursey) diagnosed it as an IB/UCX fabric connectivity/system-configuration problem (ucx_perftest between worker nodes failed), not an Open MPI defect; reporter could not complete diagnosis. Usage/support issue, stale since 2022, recommend close. |
| [#12845](https://github.com/open-mpi/ompi/issues/12845) — Facing issue using mpirun as Grid points increased | A user's OpenLB CFD application aborts with a C++ 'out of memory' runtime_error when grid resolution is increased, with the crash inside the application's CUDA allocation code. | n/a | 🗑️ Close | The traceback is entirely in the user's application (libolbcuda allocating a huge CyclicColumn) running out of memory, not in any Open MPI code; this is a user resource/application issue with no Open MPI defect, no maintainer engagement, and should be closed. |
| [#7682](https://github.com/open-mpi/ompi/issues/7682) — Performance drop of MPI_Allreduce (ring allreduce) as the number of containers increases | User benchmarks MPI_Allreduce over TCP in Docker containers and reports that effective bandwidth drops as the number of containers grows from 2 to 8. | n/a | ❓ Needs info | No comments, no maintainer engagement, and the report is a performance-tuning question over a container TCP bridge (not a clear defect); behavior likely reflects TCP/container topology overheads rather than an OMPI bug. Stale since 2020 with no reproducer on a supported transport; recommend pinging for info and closing if none. |
| [#8964](https://github.com/open-mpi/ompi/issues/8964) — shmem get throughput plummeting after 64KB get in IB | Reporter sees OpenSHMEM shmem_get throughput collapse past 64KB over ConnectX-3 IB with UCX, and is unsure whether it is an Open MPI, UCX, or fabric/SoftRoCE issue. | n/a | ❓ Needs info | No maintainer response and no comments; the reporter himself believed it was a UCX/transport-layer issue and posted it upstream too. It is a performance/support question against an old (v4.1.0) build with no reproducer; recommend pinging for current-version status and closing if no response. |
| [#10071](https://github.com/open-mpi/ompi/issues/10071) — mpirun args for multi-node run with multiple cores per MPI task | --map-by slot:PE=N --bind-to core works on one node but on multi-node Slurm jobs errors with 'binding more processes than cpus', placing all ranks on the first node. | v4.1.x (ORTE/Slurm 19.05); needs retest on v5.0.x/PRRTE | ❓ Needs info | Likely an ORTE/Slurm 19.05 binding interaction (an explicit rankfile works as a workaround); never root-caused and the entire launch stack moved from ORTE to PRRTE in v5.0, so it needs retest on a current release before keeping open. |
| [#11504](https://github.com/open-mpi/ompi/issues/11504) — mpiexec: Forwarding signal 18 to job \| attempt stopped \| linux server \| no hanging up | User's long-running mpiexec job with 16 ranks stopped after 'Forwarding signal 18 (SIGCONT) to job' with no other error, while a 4-rank run succeeded. | n/a | ❓ Needs info | Signal 18 forwarding is normal job-control behavior, not a crash; the report lacks any actual error and is about a third-party app (microhh) on EOL 4.1.5. No reproducer or follow-up; recommend pinging reporter and closing if no response. |
| [#11709](https://github.com/open-mpi/ompi/issues/11709) — Vprotocol not working in v5.0.0? | User trying the vprotocol pessimist sender-based message logger found the generated senderbased log files were all zeros, and asked whether vprotocol is broken. | n/a | ❓ Needs info | No maintainer ever responded and there is no reproduction or confirmation; vprotocol/pessimist is a niche, lightly-maintained component that still exists in the tree. Recommend pinging the reporter for more detail and closing if no response. |
| [#12310](https://github.com/open-mpi/ompi/issues/12310) — Memory leak with OpenMPI and Vasp? | User sees growing memory usage to OOM running VASP molecular dynamics with Open MPI 5.0.x, but not with Intel MPI; no reproducer provided (VASP is proprietary). | n/a | ❓ Needs info | Maintainer attributes it to lack of flow control on MPI_Bcast/Send progress (mitigated by coll/sync) rather than a confirmed leak, and notes no reproducer for proprietary VASP; needs updated info with current versions, close if none. |
| [#12323](https://github.com/open-mpi/ompi/issues/12323) — Help needed with understanding ompi_display_comm output | User asks why many inter-node connections in ompi_display_comm show a generic 'ucx' transport rather than the expected 'ucx=dc_mlx5;mlx5_0' for all off-host pairs. | n/a | ❓ Needs info | Usage/interpretation question about the ompi_display_comm report (generic 'ucx' likely means the per-connection transport wasn't resolved at display time); never answered, a second user echoed it. Ping for a maintainer explanation; not a confirmed defect, needs info. |
| [#12489](https://github.com/open-mpi/ompi/issues/12489) — Open MPI fails with 480 processes on a single node | User running OpenFOAM/LAMMPS with hundreds of processes on a single Power 10 node hits 'out of file handles' / PMIX_ERR_SYS_LIMITS_PIPES errors, suspecting n^2 connection scaling. | n/a | ❓ Needs info | Members explained this is an OS file-descriptor/pipe limit (iof pipes per process), suggested raising ulimit and forcing shared-memory BTL; reporter then hit a separate PRTE pipe limit and lost access to the machine. Marked Stale, abandoned with no resolution; this is a usage/limits question, ping reporter and close if no response. |
| [#12915](https://github.com/open-mpi/ompi/issues/12915) — Install fail on external PMIx, I get "error: 'PMIX_SESSION_PROVISION' undeclared" | Building OMPI git main against an external PMIx on a Raspberry Pi/Slurm cluster fails because the bundled PRRTE references PMIX_SESSION_PROVISION, which the user's older external PMIx install lacks (plus a Permission denied .deps write error). | n/a | ❓ Needs info | Maintainer diagnosed an environment problem (the external PMIx being picked up is not actually current master, and there is a clear filesystem permission error), not an Open MPI defect; this is user-environment-specific, unresolved on the reporter's side with no further response, so it should be pinged/closed for lack of info. |
| [#12918](https://github.com/open-mpi/ompi/issues/12918) — openMPI dropped inbound connection | A two-node TCP BTL job drops inbound connections because the peer advertises only IPv6 addresses but tries to connect over an IPv4 (RoCEv2) address that is not in the peer's known-address list. | n/a | ❓ Needs info | A maintainer explained the message reflects a node-side interface/routing misconfiguration (peer published IPv6 but initiated over IPv4) and suggested disabling IPv6 and constraining interfaces; no Open MPI defect was identified and the reporter did not follow up, so this is a config/support question to ping then close. |
| [#12931](https://github.com/open-mpi/ompi/issues/12931) — I am getting a 'Profiling Timer Alarm' signal after upgrading openmpi versions | After upgrading from 4.1.6 to 5.0.5 (heavily reduced static/no-dlopen build), even 'mpirun --version' dies with signal 27 (SIGPROF, 'Profiling timer expired'/'Profiling time alarm'). | n/a | ❓ Needs info | No maintainer responses and no reproducer; SIGPROF on a basic mpirun strongly suggests an environment/toolchain or unusual static-build interaction rather than a confirmed Open MPI defect, so this needs more info from the reporter and should be pinged then closed if no response. |
| [#13183](https://github.com/open-mpi/ompi/issues/13183) — RDMA Read Error on DGX B200 systems | User hits a UCX/mlx5 remote-access error (synd 0x14) doing GPUDirect RDMA over IB on a DGX B200 with rail-mapped HCAs. | n/a | ❓ Needs info | An NVIDIA engineer diagnosed it as GPUDirectRDMA not being properly enabled (DMA-BUF inactive or nvidia-peermem not loaded) rather than an Open MPI defect, and tagged UCX folks; this is an environment/UCX config question awaiting the reporter's verification, so ping and close if no response. |
| [#13361](https://github.com/open-mpi/ompi/issues/13361) — mprotect failure on Fedora 41 during memory hook installation when clang asan is used | Building a user app with clang AddressSanitizer causes a SIGSEGV during Open MPI's UCX memory-patcher hook installation (mprotect routed to a garbage address). | unknown (likely all) | ❓ Needs info | Labeled question/Target v5.0.x with no maintainer follow-up; this is an ASan-vs-memory-patcher interaction (the patcher overwrites libc symbols, which conflicts with ASan interceptors) and the workaround is to disable the memory patcher; needs maintainer input or a confirmation it is out of scope under ASan. |

## Obsolete / moot (7)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#5885](https://github.com/open-mpi/ompi/issues/5885) — PML fallback not selected if explicit MCA components specified | User asked why specifying multiple PMLs (cm,yalla) plus an MTL (psm) does not fall back to yalla when psm fails, instead erroring with 'No components were able to be opened in the pml framework'. | EOL versions only (yalla/psm MTL removed) | 🗑️ Close | The specific components in the example (pml/yalla, mtl/psm, mtl/mxm) have all been removed from the tree, so this exact scenario no longer applies; the general selection-fallback behavior question received no developer response and is stale since 2018. Recommend closing as obsolete. |
| [#6896](https://github.com/open-mpi/ompi/issues/6896) — .dylib warning from dlopen() for libcuda on Linux | On Linux, Open MPI's CUDA dlopen attempts include libcuda.dylib paths, producing a confusing (cosmetic) 'cannot open ... .dylib' line in the CUDA-unavailable warning message. | n/a | 🗑️ Close | The old opal/mca/common/cuda/common_cuda.c that emitted these .dylib attempts no longer exists; CUDA was moved to the accelerator framework and the accelerator/cuda code has no .dylib references. The specific cosmetic warning is obsolete on supported branches; close. |
| [#7683](https://github.com/open-mpi/ompi/issues/7683) — Getting Out of resource error | PMIX OUT-OF-RESOURCE error during MPI_Init_thread when launching Horovod via Singularity under SLURM with Open MPI 3.0.0. | n/a | 🗑️ Close | Reported against ancient/EOL Open MPI 3.0.0 with the old PMIx2 client; no comments, no reproducer, and the PMIx layer has been completely replaced since. Environment-specific launch/PMIx setup issue that is moot on supported versions; recommend closing. |
| [#9129](https://github.com/open-mpi/ompi/issues/9129) — clang: error: cannot specify -o when generating multiple output files | Android NDK cross-build of Open MPI 4.1.1 fails in embedded PMIx3 because a user-supplied duplicate -include in CPPFLAGS gets mangled, leaving a stray source file on the compile line. | n/a | 🗑️ Close | Reporter self-resolved by removing their duplicate -include; the only open thread was an optional config enhancement to opal_functions.m4. The failure was in embedded pmix3x, which no longer exists on main (now an openpmix submodule), so the scenario is moot; recommend closing. |
| [#9205](https://github.com/open-mpi/ompi/issues/9205) — Dmtcp Launch with Mpi example throws segmentation fault 11 | User on Open MPI 1.10.7 gets a segfault when checkpointing an MPI hello-world via DMTCP; crash is inside calloc, suggesting memory corruption. | n/a | 🗑️ Close | Open MPI 1.10.x is ancient/unsupported and its built-in checkpoint support was long removed; maintainers advised retesting on a modern release since DMTCP needs no special MPI support. Reporter moved to the DMTCP project and never followed up; recommend closing. |
| [#9589](https://github.com/open-mpi/ompi/issues/9589) — PMIX ERROR: NOT-FOUND in file server/pmix_server.c at line 237 | User running NVIDIA HPC-X 2.9.0 Open MPI 4.1.2a1 with Slurm gets a PMIx psec/munge component-not-found error during pmix_init. A commenter found that building with external PMIx (--with-pmix=/usr) resolves it. | n/a | 🗑️ Close | This is a packaging/config question about the embedded PMIx in HPC-X 2.9, not an Open MPI defect; a workaround (external PMIx) was given and the embedded PMIx is long superseded. Recommend closing as a moot support question. |
| [#10651](https://github.com/open-mpi/ompi/issues/10651) — MPI_Comm_spawn() giving unreachable errors across nodes | MPI_Comm_spawn on Open MPI 4.0.4 fails with 'unable to reach each other' / Unreachable BTL errors when spawned workers land on remote nodes, while plain mpirun across nodes works. | v4.0.x (EOL) | 🗑️ Close | Reported against EOL v4.0.4 (ORTE runtime) with no maintainer response and no reproduction on the PRRTE-based v5.x/main runtime that replaced ORTE, so the specific code path is obsolete. Close as moot on an unsupported version. |

## Duplicate / tracked elsewhere (2)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#12381](https://github.com/open-mpi/ompi/issues/12381) — OpenMPI 4.1.3 with pmix | User reports that launching with srun --mpi=pmix hangs on Open MPI 4.1.3 with Slurm, while pmi/pmi2 work fine. | n/a | 🗑️ Close | A commenter flagged this as a likely duplicate of #11471 (also open, Target: v4.1.x); it is a v4.1.x-only Slurm/PMIx interop support question on an EOL series with no reproducer or maintainer follow-up. Recommend closing as a duplicate. |
| [#12476](https://github.com/open-mpi/ompi/issues/12476) — Problems executing mpirun in parallel, it hangs and sends ORTE does not know how to route a message... | Same user/report as #12473: multi-node mpirun hangs with the ORTE message-routing error on Ubuntu 22.04 with Open MPI 4.1.2. | n/a | 🗑️ Close | This is a duplicate of #12473 (same reporter, same ORTE routing error, no comments). Recommend closing as a duplicate. |

## Wontfix / out of scope (7)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#1360](https://github.com/open-mpi/ompi/issues/1360) — shmem_align argument ordering | Reporter noted shmem_align(align, size) argument order differs from Cray SMA's shmem_align(size, alignment), causing portability pain. | n/a | 🗑️ Close | Open MPI follows the OpenSHMEM spec (align, size) per Section 8.2.1, confirmed still in oshmem/shmem/c/shmem_align.c; Cray is the divergent implementation, so this is wontfix/out of scope for Open MPI. |
| [#6878](https://github.com/open-mpi/ompi/issues/6878) — Linking separate executables with MPI_open_port sharing port information in a text file | User asks whether two independently-launched mpiexec jobs can rendezvous via MPI_Open_port by passing the port_name through a shared file; this works with MPICH but hangs at Comm_connect/accept with Open MPI. | n/a | 🗑️ Close | This is a usage question; Open MPI requires a rendezvous server (ompi-server / PRRTE DVM) to connect separately-launched jobs and does not support bare file-passed port handshakes the way MPICH does (see related #6916). No reply since 2019; the answer is the documented ompi-server/prte workflow. Close as out of scope/answered. |
| [#8463](https://github.com/open-mpi/ompi/issues/8463) — OpenMPI and mingw (msys2) | User tries to build Open MPI 4.1.0 under mingw/msys2 and hits configure failures (no statfs/statvfs, libevent var-scope clash); essentially asks for native Windows support. | n/a | 🗑️ Close | Maintainer answered that Open MPI requires statfs()/statvfs() and is not tested/supported on native Windows, recommending WSL; native Windows/mingw support is out of scope. Recommend closing as answered/out-of-scope. |
| [#9363](https://github.com/open-mpi/ompi/issues/9363) — Calling mpirun from std::system() and MPI_Init(). | User wants to launch mpirun via std::system() from inside a process that has already called MPI_Init(); this fails because Open MPI's inherited environment confuses the recursive launch. | n/a | 🗑️ Close | Maintainer explained that invoking mpirun from within an MPI-initialized process is not supported (environment passed between mpirun and children breaks recursively) and pointed to MPI_Comm_spawn as the supported path. This is answered/out-of-scope behavior, not a fixable bug; recommend closing. |
| [#10356](https://github.com/open-mpi/ompi/issues/10356) — Latest conda-forge build of 4.1.3 for Linux makes cudatoolkit an indirect dependency | conda-forge 4.1.3 Linux package pulls ucx (and thus cudatoolkit) as a hard dependency, bloating package size. | n/a | 🗑️ Close | This is a conda-forge packaging decision outside the OMPI project's control, as the maintainer noted directing the user to the conda-forge package maintainer; out of scope, close. |
| [#12470](https://github.com/open-mpi/ompi/issues/12470) — Internal hwloc shipped with OpenMPI 4.1.7 no longer compatible with SLURM 23.11 cgroup plugin / system hwloc | The hwloc 2.0 bundled with Open MPI 4.1.x mishandles cgroup v2 affinity under Slurm 23.11, causing hwloc_set_cpubind binding failures; using system hwloc or v5.0.x avoids it. | n/a | 🗑️ Close | A maintainer stated the v4.1.x bundled hwloc will not be updated and recommended the documented workaround (system hwloc) or upgrading to v5.0.x, which uses system hwloc by default. Reporter kept it open mainly for searchability; recommend closing as wontfix for the EOL series. |
| [#13385](https://github.com/open-mpi/ompi/issues/13385) — Open-Coarrays and gfortran-15 with OpenMPI | OpenCoarrays disables Open MPI support because it assumes MPI window handles are identical integer IDs across all images, whereas Open MPI uses per-process pointer handles. | n/a | 🗑️ Close | Maintainers concluded OpenCoarrays relies on a non-MPI-guaranteed assumption (globally consistent window IDs) and the correct fix is for OpenCoarrays to compute its own unique IDs; a maintainer filed upstream OpenCoarrays PRs, so this is out of scope for Open MPI and can be closed. |
