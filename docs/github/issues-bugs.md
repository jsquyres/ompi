# Open bug issues older than 6 months

Triage of open `open-mpi/ompi` issues opened more than 6 months ago (created before 2025-12-20), as of 2026-06-20. Classification and status are a **reasoned determination** from each issue's text, discussion, labels, and linked/merged PRs, plus spot-checks of current `main` — not a per-branch build-and-reproduce test. Treat "Close" as *recommended for maintainer confirmation*, not automatic.

Issues are grouped by **why they are still open**. See [issues-overview.md](issues-overview.md) for totals and cross-tabs, and the companion files: [bugs](issues-bugs.md) · [feature requests](issues-feature-requests.md) · [questions](issues-questions.md) · [other](issues-other.md).

**Relevance** = which release branches the defect likely still affects.


**399 issues** — 🗑️ Close 160 · ❓ Needs info 90 · ✅ Keep open 149.


## Unresolved, still valid (165)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#9432](https://github.com/open-mpi/ompi/issues/9432) — romio321 segfault in mpi4py non-blocking collective MPI-IO | mpi4py I/O tests segfault because non-blocking collective MPI-IO (e.g. MPI_File_iwrite_all) is not implemented in the Open MPI romio321 component, which lacks the MPICH Grequest extensions. | fixed on all | 🗑️ Close | The real-world trigger was a Debian packaging bug (dangling ompio symlinks) that forced fallback to romio321; the default is now the native ompio component which works, and the reporter confirmed tests pass once packaging was fixed. The underlying romio non-blocking-collective gap remains but is low priority since ompio is default; recommend closing given the packaging fix and default change. |
| [#76](https://github.com/open-mpi/ompi/issues/76) — In obscure situations, sizeof(MPI_Fint) != sizeof(int) is a problem for us | When sizeof(MPI_Fint) differs from sizeof(int) (e.g. ifort -i8), the f_to_c index passed to Fortran attribute copy/delete keyval handlers is a C int and can be misinterpreted, causing problems. | v5.0.x, v6.0.x, main | ❓ Needs info | Attribute callbacks in ompi/attribute/attribute.h still use MPI_Fint/c_f_to_c_index for the f_to_c index, so the niche mismatch likely still exists on current branches; very obscure and untouched since 2014, recommend pinging/needs info before deciding. |
| [#2490](https://github.com/open-mpi/ompi/issues/2490) — Zero-length shmem_broadcast raises floating point exception | shmem_broadcast32/64 with nelems==0 triggers a floating point (divide-by-zero) exception, traced to the tuned collectives; zero-length calls should be valid per OpenSHMEM 1.2+. | unknown | ❓ Needs info | No fix PR was ever linked and the tuned bcast still performs segment-count divisions, so the zero-length edge case is likely still latent; given it is very stale and unverified on current code, recommend Needs info (request a quick retest) and close if not reproducible. |
| [#3394](https://github.com/open-mpi/ompi/issues/3394) — Nightly snapshot tarball filename problematic for RPMs | Nightly snapshot version strings like v2.0.x-201704190318-24b5b83 use dashes, which RPM rejects as illegal in the Version field; suggested using dots. | unknown | ❓ Needs info | No PR is linked; this is a packaging/build-string nit that may or may not have been addressed in current autogen/configure version-string generation, so it is unverified. Labeled a question, but it is effectively a packaging defect; recommend Needs info / verify against current snapshot naming, lean Close if already dotted. |
| [#4963](https://github.com/open-mpi/ompi/issues/4963) — TCP/BTL multi-NIC still hangs | On hosts with multiple subnets/asymmetric NIC names, the TCP BTL tries an unreachable interface and the job hangs/aborts instead of failing over to a working interface; a working btl_tcp_if_include workaround exists. | v4.1.x, v5.0.x, v6.0.x, main (likely, unverified) | ❓ Needs info | bwbarrett reopened to drive to ground and bosilca noted OMPI should not abort if other connections are possible; the TCP BTL and reachable component still exist on main, so the no-failover behavior likely persists, but it was never confirmed fixed. Recommend pinging to revalidate on a current release; close if not reproducible. |
| [#5902](https://github.com/open-mpi/ompi/issues/5902) — TCP BTL progress thread segv's | Frequent segfaults during MPI_Finalize when using TCP BTL with btl_tcp_progress_thread=1, traced to a race where opal_progress runs (via the PMIx fence wait or the progress thread) after a peer has closed its TCP socket / the component is being torn down. | main (TCP BTL still present; status unverified) | ❓ Needs info | ggouaillardet root-caused a finalize-time race and proposed candidate fixes, but no merged fix is referenced and the issue (refs #5849) was left assigned; the TCP BTL and the progress-thread option still exist on main, so the race may persist. Recommend pinging to revalidate on a current release before closing. |
| [#7394](https://github.com/open-mpi/ompi/issues/7394) — shmem related files left in /dev/shm after interrupt | Interrupting (CTRL-C) a job using the posix shmem component leaves open_mpi.* segment files in /dev/shm; after 128 accumulate, opal_shmem_base_select fails and jobs cannot start. | v4.1.x, v5.0.x, main | ❓ Needs info | This is a genuine cleanup-on-interrupt defect that was never triaged; needs re-verification against current main (posix shmem still present) since the runtime moved to PRRTE in v5, so request a fresh reproducer and keep/close based on response. |
| [#8102](https://github.com/open-mpi/ompi/issues/8102) — IMB-RMA failure on POWER | IMB-RMA segfaults at startup in ompi_osc_rdma_lock_atomic (NULL peer/endpoint) on POWER9 with the UCX PML and default RDMA OSC component; reproduced by IBM, worked around with '-mca osc ucx'. | EOL versions only | ❓ Needs info | Confirmed by multiple developers as a UCX-PML / osc-rdma interaction on ppc64le in v4.0.x/v4.1.x, but never root-caused and stalled in 2021 on EOL versions with no POWER hardware available. The reporter has migrated to mvapich; recommend asking whether it reproduces on a supported v5.0.x/main build and closing if unverifiable. |
| [#8252](https://github.com/open-mpi/ompi/issues/8252) — OSU one-sided ompi-4.0.5/ucx-1.9.0 ucp_worker error | OSU one-sided (RMA) tests fail on POWER9/aarch64 IB systems with 'failed to set active message handler id 1: Invalid parameter' and remote-access errors, across UCX 1.9/1.10 and Open MPI 4.0.x/5.0.x using osc/ucx or osc/rdma. | EOL versions only | ❓ Needs info | Multiple developers reproduced UCX RMA failures on POWER9 and aarch64 ConnectX but the root cause was never pinned down (largely a UCX issue) and the thread stalled on EOL versions; the original reporter was repeatedly pinged without confirming a fix. Recommend asking whether current UCX + supported Open MPI still fails on available IB hardware and closing if unverifiable. |
| [#8527](https://github.com/open-mpi/ompi/issues/8527) — Add check TSD key != NULL to support cases where TSD values are accessed on different threads (ULT)? | Segfault during MPI_Finalize when the qthreads (ULT) TSD backend deletes a NULL TSD key, likely because Finalize runs on a different thread than the progress thread. | v5.0.x, v6.0.x, main (qthreads only) | ❓ Needs info | opal_tsd_tracked_key_destructor still calls opal_tsd_key_delete(key->key) unconditionally and the qthreads opal_tsd_key_delete passes the key straight to qthread_key_delete with no NULL guard, so the reported crash path appears unfixed; however it only affects the niche/experimental qthreads ULT component and the discussion never reached a fix decision, so ping reporter to confirm relevance before closing. |
| [#9080](https://github.com/open-mpi/ompi/issues/9080) — Mixing UCX pml and osc performance degradation | Combining osc/ucx (RMA) with pml/ucx (P2P) in PaRSEC causes severe performance degradation at 64+ nodes, with ucp_put_nbi calls intermittently taking milliseconds; reproducible with MPICH+UCX too. | v4.1.x, v5.0.x, main (UCX-related) | ❓ Needs info | Long UCX-side investigation never reached a root cause; reporter showed the same pattern on MPICH+UCX, suggesting a UCX issue, and hjelmn noted osc/ucx has known issues. No resolution and the reporter went on extended hiatus; recommend pinging for current-version status (osc/rdma vs osc/ucx) and closing if stale. |
| [#10048](https://github.com/open-mpi/ompi/issues/10048) — Open MPI memory leaks on Ubuntu | A trivial MPI_Init/MPI_Finalize program leaks ~9KB (op_select, framework register, orte_finalize) per valgrind/leak-sanitizer on Ubuntu. | v4.1.x; needs retest on v5.0.x/main | ❓ Needs info | Some leaks were addressed in PR #10105 but not all, and the report was against the v4.1.x/ORTE codebase which was replaced by PRRTE in v5.0; current leak status is unverified, so ping for a v5.0.x/main re-run before deciding to close. |
| [#10117](https://github.com/open-mpi/ompi/issues/10117) — Intermittent crashes inside MPI_Finalize | Rare (~1/1000) crashes/hangs in MPI_Finalize inside PMIx (pmix_notify_check_range) during teardown, seen across several Open MPI 4.x versions in CI. | v4.0.x, v4.1.x; likely fixed/moot on v5.0.x+main | ❓ Needs info | rhc54 said the underlying PMIx problems were addressed on main/v5 but not back-ported to v4; a later PR #13073 may also help, but no reporter confirmation. Still valid on the now-EOL v4.x branches; ping reporters to retest on a current release and close if no response. |
| [#10220](https://github.com/open-mpi/ompi/issues/10220) — Multiple failures running collective-big-count tests with OMPI v4.1.x branch | collective-big-count self-checking tests fail with wrong data, read errors, SIGSEGV, and double-free on the v4.1.x branch across basic/adapt/han/tuned coll components. | v4.1.x (EOL); related failures on main tracked elsewhere | ❓ Needs info | jsquyres asked whether main fixes could be back-ported; reporter said only some overlap with main and gave partial answers. v4.1.x is now EOL, so back-porting is unlikely; the main-branch counterparts are tracked in #10186/#10214. Ping/close given EOL status. |
| [#10244](https://github.com/open-mpi/ompi/issues/10244) — MTT ibm one-sided test failures for ompi v5.0.x | An MTT tracking issue cataloguing ~40 IBM one-sided test failures (segfaults/hangs) on v5.0.x over TCP, OFI, and EFA paths in April 2022. | v5.0.x, main (status unverified; MTT-era) | ❓ Needs info | Several listed sub-failures were fixed by merged PRs (10462, 10463, 10473) but remaining btl/tcp hangs/segfaults and a btl/ofi mt_1sided segfault were never confirmed resolved; since v5.0.x has long since released and these are old MTT data, ping to re-run current MTT before keeping open. |
| [#10377](https://github.com/open-mpi/ompi/issues/10377) — Fortran configure warnings and build errors with nvidia fortran compiler | Building 4.1.3/4.1.4 with the NVIDIA HPC SDK (nvc/nvfortran/nvcc) yields wrong Fortran type-size warnings and embedded-libevent build errors ('No way to define ev_uint64_t', missing -fPIC, nvfortran libtool detection). | v4.1.x (EOL); main/v6.0.x unverified (libevent now external) | ❓ Needs info | Reported on EOL 4.1.x; main no longer embeds the libevent2022 component (it uses external/3rd-party libevent and updated libtool), so the build errors likely no longer apply as-is, but NVHPC Fortran type-size warnings may persist; needs a re-test on current main before keeping open. |
| [#10410](https://github.com/open-mpi/ompi/issues/10410) — v4.1: UCX onesided crash (ibm/onesided/1sided) | MTT found an osc/ucx crash in MPI_Win_fence/MPI_Accumulate (UCX 'remote key on a different endpoint' / segfault) on v4.1.x with ppc64le and UCX 1.11.2. | v4.1.x (EOL-ish); possibly v5.0.x | ❓ Needs info | No comments and never confirmed/fixed; possibly a UCX 1.11.2-specific bug. v4.1.x is in late maintenance and the old UCX is suspect, so recommend pinging the reporter to retest with current UCX/Open MPI before keeping open; close if unreproducible. |
| [#10436](https://github.com/open-mpi/ompi/issues/10436) — UCX issues with ibv_fork_init() and Open MPI 4.1.x | btl/ofi calls ibv_open_device() before UCX calls ibv_fork_init(), triggering UCX fork warnings and intermittent corruption/hangs; user suggests ordering or a common fork-safe-init MCA parameter. | v4.1.x; potentially v5.0.x/main | ❓ Needs info | Reported on v4.1.x; the verbs/fork init ordering concern could still apply on later branches but was never confirmed fixed and no PR landed. Recommend pinging to confirm whether current Open MPI (5.0+) still loads/initializes btl/ofi before UCX; keep open only if still reproducible. |
| [#10454](https://github.com/open-mpi/ompi/issues/10454) — pkg-config and PMIx potential conflict | Open MPI installs a pmix.pc that pkg-config discovers, but with internal/embedded PMIx the referenced include/lib directories contain no PMIx headers or libraries, breaking downstream pkg-config builds. | v4.1.x; check v5.0.x/main | ❓ Needs info | Reported on 4.1.4 with internal PMIx; no comments or resolution. PMIx packaging changed substantially in 5.0 (PMIx is now a separate 3rd-party submodule), so this may be moot on main. Recommend pinging to confirm whether a stray pmix.pc is still installed on current releases; close if not. |
| [#10484](https://github.com/open-mpi/ompi/issues/10484) — Missing libucm with static build | With --disable-shared --enable-static, linking apps fails with an undefined reference to ucm_test_external_events because -lucm is missing from the wrapper link line. | main, v5.0.x (static builds with UCX) | ❓ Needs info | A static-link flags bug in the common/ucx wrapper library line; no fix referenced and the static UCX linking path is unchanged by anything in the discussion, so it likely still affects current static builds with UCX. Recommend pinging to confirm reproduction on current main; keep open if confirmed. |
| [#11047](https://github.com/open-mpi/ompi/issues/11047) — Cuda Managed Memory fails with cuMemcpyAsync | Running osu_bw with CUDA managed memory (MD MD) under pml ob1 on main fails in cuMemcpyAsync, though plain memcpy works; suggested fix is to check CONVERTOR_ACCELERATOR_UNIFIED and use memcpy for managed buffers. | v5.0.x, v6.0.x, main | ❓ Needs info | A separate commenter's report turned out to be a user code bug; the original maintainer-reported managed-memory cuMemcpyAsync defect was never confirmed fixed and the accelerator/convertor path still exists. Stale since 2023; recommend ping reporter to confirm against current main, close if not reproducible. |
| [#11646](https://github.com/open-mpi/ompi/issues/11646) — ARM invalid address alignment on IBM benchmarks | On aarch64, the IBM onesided c_reqops test SIGBUSes with 'Invalid address alignment' inside vader's software-emulated atomics (mca_btl_vader_sc_emu_atomic_32) reached via the barrier/osc path. | EOL versions only (v4.0.x/v4.1.x); verify v5.0.x | ❓ Needs info | Reproduced on v4.0.x/v4.1.x (now EOL); a candidate alignment fix (dc8ead9) was never backported. vader was renamed to btl/sm in v5; unclear if the sc-emu atomic alignment bug persists there. Recommend pinging to retest on v5.0.x/main and closing if not reproducible. |
| [#21](https://github.com/open-mpi/ompi/issues/21) — Make mpi_preconnect_all work for multiple interfaces | The BTL-agnostic preconnect code (mpi_preconnect_all/preconnect_mpi MCA param) only pre-connects one BTL per proc when multiple endpoints/BTLs are active, instead of all of them. | unknown | ✅ Keep open | The preconnect_mpi/preconnect_all MCA param still exists (ompi/runtime/ompi_mpi_preconnect.c) using the same generic send/recv approach, so the multi-interface limitation likely persists on main; a niche enhancement with no activity since 2007, so realistically a stale candidate to revisit, but technically still valid. |
| [#50](https://github.com/open-mpi/ompi/issues/50) — Have ompi_proc_t destructor call pml.del_procs | The ompi_proc_t destructor should call the PML's del_procs to release per-peer PML/BTL resources, but does not, due to thread-safety and BTL no-op concerns. | v5.0.x, v6.0.x, main | ✅ Keep open | ompi_proc_destruct in ompi/proc/proc.c still only releases the convertor and removes the proc from lists/hash; it does not call pml.del_procs, so the described incomplete cleanup persists on current main. |
| [#58](https://github.com/open-mpi/ompi/issues/58) — Enable global ID lookup in debugger msgq when using sparse groups | The message-queue debugger DLL (ompi_msgq_dll.c) skips the global rank lookup for sparse (non-dense) groups, setting local_to_global to -1 instead of resolving the COMM_WORLD rank. | v5.0.x, v6.0.x, main | ✅ Keep open | ompi/debuggers/ompi_msgq_dll.c still sets group->local_to_global[i] = -1 for non-dense groups (dense-only lookup), so the limitation persists on main; a 2010 comment called it a significant effort with no known sparse-group users, so it is valid but low priority. |
| [#71](https://github.com/open-mpi/ompi/issues/71) — Stamp out valgrind warnings in OMPI | Open-ended cleanup task to build OMPI --with-valgrind and eliminate valgrind warnings, primarily in the base/framework code. | all (open-ended cleanup) | ✅ Keep open | Valgrind/memchecker support still exists (opal/mca/memchecker, including a valgrind component), but eliminating all valgrind warnings is a perpetual, never-fully-complete cleanup goal with no activity since the 2008 import; arguably a candidate to close as too open-ended, but treated here as a still-valid maintenance item. |
| [#123](https://github.com/open-mpi/ompi/issues/123) — Fix MPI_File auto-close on Finalize | Leaving an MPI_File open at MPI_Finalize triggers ROMIO's internal MPI_Barrier during cleanup, hitting Open MPI's 'no MPI calls after finalize' guard and aborting. | v5.0.x, v6.0.x, main | ✅ Keep open | ompi_file_finalize still OBJ_RELEASEs open files whose destructor calls io_module_file_close (ROMIO close, which barriers); the mechanism that caused the abort is still present on current branches, so it likely remains valid, recommend keep open pending a re-test. |
| [#124](https://github.com/open-mpi/ompi/issues/124) — mpi_show_handle_leaks=1 doesn't show all leaks | With mpi_show_handle_leaks=1, Open MPI only reports leaked communicators, infos, files (and windows), but not datatypes, errhandlers, groups, keyvals, ops, or requests. | v5.0.x, v6.0.x, main | ✅ Keep open | Grep of current main confirms show_handle_leaks is referenced only in file/win/comm/info and not in datatype/group/op/errhandler/attribute code, so the gap still exists on all maintained branches; minor debug feature, recommend keep open (low priority). |
| [#160](https://github.com/open-mpi/ompi/issues/160) — TCP BTL does not support Linux virtual interfaces | The TCP BTL keys interfaces by kernel index, which Linux reuses for virtual interfaces (e.g. eth0:0), confusing the BTL and causing hangs; macOS IPv6 kindex inconsistency is a related symptom. | v5.0.x, v6.0.x, main | ✅ Keep open | Current TCP BTL and opal/util/if.c still rely on if_kernel_index from if_nametoindex, so the underlying assumption persists; the proposed fix of building own kindexes from ifname was never implemented. Re-opened in 2018 and still genuine, keep open. |
| [#321](https://github.com/open-mpi/ompi/issues/321) — Fix the attribute mess | Request to replace the band-aid global lock around MPI attribute access with a proper object-specific flag scheme, since attributes are stored in an opal_hash_table that is awkward to make correctly thread-safe. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | No PR has reorganized the attribute/hash-table locking scheme and the underlying design is unchanged on main, so the concern remains valid though low priority; this is a long-standing refactor that should either be done or formally declined. Recommend keeping open (or wontfix). |
| [#442](https://github.com/open-mpi/ompi/issues/442) — Fix MCA var alias problem | Registering an MCA var alias in a different component than the real variable creates a race: the value may be read before the aliasing component registers, so a user-set alias value is silently ignored. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The MCA var synonym/alias machinery still exists on main (mca_base_var.c, mca_base_alias.h) and no fix forcing same-location registration or value-conflict detection appears to have landed, so the underlying race is likely still present. Recommend keeping open. |
| [#628](https://github.com/open-mpi/ompi/issues/628) — opal_path_nfs test is undeterministic and may fail in chroot environments (such as mock) | The opal_path_nfs unit test reads /proc mounts and asserts NFS detection, which fails inside chroot/mock builds where the inherited /proc mounts are not actually accessible as NFS, breaking deterministic RPM builds. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The test still relies on parsing live mount info and has no mock/chroot detection on main (only a fuse-skip exists), so the non-deterministic chroot failure can still occur. Recommend keeping open (small fix: detect mock and exit 77). |
| [#1666](https://github.com/open-mpi/ompi/issues/1666) — MPI_Accumulate Error when using datatype "MPI_SHORT_INT" | MPI_Accumulate with MPI_MINLOC and the padded MPI_SHORT_INT type produces wrong results in the one-sided code because the 8-byte storage vs 6-byte payload (true_extent vs extent) is mishandled. | v5.0.x, v6.0.x, main | ✅ Keep open | Was fixed only in the now-removed pt2pt OSC; the rdma OSC was the remaining gap. osc_rdma_accumulate.c on main does use true_lb/true_extent so it may be partly addressed, but the SHORT_INT MINLOC case was never confirmed fixed and the Forum-clarification question stayed open; keep open pending verification. |
| [#1763](https://github.com/open-mpi/ompi/issues/1763) — Tuned bcast data corruption | Tuned/base collective algorithms (pipelined, split-binary-tree bcast) corrupt data when ranks supply different counts/datatypes with the same type signature, because segmentation cannot split at matching boundaries. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Maintainers stated (2020) the root cause cannot be fixed efficiently in current infrastructure; the PoC PR #1789 was abandoned and only a suboptimal workaround (disabling tuned decisions for mismatched counts in libnbc) was implemented. The defective pipelined algorithms remain on all branches, so keep open. |
| [#2025](https://github.com/open-mpi/ompi/issues/2025) — can opal_progress() call opal_progress()? (test hanging in MPI_COMM_DUP) | Multiple outstanding MPI_Comm_idup (or idup interleaved with other nonblocking collectives) on the same communicator can deadlock or produce mismatched CIDs, because idup is implemented as multiple internal allreduces whose global ordering is not enforced. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Last reporter (2019) confirmed Gilles' proof-of-concept patches did not fully fix the case where idup interleaves with other collectives; main has since adopted an extended-CID/unique-tag scheme that may mitigate but is not confirmed to resolve this, so keep open and reverify. |
| [#2335](https://github.com/open-mpi/ompi/issues/2335) — MPI_Reduce op x datatype parameter check needs improvement | Open MPI does not reject invalid MPI_Op/datatype combinations (e.g., MPI_SUM with MPI_BYTE), silently producing wrong answers instead of flagging the erroneous program. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | reduce.c still uses ompi_op_is_valid after the MPI->OPAL datatype conversion, which the maintainer noted loses the distinction (MPI_BYTE becomes UINT8_T) so the stricter check described is still not implemented. The enhancement remains valid on all branches, so keep open. |
| [#2985](https://github.com/open-mpi/ompi/issues/2985) — wrong endianness for "external32" representation | MPI-IO external32 data representation does not honor big-endian conversion in Open MPI; only romio314 supported it correctly at the time. | v5.0.x, v6.0.x, main | ✅ Keep open | ROMIO has since been removed (only OMPIO remains), and OMPIO source still carries 'work to be done e.g. for external32' comments, so external32 conversion remains incomplete and the romio314 workaround is gone; recommend Keep open as a still-valid limitation. |
| [#3182](https://github.com/open-mpi/ompi/issues/3182) — MPI_ALLOC_MEM slowness with CP2K application | CP2K spends a large fraction of time in MPI_ALLOC_MEM/MPI_FREE_MEM due to eager registration/deregistration; proposed an MCA param to disable de/registration during alloc/free. | unknown | ✅ Keep open | The discussed MCA-param work was explicitly deferred to a future milestone in 2017 and no resolution is linked; the memory/registration model has changed substantially since (openib gone, UCX-based), so relevance to current branches is uncertain. Recommend Needs info / retest, lean Keep open. |
| [#3357](https://github.com/open-mpi/ompi/issues/3357) — --enable-script-wrapper-compilers and oshmem build failures | With --enable-script-wrapper-compilers, shmemcc fails with 'Could not determine requested language' when compiling an OpenSHMEM C program. | v5.0.x, v6.0.x, main | ✅ Keep open | Both --enable-script-wrapper-compilers and the ompi_wrapper_script.in that prints 'Could not determine requested language' still exist, and shmemcc wrapper-data is still generated, so the OSHMEM script-wrapper path likely still mis-detects the language; never linked to a fix, recommend Keep open (or Needs info to confirm reproducibility). |
| [#3612](https://github.com/open-mpi/ompi/issues/3612) — XL Fortran Compiler F08 Bindings broken for OpenSHMEM | OpenSHMEM Fortran bindings fail to link with IBM XL Fortran because oshmem only emits trailing-underscore symbol variants, not the no-underscore convention XL uses by default. | v5.0.x, v6.0.x, main | ✅ Keep open | OSHMEM is still in the tree and the reporter re-confirmed the failure as recently as 2021 (master); the oshmem Fortran binding symbol-mangling lacks the no-underscore variant. No fix landed (jjhursey found it too pervasive), so recommend Keep open (help wanted). |
| [#4019](https://github.com/open-mpi/ompi/issues/4019) — opal_bitmap test is always passed | The opal_bitmap unit test's main() ignores test_finalize()'s result and always returns 0, so the make-check test can never report a failure. | fixed on all (still present on main) | ✅ Keep open | Verified on current main: test/class/opal_bitmap.c line 107 still does return 0 after calling test_finalize() unconditionally, so the test still cannot fail. The defect is real and unfixed on all branches; keep open (trivial fix). |
| [#4507](https://github.com/open-mpi/ompi/issues/4507) — romio meaning of fp_ind for location within a file view | ROMIO's flattened datatype representation includes 0-byte chunks (e.g. for LB/UB markers) that the fp_ind file-pointer update/interpret logic mishandles, leading to incorrect write positioning with resized datatypes. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | This is a genuine ROMIO bug in the embedded romio component; no fix PR was ever produced and the romio code is still embedded on main. Likely affects all supported branches; keep open (though romio is bundled upstream code and may be hard to verify deeply). |
| [#6240](https://github.com/open-mpi/ompi/issues/6240) — TCP: unexpected process identifier in connect_ack | BTL/tcp connection setup picks wrong/non-routable interfaces (virbr0/docker0), causing 'received unexpected process identifier' errors, hangs, or dropped connections unless if_include/if_exclude is set manually. | v4.0.x/v4.1.x; partially addressed v5.0.x/main | ✅ Keep open | Labeled 'Target: main'; PR 7134 (interface pairing) was merged for 5.0 but commenters still needed manual exclude lists, and PRRTE startup was noted as not using the reachable component. The connect_ack error code is still present on main; likely still affects multi-interface setups, so keep open. |
| [#6464](https://github.com/open-mpi/ompi/issues/6464) — DDT shows message queues with pml ob1 but not with ucx and yalla | Parallel debuggers (DDT) cannot display MPI message queues when using pml/ucx (or the removed pml/yalla) because those PMLs bypass the mca_pml_base_send/recv_requests free lists used by the debugger DLL. | affected on all; v5.0.x/main lack MPIR debugger interface | ✅ Keep open | Labeled bug/good-first-issue and confirmed still open by a maintainer in 2022; pml/ucx still does not populate the base request lists. Note v5.0.x/main removed the MPIR debugger interface (PMIx tools interface now), so the fix landscape changed, but the underlying gap remains; keep open. |
| [#6728](https://github.com/open-mpi/ompi/issues/6728) — shmem.h uses C++ <complex> type for C++, but standard uses _Complex from <complex.h> | When compiled as C++, oshmem's shmem.h defines complex reduction arguments as std::complex<T> instead of the OpenSHMEM-standard C _Complex type, breaking spec-conformant C++ test programs. | main | ✅ Keep open | Verified shmem.h.in on main still defines OSHMEM_COMPLEX_TYPE as std::complex<type> for C++ (line 23), so the spec-noncompliance is still present; however maintainers noted OpenSHMEM C++ support is being deprecated and removed target labels, making this low priority. Still technically valid. |
| [#6777](https://github.com/open-mpi/ompi/issues/6777) — UCX seemingly causing Python MPI tests to fail with Open MPI v4.0.5 | RMA atomic operations (CompareAndSwap/FetchAndOp) on 1-, 2-, and 16-byte datatypes fail under osc/ucx because UCX only supports 4/8-byte atomics; osc/ucx should fall back to active messages but instead errors. | v5.0.x, v6.0.x, main | ✅ Keep open | yosefe/hjelmn confirmed osc/ucx does the wrong thing (should fall back to active messages for non-4/8-byte datatypes) and UCX still lacks 1/2-byte HW atomics. The hppritcha 'close' was premature; dalcinl showed it still failing on later versions. osc/ucx remains in v5.0.x and main, so likely still valid; keep open. |
| [#7143](https://github.com/open-mpi/ompi/issues/7143) — OSHMEM: SHPCLMOVE returns incorrect error code for 2 cases | The OSHMEM Fortran SHPCLMOVE routine returns -2 for the already-freed-block (expected -4) and not-at-block-start (expected -5) error cases, instead of the distinct status codes the API specifies. | v5.0.x, v6.0.x, main | ✅ Keep open | Verified still present on main: oshmem/shmem/fortran/shpclmove_f.c only ever sets status to -1/-2/0/1 and relies on shrealloc returning NULL, so it cannot distinguish the -4/-5 cases. Genuine, unresolved defect affecting all supported branches (v5.0.x/v6.0.x/main); keep open. |
| [#7393](https://github.com/open-mpi/ompi/issues/7393) — A system call failed during shared memory initialization ... | Shared-memory init fails with unlink ENOENT errors, caused on macOS by an overly long default TMPDIR path (and separately a session-dir cleanup race between concurrent jobs). | v5.0.x, main | ✅ Keep open | Workarounds exist (TMPDIR=/tmp; add_pid_to_session_dirname in v5) but the agreed permanent fix to detect/warn on too-long TMPDIR in Open MPI never landed and the old macOS FAQ is not migrated to current docs; affects 4.x and 5.x, so keep open. |
| [#7423](https://github.com/open-mpi/ompi/issues/7423) — Segv / freeing memory from dlclose()'ed common libraries | A component's `type` string can be free()'ed from a common library that has already been dlclose()'ed, causing a segfault during teardown. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | No comments and no linked fix; this is a latent shutdown-ordering bug in the MCA/dl machinery that is plausibly still present across all branches, so keep open. |
| [#7481](https://github.com/open-mpi/ompi/issues/7481) — Configure doesn't fail when enable-mca-no-build mca and component names are invalid | A typo in --enable-mca-no-build (e.g. btl-utc instead of btl-uct) is silently accepted; configure does not validate framework/component names. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Verified in config/opal_mca.m4 on main: the --enable-mca-no-build (and -dso/-static) parsing still only sets DISABLE_* vars with no existence/validity check, so the bug persists on all branches. Keep open. |
| [#7693](https://github.com/open-mpi/ompi/issues/7693) — btl/uct using wrong topology | mca_btl_uct context-count decision uses the node core count from hwloc as seen by the PRRTE daemon, not the cpuset actually available to the application, so it can over-provision contexts. | v5.0.x, v6.0.x, main | ✅ Keep open | Code in opal/mca/btl/uct/btl_uct_component.c still derives num_contexts_per_module from a raw core_count with no PMIX_JOB_CPUSET masking, so the defect persists on main; btl/uct is low priority (blocker label removed) but the bug is real and unresolved, so keep open. |
| [#7765](https://github.com/open-mpi/ompi/issues/7765) — Dynamic Tuning (coll/tuned) behavior doesn't have any non commutative checks | When a user forces a coll/tuned reduce-family algorithm via MCA parameters, there is no validation or fallback ensuring the chosen algorithm is valid for non-commutative operations. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The forced/dynamic decision files (coll_tuned_*_decision.c) on main contain no ompi_op_is_commute guard, unlike the fixed-decision path; so a user can still force a commutative-only algorithm with a non-commutative op without a safety check. Defect still present across release branches; keep open. |
| [#7795](https://github.com/open-mpi/ompi/issues/7795) — segfault with HDF5 chunked file | ROMIO (romio321) segfaults in ADIOI_Flatten during MPI_File_set_view with HDF5 chunked datasets on Lustre/GPFS; OMPIO works as a workaround. | v5.0.x, v6.0.x, main | ✅ Keep open | OMPIO avoids the crash and is now the default, but the bug is in the embedded ROMIO (romio321), and the planned ROMIO update was deferred for 4.0.x/4.1.x and milestoned to 5.0; the romio component still ships, so the underlying defect likely persists unless ROMIO was refreshed. Keep open as a still-valid ROMIO bug (mitigated by OMPIO). |
| [#7958](https://github.com/open-mpi/ompi/issues/7958) — Undefined behaviour reported by clang UBSan in pml_ob1_hdr.h | Clang UBSan flags a memcpy with a NULL pointer argument in the ob1 PML when a BTL has no memory-registration handle (zero-length copy), e.g. MPI_Bcast of large messages over shared memory. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The unguarded memcpy(hdr + 1, local_handle, local_handle_size) with a potentially-NULL local_handle and 0 size is still present on main (pml_ob1_hdr.h ~lines 266/421); rdma_btls set btl_reg=NULL. Labeled Target: main/v5.0.x and never fixed, so still valid across all branches. |
| [#8010](https://github.com/open-mpi/ompi/issues/8010) — reduce_scatter and reduce_scatter_block algorithms incorrectly handling noncommutative ops | Several tuned reduce_scatter and reduce_scatter_block algorithms (recursive halving, ring, butterfly) produce wrong results for noncommutative operators, and the algorithm commute-capability labels do not match observed behavior. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Maintainer explicitly left it open pending investigation to relabel algorithms as commute-only or fix them; no fix PR was referenced. A genuine correctness bug in the coll base/tuned algorithms that persists, so keep open across current branches. |
| [#8264](https://github.com/open-mpi/ompi/issues/8264) — All TYPE(MPI_Foo) types need to be in the "mpi" module | MPI 4.0 requires the Fortran 'mpi' module to define all named handle types (MPI_Comm, MPI_Datatype, etc.) used in mpi_f08 plus the .EQ./.NE./==//= operators, which Open MPI does not fully provide. | v5.0.x, v6.0.x, main | ✅ Keep open | Labeled bug/fortran-bindings, targeted at main and v5.0.x and last updated 2024; this is a Fortran standards-conformance gap with no evidence of a merged fix, so it likely still affects v5.0.x/v6.0.x/main. Recommend keeping open. |
| [#8561](https://github.com/open-mpi/ompi/issues/8561) — Memory leak with multiple equivalent tag pt2pt communications | High peak memory growth on the receiver when many unexpected messages are queued (notably under UCX), seen as a memory leak though memory is released at MPI_Finalize. | v5.0.x, v6.0.x, main | ✅ Keep open | Consensus is that this is expected unexpected-message buffering rather than a true leak, but bosilca noted UCX may amplify it and the reporter (re-bumped in 2023) asks for periodic reclamation of internal structures; nothing concrete was fixed, so the underlying peak-memory concern remains valid across current branches. |
| [#8720](https://github.com/open-mpi/ompi/issues/8720) — Derived types with CUDA-Aware MPI | Non-contiguous derived datatypes (vector/hvector/subarray) over CUDA-aware MPI trigger a separate host-device copy per contiguous chunk, making MPI_Alltoall extremely slow on GPU buffers. | v5.0.x, v6.0.x, main | ✅ Keep open | This is a known datatype-packing performance limitation for non-contiguous GPU datatypes that a second user independently reproduced in 2021; no fix or general GPU datatype-packing improvement was indicated, so the limitation plausibly still affects current branches and the issue remains valid. |
| [#8939](https://github.com/open-mpi/ompi/issues/8939) — MPI_Neighbor_allgather hang with reorderd Dist Graph | MPI_Neighbor_allgather hangs when the distributed graph communicator is created with reorder=1 (works fine with reorder=0); reporter traced it to wait_all in mca_coll_basic_neighbor_allgather_dist_graph not returning on one rank. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The basic neighbor_allgather code still exists on main and a 2022 commenter confirmed the bug persists; a recent dist-graph tag-collision fix (PR #13931/#13918, June 2026) addresses a different looped-create deadlock, not this reorder=1 neighbor case, so it remains a valid open bug likely affecting all maintained branches. |
| [#8982](https://github.com/open-mpi/ompi/issues/8982) — btl/tcp: Assert with MPI_Sendrecv() | A single-node, 2-proc run with btl self,tcp hits assertion endpoint_sd_next == -1 in mca_btl_tcp_endpoint_accept (btl_tcp_endpoint.c) during the alloc-mem MTT test, a long-standing race in TCP endpoint connection setup. | v5.0.x, v6.0.x, main (v4.0.x EOL) | ✅ Keep open | The assert at btl_tcp_endpoint.c:525 is still present in main and bosilca could not reproduce; no fix landed. v4.0.x was explicitly dropped as wontfix near EOL, but the race likely still affects current branches, so keep open. |
| [#9021](https://github.com/open-mpi/ompi/issues/9021) — Failures on MPICH tests (large_count, structpack2, struct_no_real_types, get_elements_pairtype, indexed_misc) | Several MPICH datatype test programs fail on Open MPI (large_count, struct_no_real_types, get_elements_pairtype reproduce on master; structpack2/indexed_misc only on v4.1.x), indicating datatype-handling defects. | v5.0.x, v6.0.x, main (datatype tests still failing) | ✅ Keep open | hppritcha confirmed in 2022 that large_count, struct_no_real_types, and get_elements_pairtype fail on master and assigned to bosilca; no fix recorded. Likely still-valid datatype bugs (esp. large-count) on current branches, so keep open. |
| [#9051](https://github.com/open-mpi/ompi/issues/9051) — Memory Leak using Cuda-Aware MPI_Send and MPI_Recv for large packets of data | GPU memory grows across iterations when sending/receiving large CUDA buffers with cuda-aware MPI over UCX, because UCX's cuda-ipc transport caches peer mappings and cudaFree does not release them. | v4.0.x, v4.1.x, v5.0.x, main (UCX cuda-ipc root cause) | ✅ Keep open | Root cause is in UCX cuda-ipc (not Open MPI); UCX maintainer confirmed in 2024 that the earlier claimed fix (UCX PR #10104) did NOT fix it and a real fix was planned for UCX 1.19. Users still reproduce on recent UCX+Open MPI; recommend keeping open (tracked upstream) until UCX fix is confirmed. |
| [#9062](https://github.com/open-mpi/ompi/issues/9062) — Large message collective performance drops when using coll/han | coll/han greatly improves small-message Allreduce latency but severely degrades large-message Allreduce performance compared to coll/tuned; reporter asks whether this is expected. | v5.0.x, v6.0.x, main (tuning/performance) | ✅ Keep open | A real performance-tuning gap in coll/han Allreduce; a later commenter notes increasing coll_han_allreduce_segsize from 64K to 1M helps large messages, but no default-tuning fix landed and coll/han still exists on main. Performance tracking item; keep open (or close as tuning-only if maintainers prefer). |
| [#9146](https://github.com/open-mpi/ompi/issues/9146) — osc/rdma: segfault in MPI_Compare_and_swap with flat MPI | With --mca osc rdma over btl/ofi on Omni-Path, MPI_Win_lock_all (atomic path) segfaults in mca_btl_ofi_afop when running multiple processes per node across multiple nodes. | v5.0.x, v6.0.x, main (osc/rdma over btl/ofi) | ✅ Keep open | The osc/rdma lock_all_atomic and btl/ofi afop code paths still exist on main and no fix commit was found; hjelmn proposed but never delivered ensuring btl/uct is used with osc/rdma. Likely still reproducible on current branches; recommend keeping open. |
| [#9260](https://github.com/open-mpi/ompi/issues/9260) — Using MPI_T for monitoring | When the monitoring PML is used via MPI_T (pml_monitoring_flush pvar), MPI_T_pvar_get_index fails because the pvar is flagged INVALID when ob1 unloads the monitoring component during selection, aborting the test program. | v4.0.x/v4.1.x (EOL), v5.0.x, v6.0.x, main | ✅ Keep open | Confirmed by jsquyres on 4.0.x/4.1.x/master in 2021; the monitoring common code, pml_monitoring_flush pvar, and the mark-invalid-on-close logic in mca_base_pvar.c all still exist on main and no fix commit was found. A 2023 user reported the same problem, so it is still valid; recommend keeping open. |
| [#9479](https://github.com/open-mpi/ompi/issues/9479) — OMPIO deadlock and .lock file on Lustre with MPI_File_write_all | Collective MPI-IO writes (MPI_File_write_all/write_at_all) with the default OMPIO component deadlock and leave a .lock file on Lustre; switching to romio321 works, and OMPIO I/O bandwidth on Lustre is also dramatically (up to 10x) slower. | v5.0.x, v6.0.x, main | ✅ Keep open | Multiple independent users reproduced the deadlock and severe performance gap on Lustre across v4.1.x; reporter asked whether v5.0 fixes it but received no confirmation, and the OMPIO Lustre code path remains in current sources, so the issue is plausibly still valid on v5.0.x/v6.0.x/main. Recommend keeping open pending OMPIO-on-Lustre verification. |
| [#9630](https://github.com/open-mpi/ompi/issues/9630) — Disabling tcp btl causes issues for rdma osc when running IMB-RMA | With the tcp btl disabled, the rdma osc component aborts the job (no inter-node BTL) even though the ucx osc is ultimately selected, and a related segfault occurs in IMB-RMA when rdma osc is allowed to init. | v4.1.x, v5.0.x, main | ✅ Keep open | No fix PR was identified; the request that rdma osc not abort when an inter-node BTL is unavailable (and the related IMB-RMA segfault) appears unresolved and the osc rdma component still exists on main. Recommend keeping open pending verification on a current release. |
| [#9764](https://github.com/open-mpi/ompi/issues/9764) — SHMEM Fortran library only builds with --enable-oshmem | The OSHMEM Fortran bindings only build when --enable-oshmem is explicitly passed (because the AM_CONDITIONAL checks enable_oshmem=yes), whereas the C bindings build under autodetection too, an inconsistency. | v5.0.x, main | ✅ Keep open | Labeled Target main/v5.0.x; no fix PR was identified and the AM_CONDITIONAL logic gap is a genuine build inconsistency that likely persists. Recommend keeping open (verify the conditional on current main), though it is low impact since OSHMEM Fortran is rarely used. |
| [#9795](https://github.com/open-mpi/ompi/issues/9795) — mpi_f08 configure tests failing for NAG | Open MPI's mpi_f08 configure tests fail for the NAG Fortran compiler; two configure-test bugs were fixed (PR #9812), but the deeper issue is that NAG correctly rejects the non-conforming ignore-TKR Fortran that Open MPI's mpi_f08 currently requires, so mpi_f08 still does not build with NAG. | v5.0.x, main | ✅ Keep open | Confirmed still failing on Open MPI 5.0.1 (Jan 2024 comment); NAG developer notes the bindings rely on non-standard ignore-TKR that NAG won't accept, and the real fix (proper TYPE(*),DIMENSION(..) support / auto-generated Fortran bindings) is not yet done. Recommend keeping open on v5.0.x/main. |
| [#9828](https://github.com/open-mpi/ompi/issues/9828) — Fix non-standard use of C99 flexible array in opal/mca/btl/base/btl_base_am_rdma.c | A struct with a flexible array member is used as a non-last member of another struct, which is invalid C11 though all current compilers accept it without -pedantic. | main, v5.0.x, v6.0.x (and older) | ✅ Keep open | Self-filed low-priority code-cleanup with no fix evidence in history; the construct is portability/standards risk that persists across all branches, so it remains a genuine if minor open item. |
| [#9845](https://github.com/open-mpi/ompi/issues/9845) — CUDA-aware Ireduce and Iallreduce operations for GPU tensors segfault | Nonblocking reductions (Ireduce/Iallreduce) on GPU buffers segfault because the CUDA collective component provides no implementation and falls through to host-only code instead of returning an error. | v4.1.x; needs verification on v5.0.x/v6.0.x/main | ✅ Keep open | Confirmed reproducible on v4.0.x/v4.1.x; the coll/cuda component has since been replaced by coll/accelerator on v5.0+/main, so behavior there is unverified, but the underlying gap (crash instead of graceful unsupported-op handling) was never resolved in the thread, so keep open pending verification on current branches. |
| [#9883](https://github.com/open-mpi/ompi/issues/9883) — coll/han: dynamic selection does not work for simple algorithms | HAN's dynamic module-selection MCA parameters (e.g. coll_han_bcast_dynamic_intra_node_module) are ignored, and the simple-algorithm path falls back to basic instead of honoring user selection. | main, v5.0.x, v6.0.x | ✅ Keep open | The coll selection/reorder logic (coll_include/ompi_comm_coll_preference) still exists in coll_base_comm_select.c on main and the thread ended with confusion and no merged fix, so the dynamic-selection bug remains unresolved on current branches. |
| [#9885](https://github.com/open-mpi/ompi/issues/9885) — UCC initialization fails at mca_coll_ucc_save_coll_handlers(), when under HAN | coll/ucc (and coll/han) error out in module_enable when a prior component has not populated all comm coll function pointers, which happens when a component has lower priority than basic/libnbc. | main, v5.0.x, v6.0.x | ✅ Keep open | coll_ucc_module.c on main still saves prior handlers in module_enable and the underlying ordering assumption flagged by the reporter was never fixed, so the design bug persists on v5.0.x and main. |
| [#9905](https://github.com/open-mpi/ompi/issues/9905) — System call failure: unlink during MPI_Finalize() | Intermittent 'system call failed during shared memory initialization' / unlink errors at MPI_Finalize because the vader/sm backing file is unlinked twice (once by the PMIx epilog and once by OMPI cleanup). | v4.1.x, v5.0.x; behavior changed on main (deferred cleanup) | ✅ Keep open | Root cause confirmed (duplicate unlink via PMIx epilog + OMPI cleanup), still reproduced through v4.1.4 and on Ubuntu 22.04 CI; a suggested patch to soften the warning was discussed but never merged, so the harmless-but-noisy error remains on release branches even though main now defers cleanup. |
| [#9907](https://github.com/open-mpi/ompi/issues/9907) — [Open MPI main branch] mpirun/mpicc error while loading shared libraries | Tools fail to load shared libraries because hwloc (>=2.5) adds a hard dependency on libcuda that propagates to the Open MPI tools. | main, v5.0.x (prterun/prte side) | ✅ Keep open | The mpicc case was fixed via hwloc --enable-plugins (PR #9921, verified by the reporter), but maintainers noted the larger prterun/PRRTE side remained unfixed (tracked as #9933), so a residual issue persists on v5.0.x/main. |
| [#9912](https://github.com/open-mpi/ompi/issues/9912) — Errors ignored in ft com_ft_detector code | The fault-tolerance detector thread (fd_progress) assigns return codes from PML irecv/request operations and ignores them, originally surfacing as a set-but-unused compiler warning that hinted the FT error handling is incomplete. | main, v5.0.x, v6.0.x | ✅ Keep open | On main the compiler warning was merely silenced by declaring 'int __opal_attribute_unused__ ret', but the return values from irecv/cancel/wait are still unchecked, so the substantive concern the issue raised (FT code ignores errors) remains unaddressed. |
| [#9983](https://github.com/open-mpi/ompi/issues/9983) — Warnings in master branch: src area | Numerous compiler warnings (strict-prototypes, format, sign-compare, unused-label) build from the embedded ROMIO/MPL sources under gcc 8.5. | main, v5.0.x (ROMIO/MPL warnings); likely v6.0.x | ✅ Keep open | Maintainers noted the warnings originate in ROMIO/MPL and must be fixed upstream and pulled in via a ROMIO update, which had not happened as of the last comment; labeled Target main/v5.0.x and likely still present, so keep open pending a ROMIO refresh. |
| [#10042](https://github.com/open-mpi/ompi/issues/10042) — Asynchronous Cuda Copy in pml ob1 implementation fails to check for copy completion for dtoh copy. | In pml/ob1 the device-to-host async copy stream is set but never polled for completion, so sends may proceed before the copy finishes (or never). | main, v5.0.x, likely v6.0.x | ✅ Keep open | On current main the accelerator-based ob1 code still has only a stub comment ('Consider progressing dtoh events here in future') and no dtoh completion check, and the proposed fix PR (#10852) was never merged; the latent bug remains, so keep open. |
| [#10053](https://github.com/open-mpi/ompi/issues/10053) — Lock files generated during parallel read of a NetCDF file | OMPIO's sharedfp/lockedfile leaves stray .lock/.locktest data/metadata files in input directories, especially when an open/IO call fails or crashes mid-run. | v4.1.x, v5.0.x, main | ✅ Keep open | The lockedfile component and its .lock/.locktest logic still exist on main; v4.1.3 only partially helped and the fundamental 'files not cleaned up on failure/crash' problem remains (workaround is to exclude sharedfp lockedfile,individual), so keep open. |
| [#10152](https://github.com/open-mpi/ompi/issues/10152) — opal_path_nfs test fails with (timed-out) autofs mount | The make-check test test/util/opal_path_nfs reports a false failure when an NFS filesystem is mounted via autofs and has timed out, because the autofs entry is detected as autofs rather than nfs. | all (test still present on main) | ✅ Keep open | test/util/opal_path_nfs.c still exists on main and there is no indication the autofs/timed-out-mount detection was changed, so this minor test-environment fragility likely remains; low priority but unresolved. |
| [#10186](https://github.com/open-mpi/ompi/issues/10186) — Multiple failures running collective-big-count tests with OMPI main branch and 'adapt' collective component | ompi-tests-public collective-big-count self-checking tests (allgather/alltoall/gather/scatter/reduce) fail with asserts, SIGSEGV, and wrong data using the adapt coll component on main. | main (v5.0.x label removed; main-only) | ✅ Keep open | bosilca fixed some but said others need more work; wenduwan later marked it main-only. The reserve>0 assert in coll_base_util.h still exists on main and no closing fix is recorded, so big-count correctness gaps remain on main. |
| [#10187](https://github.com/open-mpi/ompi/issues/10187) — Multiple failures running collective-big-count tests with OMPI main branch and 'han' collective component | collective-big-count self-checking tests fail (wrong data, SIGSEGV, double-free) when using the han coll component on the main branch. | main | ✅ Keep open | Same big-count failure class as #10186; bosilca said a PR would cover both but acknowledged remaining issues, and no closing fix is recorded. Likely still affects main; keep open and ideally consolidate with the other big-count reports. |
| [#10190](https://github.com/open-mpi/ompi/issues/10190) — Multiple failures running collective-big-count tests with OMPI main branch and 'tuned' collective component | collective-big-count self-checking tests fail (wrong data, SIGSEGV in MPI_Wait/Alltoall, double-free) using the tuned coll component on main. | main | ✅ Keep open | Part of the same big-count correctness problem set (#10186/#10187/#10214); no recorded resolution and the underlying large-count code paths are unchanged for these cases, so it likely still affects main. Keep open. |
| [#10214](https://github.com/open-mpi/ompi/issues/10214) — Multiple failures running collective-big-count tests with OMPI main branch | collective-big-count self-checking tests fail (wrong data, SIGSEGV, double-free) on main using the basic coll component, the baseline of the big-count failure reports. | main | ✅ Keep open | This is the basic-component (baseline) big-count failure; same unresolved correctness gaps as #10186-#10191, no recorded closing fix, so likely still affects main. Keep open as the representative big-count tracking issue. |
| [#10235](https://github.com/open-mpi/ompi/issues/10235) — ROMIO fails to build if level_zero is installed | Building Open MPI fails when Intel Level-Zero is installed because the embedded romio341 mpl uses a stale pre-release (v0.95) Level-Zero API in mpl_gpu_ze.c, with no easy configure flag to disable it. | v5.0.x and main (embedded romio341 mpl) | ✅ Keep open | Multiple users confirm the build failure persists through v5.0.8 and v5.0.9 (Feb 2026), and bertwesarg notes MPICH fixed its mpl in 2021; the embedded ROMIO mpl on supported branches still carries the outdated ZE API. Genuinely unresolved; keep open. |
| [#10374](https://github.com/open-mpi/ompi/issues/10374) — --prefix is not properly propagated to 3rd party libraries | Changing configure --prefix without wiping the build dir leaves opal_wrapper linked against the old 3rd-party (e.g. libevent) install path, causing dyld load failures. | v5.0.x, main | ✅ Keep open | This build-system/rpath propagation bug for embedded 3rd-party libs was reported on main/v5.0.x with no fix referenced and no follow-up indicating resolution; plausibly still valid, keep open. |
| [#10522](https://github.com/open-mpi/ompi/issues/10522) — btl/uct not compatible with AM RDMA? | Running osc/rdma over btl/uct aborts in am_rdma_process_rdma with 'BTL is not compatible with active-message RDMA' because the receive descriptor lacks an endpoint. | main, v5.0.x | ✅ Keep open | The am_rdma_process_rdma path and the 'not compatible with active-message RDMA' error still exist in opal/mca/btl/base/btl_base_am_rdma.c on main (now BTL_ERROR rather than abort), so the underlying btl/uct + AM RDMA incompatibility is unresolved. Labeled bug; keep open. |
| [#10523](https://github.com/open-mpi/ompi/issues/10523) — coll/tuned: Three bcast algorithms fail when mixing contig and noncontig dtypes | Three coll/tuned bcast algorithms (split_binary_tree, scatter_allgather, scatter_allgather_ring) produce wrong results when the root sends a contiguous datatype and non-roots receive with a resized/noncontiguous datatype. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The scatter_allgather and scatter_allgather_ring (and split-binary) bcast algorithms still exist in ompi/mca/coll/base/coll_base_bcast.c on main with no fix referenced; data-handling bugs in these algorithms likely persist across all listed branches. Keep open as a genuine, unresolved correctness bug. |
| [#10726](https://github.com/open-mpi/ompi/issues/10726) — MPI_Win_create fails with -np 1 | MPI_Win_create fails with MPI_ERR_WIN when run with a single rank and the osc/rdma component is used (the user's original problem was a UCX --enable-mt misconfiguration, but devreal reopened it because osc/rdma genuinely fails for np=1). | v4.1.x (osc/rdma); main improvement still wanted (error message) | ✅ Keep open | devreal confirmed a real osc/rdma bug reproducible on 4.1.x (not on 5.0.x/main) and reopened, and gpaulsen asked for at least a better error message; the underlying osc/rdma np=1 defect and the missing diagnostic remain unaddressed, so keep open. |
| [#10732](https://github.com/open-mpi/ompi/issues/10732) — Regression when HOME is not set | When HOME is unset (e.g. env -i build environments), mpicc/wrappers now hard-error with 'Unable to get the user home directory' instead of silently skipping the per-user mca-params.conf, breaking such builds. | v5.0.x, v6.0.x, main | ✅ Keep open | Verified on main: opal/mca/base/mca_base_var.c:272-274 still returns OPAL_ERROR when opal_home_directory() is NULL, so the regression the reporter described persists; absence of HOME should be tolerated, so this is still a valid bug worth keeping open. |
| [#10740](https://github.com/open-mpi/ompi/issues/10740) — Fortran Neighbor_Alltoallw fails with MPI_ERR_TYPE: invalid datatype | A standard-compliant Fortran MPI_Neighbor_alltoallw on a 3D non-periodic Cartesian communicator aborts with MPI_ERR_TYPE on Open MPI 4.0.3 and 5.0.x, while Intel MPI and NEC MPI run it correctly. | v4.0.x, v4.1.x, v5.0.x; likely v6.0.x/main | ✅ Keep open | Reproduced by the reporter on both 4.x and 5.0.x with no maintainer response or fix; this looks like a genuine neighborhood-collective datatype-validation bug (possibly tied to MPI_PROC_NULL/-1 neighbors) that was never triaged, so keep open pending verification on current main. |
| [#10860](https://github.com/open-mpi/ompi/issues/10860) — MCA_MTL_BASE_FLAG_ACCELERATOR_INIT_DISABLE does not work with non contiguous buffers | The MCA_MTL_BASE_FLAG_ACCELERATOR_INIT_DISABLE flag (intended to skip pointer detection and defer it to the underlying component) breaks ompi_mtl_datatype_pack for non-contiguous device buffers, which then uses plain memcpy on device memory and segfaults. | v5.0.x, v6.0.x, main | ✅ Keep open | Filed by a maintainer (wckzhang) describing a design flaw where the flag's intent is impossible without pointer detection; no comments or fix followed, and the MTL datatype-pack accelerator logic still exists on main, so this remains a valid unresolved bug worth keeping open. |
| [#10947](https://github.com/open-mpi/ompi/issues/10947) — Reduction latency variation with coll/tuned | On a large ConnectX-6 cluster, coll/tuned shows significant small-reduction (4B) latency variation depending on node count, while coll/han is more consistent; the binomial tree itself is unchanged between fast and slow runs. | v5.0.x, v6.0.x, main | ✅ Keep open | Labeled Target: main, a genuine performance investigation by a maintainer (devreal) with no resolution or follow-up data; the coll/tuned decision logic and trees still exist on main, so the concern remains valid. Recommend keep open as an unresolved performance issue. |
| [#11087](https://github.com/open-mpi/ompi/issues/11087) — Hang in libucs on mpi_finalize | Intermittent hang in MPI_Finalize when using the UCX PML over TCP with hcoll: a PMIx fence never completes (fenced=0) while UCX spins in uct_tcp_iface_progress after opal_common_ucx_del_procs disconnects all peers. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Recently reproduced and actively investigated (Nov 2025 comments by jeking3 and bosilca pointing at opal_common_ucx_del_procs_nofence / fence completion); the relevant common_ucx finalize/fence code still exists across all branches. Genuine, still-active bug; recommend keep open. |
| [#11123](https://github.com/open-mpi/ompi/issues/11123) — btl/sm-opal/shmem finalize order & shmem unlink error | Two finalization-order bugs in btl/sm and opal/shmem: sm_finalize runs after opal/shmem is closed (so unlink/detach are no-ops), and PMIx's epilog unlinks the /dev/shm backing files prematurely. | v5.0.x, v6.0.x, main | ✅ Keep open | The btl/sm module has since been rewritten to do per-endpoint detach in the endpoint destructor rather than the original sm_finalize unlink path, but the underlying finalization-ordering concern between opal/shmem teardown and PMIx epilog cleanup was never explicitly resolved or closed; reporter is a maintainer. Recommend keeping open to verify whether the ordering issue still manifests on main. |
| [#11166](https://github.com/open-mpi/ompi/issues/11166) — Launch scaling issue: calling PMIx_Fence five times during MPI_Init/Finalize | Sessions/instance code duplicates PMIx_Fence calls so a simple hello program issues five fences instead of the expected three during MPI_Init/Finalize, a measurable launch-scaling regression. | v5.0.x, v6.0.x, main | ✅ Keep open | On current main both ompi_mpi_init.c and instance.c still contain near-identical sets of PMIx_Fence/Fence_nb calls, matching the reported duplication; the issue was updated as recently as 2026-05-14, indicating it is still being tracked and not fixed. Recommend keeping open. |
| [#11168](https://github.com/open-mpi/ompi/issues/11168) — Hang at coll/adapt reduce with ucx | coll/adapt reduce hangs at large message sizes over UCX/InfiniBand; setting UCX_RNDV_THRESH=inf or raising adapt send/recv request limits avoids it, pointing to coll/adapt assuming sends complete independent of matching receives. | v5.0.x, v6.0.x, main | ✅ Keep open | Discussion narrowed the root cause to coll/adapt's non-blocking pipelined algorithm relying on send completion semantics that UCX rendezvous does not guarantee; no fix was committed and the thread went quiet. coll/adapt is still present on main. Recommend keeping open as a genuine, unresolved bug. |
| [#11229](https://github.com/open-mpi/ompi/issues/11229) — Hyphen is unnecessarily prepended to MCA param arguments | mpirun's OMPI-layer argument preprocessing (to accept both -mca and --mca) erroneously prepends a hyphen to MCA-param values that begin with a hyphen, so e.g. plm_ssh_args "-p 2000" becomes "--p 2000"; reporter also found --mca mca_base_env_list values get dropped. | v5.0.x, v6.0.x, main | ✅ Keep open | This is in the mpirun/OMPI CLI arg-translation layer (prterun was unaffected); no fix or PR is linked, no maintainer response, and the workaround (leading space) confirms the parsing bug. Likely still affects v5.0.x and the v6.x/main lines, so keep open. |
| [#11237](https://github.com/open-mpi/ompi/issues/11237) — MTT failure for spawn_multiple | The IBM MTT spawn_multiple test fails because child ranks receive the wrong per-app argv (job 1 vs job 2 swapped), whereas the equivalent PRRTE-only run works correctly. | v5.0.x, v6.0.x, main | ✅ Keep open | Linked PR #13059 (dpm: don't use locality info for multi-PMIX-namespace environments) was CLOSED unmerged in 2025, and the related dpm/PMIx/PRRTE discussion indicates the spawn_multiple argv ordering issue is still open. Likely still affects current v5.0.x/v6.x/main; keep open. |
| [#11391](https://github.com/open-mpi/ompi/issues/11391) — MPI_Win_allocate_shared fails with UCX | MPI_Win_allocate_shared on MPI_COMM_SELF returns MPI_ERR_INTERN only when using --mca osc ucx, even though COMM_SELF is always valid for shared memory. | v4.1.x, v5.0.x, main (unknown) | ✅ Keep open | Reported on 4.1.2 against osc/ucx; reopened by reporter as a distinct osc/ucx defect related to #10726. No fix referenced and osc/ucx still exists, so likely still affects 4.1.x and later unless verified fixed; keep open as a genuine unresolved defect. |
| [#11404](https://github.com/open-mpi/ompi/issues/11404) — Getting stuck on MPI_FInalize() when using ULFM | With ULFM fault tolerance enabled, surviving processes hang in MPI_Finalize when running across multiple nodes (single node works). | v5.0.x, main (and v4.x); unknown if fixed | ✅ Keep open | Reported on main; a later commenter reproduced equivalent multi-node ULFM failures on 5.0.2 and a 2024 nightly. No fix referenced and ULFM multi-node finalize hang appears unresolved, so keep open. |
| [#11410](https://github.com/open-mpi/ompi/issues/11410) — osc/rdma: regression of acc_single_intrinsic for fetch-and-op AMOs | The acc_single_intrinsic MCA param should let 8-byte fetch-and-op use hardware AMOs without a lock, but osc/rdma always takes an exclusive lock due to a redundant conditional. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Verified the flagged code in ompi/mca/osc/rdma/osc_rdma_accumulate.c (~lines 1093-1097) still unconditionally acquires the exclusive lock on a non-exclusive peer regardless of acc_single_intrinsic; the regression persists on current main, so keep open. |
| [#11428](https://github.com/open-mpi/ompi/issues/11428) — Deadlock in multi-threaded communication (lock-order inversion in opal?) | A hybrid MPI_THREAD_MULTIPLE + OpenMP program spuriously deadlocks in ompi_sync_wait_mt, with ThreadSanitizer reporting lock-order-inversion warnings. | v4.1.x, v5.0.x, main (unknown) | ✅ Keep open | Reproducible deadlock reported across 3.x/4.1.x with a minimal reproducer; labeled Target: v4.1.x. No fix referenced and the threaded sync path is unchanged, so likely still affects current branches; keep open. |
| [#11491](https://github.com/open-mpi/ompi/issues/11491) — configure not propagating info about lustre to ROMIO | Passing --with-lustre to Open MPI's configure does not propagate Lustre support to the bundled ROMIO, which still needs --with-io-romio-flags=--with-file-systems manually. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Maintainer (edgargabriel) confirmed --with-lustre has historically never been propagated to ROMIO and the fix is non-trivial but doable; labeled Target: main and v4.1.x. No fix has landed, so this remains a valid build-system gap; keep open. |
| [#11492](https://github.com/open-mpi/ompi/issues/11492) — MPI_T_pvar_get_num() returns extraneous number of variables and causes MPI_T_pvar_get_info() to return with error later | After MPI_Init, MPI_T pvars registered by unselected/unloaded components are marked invalid, so MPI_T_pvar_get_info returns MPI_T_ERR_INVALID for those stale indices. | v5.0.x, main (per maintainer analysis) | ✅ Keep open | Part of the test failure is a buggy reporter test (in/out name_len), but bosilca notes a real MPI standard concern: pvar info changing across MPI_Init violates the must-not-change requirement, and contributors reproduced the invalid-index behavior on 5.1.0a1. Labeled v5.0.x/v4.1.x and unresolved, so keep open. |
| [#11547](https://github.com/open-mpi/ompi/issues/11547) — --with-pmix=internal should be on by default | Vanilla configure fails with 'Could not find viable pmix build' when a stray external pmix.pc (e.g., from Homebrew Open MPI) is found, instead of falling back to the bundled internal PMIx or emitting a helpful hint. | main, v5.0.x (configure UX) | ✅ Keep open | A user reports the same failure still occurs on v5.0.8 (June 2025); configure does not auto-fall-back to internal PMIx nor suggest --with-pmix=internal. Genuine configure-UX gap still valid on main/v5.0.x. |
| [#11560](https://github.com/open-mpi/ompi/issues/11560) — Inconsistent implementation of compare and swap | The C11 stdc atomics define opal_atomic_compare_exchange_strong with memory_order_relaxed success ordering, while the GCC-builtin implementation uses __ATOMIC_ACQUIRE; the two backends are inconsistent. | main, v5.0.x | ✅ Keep open | Verified still present on main: atomic_stdc.h uses memory_order_relaxed for the base CAS while gcc_builtin/atomic.h uses __ATOMIC_ACQUIRE. The inconsistency is real and unaddressed; valid bug/cleanup. |
| [#11582](https://github.com/open-mpi/ompi/issues/11582) — Possibly incorrect F08 test in ./configure script | nvfortran 23.3+ passes the configure NO_ARG_CHECK probe and gets type(*),dimension(*) interfaces that then fail to compile user MPI Fortran programs ('cannot resolve generic procedure'); the configure test should prefer type(*),dimension(..). | main, v5.0.x, v4.1.x | ✅ Keep open | Proposed fix PR #11591 was closed unmerged (broke IBM platforms); JSC users report still being hit with NVHPC 24/25 + Open MPI 5.0.5 in Jan 2025. Unresolved across active branches. |
| [#11618](https://github.com/open-mpi/ompi/issues/11618) — osu one-sided failure with vader and rocm | OSU one-sided ROCm device-to-device benchmarks (e.g. osu_put_bibw) crash with SIGBUS in xpmem memmove / btl_sm_get at 32K message size on Frontier (smsc/xpmem + sm BTL + ROCm). | main, v5.0.x | ✅ Keep open | Tracking ticket for a GPU one-sided RDMA crash on main/5.0.0rc; no fix referenced and the sm/xpmem GPU RDMA path persists in current source. Likely still valid on main/v5.0.x; needs maintainer with ROCm hardware. |
| [#11634](https://github.com/open-mpi/ompi/issues/11634) — Deprecated info key: map_by | MPI_Comm_spawn(_multiple) with a 'map_by' info key set to 'ppr:1:node' errors with 'Conflicting directives', apparently because ompi/dpm/dpm.c adds the map_by directive to the info array twice. | main, v5.0.x | ✅ Keep open | Maintainer (rhc54) believes there is a logic bug in ompi/dpm/dpm.c emitting the mapping directive twice; labeled Target:main and Target:v5.0.x and never fixed or confirmed resolved. Genuine spawn info-key bug, likely still present. |
| [#11681](https://github.com/open-mpi/ompi/issues/11681) — Error propagation in free_fn callback for generalized requests | For generalized requests, an error returned by the user free_fn is not propagated correctly through MPI_Wait/MPI_Request_free, and the request is not set to MPI_REQUEST_NULL, complicating handle lifetime management. | main, v5.0.x | ✅ Keep open | Labeled bug targeting main/v5.0.x; the reporter requested reopening after a partial fix and disagreement remained on correct behavior vs MPICH. No resolving PR is evident, so this is still valid on current branches. |
| [#11691](https://github.com/open-mpi/ompi/issues/11691) — Missing BEGIN_C_DECLS (and END_C_DECLS) in various headers | Several headers have mismatched C-linkage guards: common_ompio_print_queue.h has END_C_DECLS without a matching BEGIN_C_DECLS, and various frameworks.h / mca_base_var.h headers were missing guards, breaking C++ inclusion. | main, v5.0.x, v4.1.x, v4.0.x | ✅ Keep open | On current main, ompi/mca/common/ompio/common_ompio_print_queue.h still has an unmatched END_C_DECLS (line 65) with no BEGIN_C_DECLS, so that defect persists; the frameworks.h headers were since removed/restructured and mca_base_var.h now has matched guards. The print_queue bug remains valid; keep open. |
| [#11740](https://github.com/open-mpi/ompi/issues/11740) — Need to increase the timeout value for PMIx_Get when using async_modex | With async_modex, OMPI needs to increase the PMIx_Get timeout because the current value causes failures even at smallish scale (per linked openpmix issue 3077). | main, v5.0.x (unknown if v4.1.x) | ✅ Keep open | Labeled bug with no comments and no linked resolving PR; the async_modex timeout behavior is internal OMPI code that would persist on current branches absent a fix. Treat as still valid and keep open. |
| [#11794](https://github.com/open-mpi/ompi/issues/11794) — Han collective do not return errors from internal colls | The HAN coll module ignores return codes from internal sub-collectives and unconditionally returns OMPI_SUCCESS (except ALLREDUCE), causing fault-tolerant (ULFM) code to miss failures and deadlock. | main, v5.0.x, v6.0.x | ✅ Keep open | Verified on current main: ompi/mca/coll/han/coll_han_barrier.c still ignores the return codes of low/up sub-barriers and returns OMPI_SUCCESS unconditionally. Labeled bug, no resolving PR; genuine and still valid across all HAN-bearing branches. Keep open. |
| [#11830](https://github.com/open-mpi/ompi/issues/11830) — [4.1.5] ORTE has lost communication with a remote daemon | On AWS EFA (Amazon Linux 2), mpirun --map-by ppr:1:node from a head node loses communication with a remote daemon during local proc launch; --bind-to core/socket works around it. | v4.1.x (ORTE); possibly v5.0.x+ per recent report | ✅ Keep open | Reporter confirmed it still reproduces on 4.1.6 (ORTE-based, binding/compute_bindings path); a 2026 third-party comment reports a similar launcher-not-in-ranks hang, while rhc54 could not reproduce on non-Amazon 4.1.8 and suspects AWS topology. Still unresolved and reproducible; keep open (likely AWS/EFA-specific, may be moot on PRRTE-based v5+). |
| [#12004](https://github.com/open-mpi/ompi/issues/12004) — The flow hangs when using MPI_Comm_Spawn for large number of processes | MPI_Comm_spawn followed by MPI_Comm_disconnect hangs for large numbers of processes (oversubscribed); the discussion uncovered a real bug in the disconnect path while Comm_free works. | v4.1.x, v5.0.x, main | ✅ Keep open | Labeled for v4.1.x/v5.0.x/main and the long thread identified a genuine disconnect-path bug (bosilca) that was never confirmed fixed/merged; keep open as a still-valid spawn/disconnect hang. |
| [#12012](https://github.com/open-mpi/ompi/issues/12012) — Performance issue in part/persist | The part/persist progress callback is registered unconditionally and performs unconditional atomic add/fetch (re-entry guard) plus a mutex on every progress loop even when no partitioned communication is used, costing ~17% of cycles in a single-threaded test on Summit. | v5.0.x, main | ✅ Keep open | The unconditional opal_atomic_add_fetch_32 re-entry guard is still present at ompi/mca/part/persist/part_persist.h around line 166 on current main and no fix has merged (a 2026 commenter is still monitoring), so the performance defect is still valid; keep open. |
| [#12019](https://github.com/open-mpi/ompi/issues/12019) — MPI_Intercomm_create leaks memory even with symmetric MPI_Comm_free calls | Repeatedly creating and freeing intercommunicators via MPI_Intercomm_create plus MPI_Comm_free leaks memory; MPI_Comm_free does not release all intercomm resources. | v4.1.x, v5.0.x, main | ✅ Keep open | A maintainer correlated minor leaks to main in 2023 and a 2025 commenter confirmed the same behavior still reproduces on 5.0.5; labeled v4.1.x/v5.0.x/main with no fix merged, so keep open as a still-valid leak. |
| [#12045](https://github.com/open-mpi/ompi/issues/12045) — coll collectives segfaults on CUDA buffers | Reduction collectives (notably non-blocking ireduce/iallreduce via libnbc/adapt, and some blocking via tuned/adapt) segfault on CUDA device buffers because ompi_op_reduce assumes host buffers and calls AVX routines on device memory. | v5.0.x, main | ✅ Keep open | ompi/op/op.h and ompi/mca/coll/libnbc/nbc.c still have no accelerator awareness on current main and no mitigation has merged; the diagnosed reduce-on-device-buffer crash remains valid for non-blocking (and tuned) reductions, so keep open. |
| [#12111](https://github.com/open-mpi/ompi/issues/12111) — Cannot include `mpi-ext.h` without `mpi.h` | Including <mpi-ext.h> before/without <mpi.h> fails because mpiext headers use OMPI_DECLSPEC, which is defined in mpi.h but not pulled in by mpi-ext.h. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The generated mpi-ext.h (config/ompi_ext.m4) still does not #include <mpi.h>, and the fix PR #12122 remains open and unmerged, so the issue persists on main and all current branches. Recommend Keep open until #12122 lands; longstanding (present since v4.1.x). |
| [#12115](https://github.com/open-mpi/ompi/issues/12115) — Open MPI hangs on MPI_Comm_split_type | Open MPI hangs in MPI_Comm_split_type(COMM_TYPE_SHARED) after Comm_spawn + Intercomm_merge + Disconnect (reproduced in C and mpi4py). | v4.1.x, v5.0.x (likely v6.0.x/main too) | ✅ Keep open | Reporter confirmed the hang on both 5.0.0 and 4.1.5 and a maintainer saw a related TCP BTL/modex error; no fix was identified despite ongoing DPM work, so the dynamic-process bug appears still valid. Recommend Keep open. |
| [#12123](https://github.com/open-mpi/ompi/issues/12123) — OpenMPI hangs during allocation of shared memory if done after allgather | Open MPI hangs in Win_allocate_shared after spawn/merge/allgather with >=128 ranks on certain Intel CPUs (8468), but works on others (8276L); also surfaces DPM-finalize crashes. | v4.1.x, v5.0.x (likely v6.0.x/main too) | ✅ Keep open | Reproduced by reporter on 4.1.6 and 5.0.0 and tied to the same dynamic-process/DPM cleanup and large-rank shared-memory path as #12115; no confirmed fix despite later DPM commits, so it remains a genuine open bug. Recommend Keep open. |
| [#12202](https://github.com/open-mpi/ompi/issues/12202) — CUDA + MPI_TYPE_INDEXED + MPI_SEND/RECV super slow | Packing/unpacking device (GPU) memory for indexed datatypes issues one cudaMemcpy per item, making MPI_TYPE_INDEXED transfers of CUDA buffers ~500x slower than manual linearization. | v5.0.x, v6.0.x, main | ✅ Keep open | A maintainer (bosilca) confirmed the inefficient one-cudaMemcpy-per-item packing is a known real defect with no fix timeline; no PR has landed. Genuine performance bug; keep open targeting v5.0.x and later. |
| [#12232](https://github.com/open-mpi/ompi/issues/12232) — TCP BTL fails to collect all interface addresses (when interfaces are on different subnets) | On a NIC carrying two IPs on different subnets, the TCP BTL keeps only one IP per kernel interface, causing 'inbound connection dropped' errors when peers bind/connect to the differing addresses (Perlmutter). | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Extensive maintainer debugging (bosilca, ggouaillardet) confirmed a real address-matching problem; ggouaillardet reproduced and prototyped a fix on main but no PR was merged, and the v4.1.x modex behavior differs. Genuine unresolved TCP BTL bug; keep open across affected branches. |
| [#12255](https://github.com/open-mpi/ompi/issues/12255) — osc/rdma does not select btl/sm for intra-node communications | osc/rdma's accelerated-BTL selection picks btl/ofi over btl/sm on single-node, hurting performance; sm is disqualified by the accelerated check and ofi lacks btl_latency for sorting. | v5.0.x, v6.0.x, main | ✅ Keep open | On current main the full_connectivity_btls default is still 'uct,ofi' (no sm) and the accelerated/alternate selection design is unchanged, so the suboptimal intra-node selection persists; discussion ended without a fix, so keep open. |
| [#12257](https://github.com/open-mpi/ompi/issues/12257) — mpifort does not pass -I/usr/include to gfortran | On systems where Open MPI installs into /usr, the Fortran wrappers omit -I/usr/include, so 'include mpif.h' fails; the wrapper deliberately filters out -I/usr/include but gfortran still needs it. | v5.0.x, v6.0.x, main | ✅ Keep open | On main the wrapper still filters '-I/usr/include' (opal_wrapper.c) and the FCFLAGS m4 still has the 'OMPI_WRAPPER__FCFLAGS' double-underscore typo at config/opal_setup_wrappers.m4:682 making OMPI_WRAPPER_FCFLAGS empty; root cause unaddressed, keep open. |
| [#12282](https://github.com/open-mpi/ompi/issues/12282) — OpenMPI 5.0.1-3 Installation Failure (make check segfaults) | 'make check' in the datatype tests segfaults because it picks up MCA components from a previously installed older Open MPI; workaround is to clear OMPI_MCA_mca_component_path or uninstall the old package before make check. | v5.0.x, v6.0.x, main | ✅ Keep open | Maintainer reopened it intending Open MPI to isolate make-check from an installed component path automatically; user's immediate problem was worked around but the underlying robustness fix was never implemented, so keep open. |
| [#12328](https://github.com/open-mpi/ompi/issues/12328) — MPI_Test and MPI_Request_get_status return wrong state with partitioned communication | After a second MPI_Start on a persistent send request from MPI_Psend_init, MPI_Test/MPI_Request_get_status report flag=true before the required MPI_Pready calls, violating the MPI 4.1 partitioned-comm semantics. | v5.0.x, v6.0.x, main | ✅ Keep open | part/persist code (part_persist.h, request_get_status.c.in) is essentially unchanged on main and the discussion ended with maintainer pings going unanswered; no fix landed, so the spec-violating completion reporting is still present. Keep open. |
| [#12413](https://github.com/open-mpi/ompi/issues/12413) — NAG 7.2 does not build 5.0.2, but does 4.1.6: -pthread passed to ld | Building Open MPI 5.0.2 with the NAG Fortran compiler fails when ld is passed -pthread (pulled from the internal libevent .la file), which 4.1.6 did not do. | v5.0.x, v6.0.x, main | ✅ Keep open | The proposed fix PR #12458 is still OPEN/unmerged, and the reporter confirmed in Aug 2025 the issue persists; the -pthread leak from internal libevent still affects v5.0.x and main. Keep open until the fix lands. |
| [#12419](https://github.com/open-mpi/ompi/issues/12419) — Internal error using shmem_reduce in example/oshmem_max_reduction.c | The newer OpenSHMEM team-based reduction API (e.g. shmem_long_max_reduce) returns an internal error because it is not implemented in the UCX SPML. | v5.0.x, v6.0.x, main | ✅ Keep open | Verified on main: mca_spml_ucx_team_reduce in oshmem/mca/spml/ucx/spml_ucx.c still just returns OSHMEM_ERR_NOT_IMPLEMENTED, and a maintainer acknowledged it is a future TODO. Genuine unimplemented feature/bug; keep open. |
| [#12437](https://github.com/open-mpi/ompi/issues/12437) — Deprecated MCA variable warning mistakenly quotes config file as source of issue instead of environment | When a deprecated MCA variable is set in the environment but a (different) value also exists in the params file, the warning blames the params file rather than the environment. | v5.0.x, v6.0.x, main | ✅ Keep open | The var_set_from_file path in opal/mca/base/mca_base_var.c still unconditionally emits deprecated-mca-file for file-matched synonyms; no fixing commit references 12437 and a maintainer called it a valid parser bug. Keep open. |
| [#12446](https://github.com/open-mpi/ompi/issues/12446) — Rankfile and MPMD Issue | MPMD launch with a rankfile using relative host indexing (+n0/+n1) fails because each app's mapping recomputes the node list excluding filled nodes, misinterpreting the relative index. | EOL versions only | ✅ Keep open | The buggy orte/mca/rmaps/rank_file code only exists in ORTE (v4.1.x); ORTE was replaced by PRRTE in v5.x and PRRTE PR #2052 was suggested as a fix. Bug is genuine but only affects the EOL v4.1.x series; keep open pending PRRTE-side confirmation or close as EOL. |
| [#12517](https://github.com/open-mpi/ompi/issues/12517) — openmpi osc_ucx_component error | On v5.0.x the osc/ucx component prints copious misleading 'osc_ucx_component.c:369' errors when UCX/ucp init fails (e.g. local machine, no usable network), which did not happen on v4.1.x. | v5.0.x, v6.0.x, main | ✅ Keep open | Members acknowledged the message is misleading and tied to failed ucp init; multiple users (through 5.0.9 in Jan 2026) still report it, with the workaround '--mca osc ^ucx' sometimes reducing bandwidth. Labeled bug/Target v5.0.x, never fixed; still affects current releases. |
| [#12548](https://github.com/open-mpi/ompi/issues/12548) — Discrepancy between 'oshcc' compiler wrapper and corresponding pkg-config files "oshmem-c.pc" and "oshmem.pc" | The generated oshmem pkg-config files (oshmem-c.pc, oshmem.pc) emit a bogus '-I.../openmpi/lib' include flag that the oshcc/oshcxx wrappers do not, and that directory contains no headers. | v5.0.x, v6.0.x, main | ✅ Keep open | No comments and no fix; this concerns generated .pc files which are produced by Open MPI's build (not purely Fedora packaging), so a stray -I in the cflags template likely still ships on v5.0.x/main. Labeled Target v5.0.x/main; unverified deeply but plausibly still present, keep open (low priority). |
| [#12584](https://github.com/open-mpi/ompi/issues/12584) — when i run mpi program using ASAN, asan reports some memory leaks | Running a trivial MPI_Init/Finalize program under AddressSanitizer/LeakSanitizer reports many leaks in ompi/pmix/opal init paths; another user reports ASAN SIGILL in mca_base_patcher (memory patcher) before reaching that point. | v5.0.x, v6.0.x, main | ✅ Keep open | A member said some leaks likely are real one-time-init leaks and intended to reproduce; no fix landed. The patcher-vs-ASAN interaction and init-time leaks are general and not version-specific, so they likely persist on v5.0.x and main; keep open. |
| [#12585](https://github.com/open-mpi/ompi/issues/12585) — Error when using MPI_Comm_spawn with ULFM enabled | With --with-ft=ulfm, MPI_Comm_spawn fails selecting an iagree coll module on the new inter-communicator ('none could be used for iagree'), an MPI_ERR_INTERN regression vs v5.0.2. | v5.0.x, v6.0.x, main | ✅ Keep open | Root cause is coll_base_comm_select requiring agree/iagree even for inter-communicators where ftagree provides no inter implementation; bosilca's proposed guard (wrap agree/iagree in OMPI_COMM_IS_INTRA) was NOT applied to main (lines ~332-333 still unconditional), and follow-on spawn/merge segfaults remain. Genuine unresolved FT-MPI bug; keep open. |
| [#12599](https://github.com/open-mpi/ompi/issues/12599) — Fault tolerant error when re spawn process in mpiexec in remote node | A ULFM master that re-spawns a worker after killing it works when the spawn target is local but fails/misbehaves once '--host node2' places the spawn on a remote node. | v5.0.x, v6.0.x, main | ✅ Keep open | Labeled Target v5.0.x/main, no comments and no fix; this is a concrete fault-tolerant MPI_Comm_spawn-on-remote-node defect with a reproducer that has not been triaged. Cannot confirm a fix landed, so treat as still valid; keep open (related to the ULFM spawn issues in 12585). |
| [#12637](https://github.com/open-mpi/ompi/issues/12637) — lower coll accelerator priority | The coll accelerator component (which stages device buffers through host memory) has a higher priority than collective components that natively support CUDA/ROCm (e.g. UCC), hurting performance unless users manually exclude it. | v5.0.x, v6.0.x, main | ✅ Keep open | The accelerator coll component default priority is still 78 in coll_accelerator_component.c with no auto-deselection logic when a device-native coll exists, so the reported priority/ordering problem still applies to main, v6.0.x, and v5.0.x; no fix has landed. |
| [#12657](https://github.com/open-mpi/ompi/issues/12657) — Bad persistent collective communication performance | Persistent (and nonblocking) collectives perform far worse than blocking ones because only the slow libnbc component supports them and hcoll is not engaged for the persistent/nonblocking path. | v5.0.x, v6.0.x, main | ✅ Keep open | Maintainers acknowledged that libnbc is the only OMPI component backing persistent collectives and its performance is poor; no component change has landed to engage faster collectives for the persistent path, so this still affects v5.0.x and later. |
| [#12816](https://github.com/open-mpi/ompi/issues/12816) — v5.0.5 sm btl hanging and producing heap-buffer-overflow | Under a workload of many concurrent point-to-point requests progressed via MPI_Testsome, the sm btl hangs and ASan reports heap-buffer-overflows in sm_prepare_src and mca_btl_sm_fbox_sendi; switching to tcp, v4.1.6, or MPICH avoids it. | v5.0.x; possibly fixed on main | ✅ Keep open | No comments and last updated Sep 2024, so unconfirmed-fixed; however significant sm btl rework landed since (fast-box rewrite 95f7141781 Jan 2025, freelist payload size fix f25ff2138e Sep 2025, descriptor-copy bound 37e8a72fe6 Apr 2026) that may address it. Likely still affects v5.0.x; recommend keeping open until the reporter confirms whether main/latest v5.0.x resolves the heap overflow. |
| [#12849](https://github.com/open-mpi/ompi/issues/12849) — [Cuda][Rocm?] memory leak in Ipc cache handling | GPU memory grows continuously to CUDA_OUT_OF_MEMORY during repeated non-blocking GPU-aware Isend/Irecv with varying message sizes, traced to cuIpcOpenMemHandle handles never being released (cache growth); disabling cuda IPC avoids the leak. | v4.1.x, v5.0.x, main (likely) | ✅ Keep open | Multiple independent reporters reproduced an unbounded IPC-handle cache growth with both the OMPI smcuda BTL path and the UCX cuda_ipc path; no fix was merged and the IPC-cache eviction behavior is unchanged, so this remains a genuine still-valid bug likely affecting v4.1.x through main. |
| [#12875](https://github.com/open-mpi/ompi/issues/12875) — Open MPI fortran configury has at least one bad test code | The Fortran configury test for STORAGE_SIZE uses storage_size(x) on an assumed-size DIMENSION(*) array argument, which the Cray (and other strict) compiler rejects, causing compliant compilers to be wrongly disqualified from Fortran feature support. | main | ✅ Keep open | Verified the bad pattern is still present on main in config/ompi_fortran_check_storage_size.m4 (storage_size(x) on DIMENSION(*) args); a comment notes a prior fix was intentionally reverted to work around a weak mpi_sizeof INTERFACE check, so the underlying audit/fix is still outstanding and the issue remains valid. |
| [#12895](https://github.com/open-mpi/ompi/issues/12895) — Passing CFLAGS down to 3rd-party/ broken due to *_CFLAGS_BEFORE_PICKY | Top-level CFLAGS are not propagated to a few 3rd-party PRRTE/PMIx sources that set CFLAGS = $(*_CFLAGS_BEFORE_PICKY) because configure never emits the AC_SUBST for those variables, so they expand empty and drop user flags. | v5.0.x, v6.0.x, main (3rd-party PMIx/PRRTE) | ✅ Keep open | Reporter provided a clear reproducer and proposed AC_SUBST patches for openpmix/prrte plus a PMIX_/PRTE_ typo fix; a maintainer was skeptical and deferred it as low priority, no fix appears merged, so the build-system defect (only triggered when picky/devel flags are active) remains valid in the bundled PMIx/PRRTE. |
| [#12922](https://github.com/open-mpi/ompi/issues/12922) — $LDFLAGS not affecting libraries linked by libmpi | User-supplied LDFLAGS (e.g. -Wl,--as-needed) appear after the linked libraries in the libmpi link command, so flags like --as-needed do not apply and unused libraries such as libcudart are not stripped. | v5.0.x, v6.0.x, main (likely) | ✅ Keep open | This is a credible libtool/Makefile link-ordering defect with no comments and no evidence of a fix; the build system's placement of LDFLAGS after the library list is unlikely to have changed, so it remains valid on current branches and warrants keeping open (verification of link ordering recommended). |
| [#12969](https://github.com/open-mpi/ompi/issues/12969) — Partitioned communications hang | A partitioned-communication program with multiple neighbors (e.g. rank 1 sending to two ranks) hangs because mca_part_persist_start reuses the outer loop variable in an inner loop, corrupting iteration over the request array. | v5.0.x, v6.0.x, main | ✅ Keep open | Confirmed: ompi/mca/part/persist/part_persist.h still declares one size_t i (line 498) and the inner loop at line 516 reuses it, exactly the shadow bug ggouaillardet proposed to fix; the fix was never merged. Still present on main and v5.0.x; keep open. |
| [#12971](https://github.com/open-mpi/ompi/issues/12971) — Memory leak with CUDA-aware OpenMPI without UCX | Repeated cudaMalloc/MPI_Bcast/cudaFree with changing buffers leaks device memory via the smcuda BTL, which registers CUDA IPC handles that are never released; --mca btl_smcuda_use_cuda_ipc 0 works around it. | v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Root cause (unreleased CUDA IPC handles in smcuda, related to #12849) confirmed by users; no fix has landed and the leak still affects production per June-2025 comments. Affects 4.1.x through main; keep open. |
| [#12977](https://github.com/open-mpi/ompi/issues/12977) — Openmpi-5.0.5 + romio + oneapi configure error | Building Open MPI 5.0.5 against a newer/incompatible oneAPI Level Zero causes the embedded ROMIO mpl_gpu_ze.c to fail to compile (undeclared zeDriverFreeMem / device_handles); --disable-io-romio works around it. | v5.0.x, v6.0.x, main | ✅ Keep open | A real build failure caused by ROMIO not version-checking oneAPI ZE; reporter requests an OMPI-side guard or a --without-ze option (as MPICH has). No fix or such option landed; the embedded ROMIO ZE detection issue likely persists in current releases. Keep open. |
| [#13002](https://github.com/open-mpi/ompi/issues/13002) — Singletons do not capture ctrl-C and therefore do not cleanup | Singleton MPI processes do not trap Ctrl-C, so on interrupt they terminate without cleaning up temporary files, a regression from earlier releases. | v5.0.x, v6.0.x, main | ✅ Keep open | Reported on both main and v5.0.x by a maintainer (rhc54) with no fix recorded; signal-handling/cleanup for singletons is in PRRTE/runtime and no PR resolving it was found. Likely still present on main and v5.0.x; keep open. |
| [#13044](https://github.com/open-mpi/ompi/issues/13044) — Segfault sometimes when building with CUDA support | A trivial MPI_Init/MPI_Finalize program segfaults ~5% of launches when Open MPI/hwloc is built with CUDA support but run on a host with no NVIDIA GPU; disabling CUDA in hwloc removes the crash. | v5.0.x, v6.0.x, main | ✅ Keep open | Reporter reproduced it across OMPI 5.0.0-5.0.9 and hwloc 2.7.1 through 2.12.2 as recently as Jan 2026, isolating the trigger to hwloc built with CUDA; still under active investigation (rhc54/bgoglin) with no fix. Affects v5.0.x and likely main; keep open. |
| [#13067](https://github.com/open-mpi/ompi/issues/13067) — Not all communicators subscribe to info keys | Communicator info-key handling is inconsistent: dup/split_type subscribe to info keys (so MPI_Comm_set_info works) while MPI_Comm_create and MPI_Comm_split only copy them, so later set/query of keys like mpi_assert_allow_overtake behaves differently. | main, v5.0.x | ✅ Keep open | Discussion confirmed the inconsistency is real (though standard-compliant) and unresolved with no PR merged; the comm.c code paths described still differ on main. Recommend keeping open as a valid harmonization task. |
| [#13068](https://github.com/open-mpi/ompi/issues/13068) — Segfault when choosing ofi btl in One-Sided mode | Selecting the ofi BTL with btl_ofi_mode=0 (one-sided only) and pml ob1 causes MPI_Send to segfault in pml_ob1_isend.c by dereferencing bml_btl->btl when it is NULL, instead of reporting a clean reachability error. | v5.0.x, v6.0.x, main | ✅ Keep open | The buggy line (pml_ob1_isend.c:93 checks bml_btl->btl->btl_sendi without a prior NULL check on bml_btl->btl) is still present on current main, so the crash likely persists on 5.0.x and later. Recommend keeping open. |
| [#13120](https://github.com/open-mpi/ompi/issues/13120) — Valgrind suppression writev(vector[...]) does not suppress for recent valgrind | The shipped openmpi-valgrind.supp tcp_send suppression uses writev(vector[...]), but valgrind 3.23+ intentionally changed the syntax so the wildcard no longer matches writev(vector[1]). | v5.0.x, v6.0.x, main | ✅ Keep open | The contrib/openmpi-valgrind.supp file on main still uses the writev(vector[...]) form (lines 79-80), so the suppression remains ineffective with modern valgrind. Recommend keeping open as a small, valid fix (update to the new syntax or a numbered index). |
| [#13127](https://github.com/open-mpi/ompi/issues/13127) — spawn: the final one to fix in ibm test suite | The ibm no-disconnect dynamic spawn test hangs because ompi_dpm_dyn_finalize's disconnect logic assumes a uniform number of dynamic intercommunicators per process, so intermediate (parent) ranks with extra dyn comms hang at MPI_Finalize. | main, v5.0.x | ✅ Keep open | A maintainer-identified design flaw in ompi_dpm_dyn_finalize's automatic dynamic-comm cleanup; no fix merged and the logic remains on main. Recommend keeping open as a genuine, still-valid bug. |
| [#13134](https://github.com/open-mpi/ompi/issues/13134) — opal_lifo test fails on FreeBSD amd64 | The opal_lifo make-check test fails on FreeBSD amd64 with clang because clang needs -mcx16 for working 16-byte atomics, but configure does not auto-detect/apply it; adding -mcx16 manually fixes it. | v5.0.x, v6.0.x, main | ✅ Keep open | configure's OPAL_ASM_CHECK_ATOMIC_FUNC tries -mcx16 only if the bare link fails, but clang links a (functionally broken) libcall first, so -mcx16 is never applied; this logic is unchanged on main and maintainers called the non-16B path broken for years. Recommend keeping open as a real, unresolved configure/atomics bug. |
| [#13170](https://github.com/open-mpi/ompi/issues/13170) — Memory leak with ROCM-aware OpenMPI with UCX 1.17.0 | GPU memory grows unboundedly when fresh ROCm buffers are used each iteration because UCX caches and never releases ROCm IPC handles. | v5.0.x, v6.0.x, main (UCX/ROCm IPC handle caching) | ✅ Keep open | Maintainer confirms this is the known IPC-handle-caching leak shared with #12849/#12971; the real fix is a UCX-side redesign not yet shipped (no quick fix in Open MPI), so the underlying issue remains valid across current versions and should stay open. |
| [#13181](https://github.com/open-mpi/ompi/issues/13181) — Linking fails with AOCC-5.0.0 compilers | Linking libmpi.so/ompi_info fails with clang/AOCC due to undefined __extendhfsf2/__truncsfhf2 half-precision builtins not in shared compiler-rt. | v5.0.x, v6.0.x, main (clang/AOCC short-float detection) | ✅ Keep open | Maintainer reproduced with upstream clang 17 and 20 and said the short-float (FP16) detection is not robust and that a fixing PR would be issued; no PR is recorded as merged in the thread, so the configure detection fix appears outstanding and the issue should stay open. |
| [#13241](https://github.com/open-mpi/ompi/issues/13241) — ompi_info needs to be more accurate about fortran f08 support | ompi_info reports inaccurate Fortran f08 capabilities (e.g. MPI_SUBARRAYS_SUPPORTED hardwired false in mpif-config.h.in) even with fully F2018-compliant compilers like recent Intel. | v6.0.x, main (mpif-config.h.in hardwired) | ✅ Keep open | Local checkout confirms mpif-config.h.in line 84 still hardwires MPI_SUBARRAYS_SUPPORTED to .false., so the configury still needs updating; the issue is unresolved and labeled Target main/v6.0.x, so keep open. |
| [#13325](https://github.com/open-mpi/ompi/issues/13325) — ULFM cannot handle faults in MPI_Comm_spawn-ed processes | With ULFM enabled, a fault in an MPI_Comm_spawn-ed child process aborts the whole job instead of raising a recoverable error, so spawned peers cannot shrink/recover. | main (ULFM + dynamic process management) | ✅ Keep open | Detailed reproducer shows spawned-process failures are not surfaced as ULFM errors (job terminates with OPAL ERROR at errhandler.c:444); no fix or workaround is recorded in the thread, so this appears to be a genuine ULFM+spawn limitation that should stay open. |
| [#13367](https://github.com/open-mpi/ompi/issues/13367) — MPI_File_get_info fails to return hints as required by MPI standard | MPI_File_get_info returns an info object with zero keys instead of all implementation-supported hints with default values as required by the MPI standard. | v5.0.x, v6.0.x, main | ✅ Keep open | Reporter reproduced 0 keys on both xfs and Lustre with 5.0.7/5.0.8; a maintainer confirmed (May 2026) this is a major rework not planned for v5/v6 and welcomes contributions, so it is a genuine standards-conformance defect still open on all current branches. |
| [#13376](https://github.com/open-mpi/ompi/issues/13376) — ompio produces adverse, conflicting access patterns for some collective writes | ompio collective writes can split data into discontiguous extents per aggregator, triggering data sieving read-modify-write and advisory locks that create conflicting writes where the application had none. | v5.0.x, v6.0.x, main | ✅ Keep open | Reproduced on origin/main (6ec63e2) with detailed analysis; the fbtl/posix data-sieving code that does the read-modify-write is still present on main (fbtl_posix_pwritev.c), so this performance/correctness-adjacent collective-IO behavior remains valid and open on current branches. |
| [#13380](https://github.com/open-mpi/ompi/issues/13380) — NAG Fortran support broken in v5.0.8 | PR 13240 introduced configure Fortran tests using the non-standard LOC() intrinsic, which NAG (and strict GNU) reject, breaking Open MPI builds with the NAG compiler. | v5.0.x, v6.0.x, main | ✅ Keep open | config/ompi_fortran_get_alignment.m4 on main still uses LOC() in its falignment test programs, so the build break persists on all current branches; the reporter offered to submit a fix (March 2026) but none has landed, so it stays open. |
| [#13427](https://github.com/open-mpi/ompi/issues/13427) — Failing to MPI_Comm_spawn proc on different node with running PRTED but no running MPI rank | Under ULFM, after shrinking away the last process on a node, MPI_Comm_spawn targeting that node fails in MPI_Init with 'Not supported' from dpm.c (PMIx_Connect). | v5.0.x, v6.0.x, main | ✅ Keep open | Maintainer fixed runtime resource-reclamation issues but confirmed the MPI/FT side still mishandles the fake namespace on spawn failure, leaving the connect_accept error path broken; needs FT folks. Genuine, unresolved on v5.0.x and main. |
| [#13428](https://github.com/open-mpi/ompi/issues/13428) — segfault due to thread-unsafe getenv() call | getenv() in interlib model_callback() races with concurrent setenv/putenv from app threads, causing segfaults in apps that launch many short-lived MPI jobs. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | Real crash confirmed by reporter; PR #13433 was filed to cache the value but maintainers debate whether any band-aid is correct since the root cause is glibc-level. No fix merged and the unsafe getenv remains in the code; broadly affects all branches. Decision pending. |
| [#13431](https://github.com/open-mpi/ompi/issues/13431) — clang OpenMP-offload + CUDA-aware Open MPI: cuMemHostRegister failed message on every MPI program | When CUDA-aware Open MPI programs are compiled with clang and -fopenmp-targets=nvptx64, MPI_Init prints a cuMemHostRegister failure (CUDA_ERROR_INVALID_VALUE) because smcuda tries to register clang's reserved-but-unbacked offload VA regions. | v5.0.x, v6.0.x, main | ✅ Keep open | Still actively under investigation as of Jan 2026; reporter supplied a workaround patch in accelerator_cuda.c but no fix is merged and maintainers are still narrowing root cause. The smcuda/accelerator-cuda registration code is present on all CUDA-capable branches. |
| [#13561](https://github.com/open-mpi/ompi/issues/13561) — mpi4py: osc/ucx dynamic window heap-buffer-overflow | ASAN reports a heap-buffer-overflow read in ompi_osc_find_attached_region_position during MPI_Win_attach on a dynamic osc/ucx window (mpi4py test). | main (and any branch with osc/ucx dynamic windows: v4.1.x, v5.0.x, v6.0.x) | ✅ Keep open | Verified on main: ompi_osc_find_attached_region_position dereferences dynamic_wins[mid_index] before checking min_index>max_index, so an empty/edge case yields mid_index=-1 (the '416 bytes before' OOB read in the report). Still unfixed; no PR merged. Genuine bug across branches with this code. |

## Fixed, not closed (70)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#27](https://github.com/open-mpi/ompi/issues/27) — Fortran wrappers are calling f2c/c2f MPI functions which get intercepted by PMPI routines | Fortran wrappers called user-facing MPI_*_f2c/c2f conversion functions, which a user's PMPI interposition library would erroneously intercept as user MPI calls. | fixed on all | 🗑️ Close | The current generated mpif-h wrappers (e.g. allgather_f.c, abort_f.c) call PMPI_Comm_f2c/PMPI_Type_f2c and PMPI_<op> rather than the MPI_* entry points, so PMPI interposition no longer intercepts these internal conversions; the reported defect is resolved on main. |
| [#48](https://github.com/open-mpi/ompi/issues/48) — Add several datatypes to Fortran and C++ bindings | MPI-2.1 promoted MPI_LONG_LONG_INT/LONG_LONG, MPI_UNSIGNED_LONG_LONG, MPI_SIGNED_CHAR, MPI_WCHAR to mandatory in all bindings, but they were missing from mpif.h and some C++ bindings. | fixed on all | 🗑️ Close | These datatypes are now defined in the Fortran bindings (e.g. mpi-f08-types.F90 defines MPI_SIGNED_CHAR, MPI_WCHAR, MPI_UNSIGNED_LONG_LONG, MPI_LONG_LONG_INT) and the C++ bindings were removed entirely, so the gap is closed on main; a 2014 comment already asked to verify-and-close. |
| [#194](https://github.com/open-mpi/ompi/issues/194) — Non-uniform BTL usage not working | When peers don't all load the same BTL, ompi_modex_recv() returning not-found was treated as a hard error (e.g. spurious usnic BTL warning) instead of simply skipping that peer for that BTL. | fixed on all | 🗑️ Close | The usnic and TCP BTLs were fixed (commits r31258/r31753) to treat a missing modex key as 'peer can't use this BTL'; the TCP BTL on main now checks OPAL_ERR_NOT_FOUND and proceeds. Functionally resolved long ago; recommend closing. |
| [#206](https://github.com/open-mpi/ompi/issues/206) — statfs() on RHEL 6.5 lies about enfs (reports it as NFS) | Linux statfs() misreports fuse.encfs mounts as NFS, causing the test/util/opal_path_nfs unit test to fail on a build machine with such a mount. | fixed on all | 🗑️ Close | The fix (skip fuse filesystems) is present on current main in test/util/opal_path_nfs.c (line 201-202), so the original test failure is resolved; recommend closing. |
| [#231](https://github.com/open-mpi/ompi/issues/231) — segmentation fault in coll_tuned_allgather | ompi_coll_tuned_allgather_intra_bruck miscomputed its scratch-buffer size (true_extent + true_lb with negative true_lb), causing a segfault/heap corruption in MPI_Allgather with MPI_IN_PLACE. | fixed on all | 🗑️ Close | A fix landed in master and v1.8 in 2014; the allgather code has since been moved to ompi/mca/coll/base and entirely rewritten (the buggy coll_tuned_allgather.c no longer exists), so the defect is resolved on supported branches. Recommend closing. |
| [#665](https://github.com/open-mpi/ompi/issues/665) — TCP causes performance penalty on single-node runs | When the TCP BTL is unused (e.g. vader/sm has exclusivity) it never decremented the opal progress counter, so libevent was polled too frequently, hurting single-node performance especially on macOS. | fixed on all | 🗑️ Close | PR #3738 (move the progress increment/decrement to connection establishment) was merged on 2017-09-20, addressing the reported behavior. Recommend closing. |
| [#1122](https://github.com/open-mpi/ompi/issues/1122) — /usr conflicts with RH 7/7.1 | Installing the openmpi RPM on RHEL7 reported file conflicts for /usr/bin and /usr/lib64 against the filesystem package. | fixed on all | 🗑️ Close | A maintainer identified the fixing commit (6ab32895) at the time; this was a v1.8-era packaging defect and the RPM spec has long since changed. Fixed but never closed. |
| [#1764](https://github.com/open-mpi/ompi/issues/1764) — Handling of datatypes with initial gaps in certain in-place collectives | MPI_Allreduce/MPI_Reduce with MPI_IN_PLACE and a datatype that has an initial gap segfaulted because the temporary buffer was not adjusted down by the gap before copying content. | fixed on all | 🗑️ Close | Fixed by commit 0e393195d (2016); current coll_base_allreduce.c uses inplacebuf = inplacebuf_free - gap exactly as requested, and the gap paradigm is applied throughout. The reported defect no longer exists, so close. |
| [#3438](https://github.com/open-mpi/ompi/issues/3438) — Problem with MPI I/O with np>1 and large filetypes | MPI-IO with more than one rank and very large filetypes crashed (datatype release) and produced wrong output in OMPIO/ROMIO. | fixed on all | 🗑️ Close | PR #3439 plus a ROMIO upstream backport and bosilca's datatype rework fixed it (confirmed working by edgargabriel) and were merged to master and release branches at the time; the issue was simply never closed. Recommend Close. |
| [#3769](https://github.com/open-mpi/ompi/issues/3769) — Segv in new monitoring coll | MTT saw segfaults in the new monitoring coll/common monitoring infrastructure even when monitoring was not explicitly enabled. | fixed on all | 🗑️ Close | bosilca reproduced it and committed fix f8ffec926e ('Protect the monitoring infrastructure initialization'), which is present in the local git history; the issue was just never closed. Recommend Close. |
| [#4032](https://github.com/open-mpi/ompi/issues/4032) — NAG Fortran compiler does not recognize the -pthread option | Building Open MPI 2.1.1 with the NAG Fortran compiler fails linking the use-mpi-tkr shared library because the unrecognized -pthread option is passed to NAG. | EOL versions only | 🗑️ Close | The reporter confirmed the issue is resolved in v3.0.0rc2 (same fix as #4034 / PR #2092). Only the now-EOL v2.1.x series was affected; recommend closing as fixed. |
| [#4034](https://github.com/open-mpi/ompi/issues/4034) — NAG Fortran compiler -rpath configuration error | With NAG 6.x and Open MPI 2.1.1, mpifort passes -Wl,-rpath which results in -rpath reaching gcc unrecognized; wrapper -Wl handling was wrong for NAG. | EOL versions only | 🗑️ Close | The reporter verified both v3.0.0rc2 and PR #2092 applied to 2.1.1 fix the problem. Only EOL v2.1.x was affected; recommend closing as fixed. |
| [#4099](https://github.com/open-mpi/ompi/issues/4099) — Huge number of leaked symbols when compiling with icc | The make-check symbol-name checker reported many false-positive leaked symbols when building with icc 17.0, causing spurious make check / MTT failures. | fixed on all | 🗑️ Close | Per the discussion the agreed resolution (PR #4119) was to remove the symbol checker from the critical make check path; on current main the symbol checking lives only under contrib/ (illegal_symbols_report.pl, symbol-hiding.pl), not in make check, so the false-failure problem is resolved. Recommend closing. |
| [#4264](https://github.com/open-mpi/ompi/issues/4264) — master: getting segv's in many Intel MPI tests over TCP BTL (double free in MPI_Finalize?) | Many Intel MPI tests segfaulted in MPI_Finalize (double free) due to proc->proc_hostname being used uninitialized after a PMIx update. | fixed on all | 🗑️ Close | ggouaillardet fixed the uninitialized proc_hostname on master (commit b3558f2) and stated only master was affected; the proc/hostname code has since been rearchitected (hostname now via opal_get_proc_hostname function pointer, no stored field). The crash is long resolved; recommend closing. |
| [#4282](https://github.com/open-mpi/ompi/issues/4282) — memory/patcher selected also when all tests fail | On platforms (e.g. Cygwin) where __curbrk/linux/mman.h/__syscall checks all fail, the memory/patcher component was still selected and tried to intercept unavailable syscalls like munmap. | fixed on all | 🗑️ Close | The proposed fix (guarding each syscall interception with #if defined(SYS_munmap) etc.) is present on current main (memory_patcher_component.c lines 192-217, 616-618), and configure.m4 was rewritten; the patcher now compiles cleanly even when individual syscalls are absent. Effectively resolved; recommend closing. |
| [#4596](https://github.com/open-mpi/ompi/issues/4596) — generalized request in FORTRAN exits with error | A Fortran generalized-request test fails with MPI_ERR_TRUNCATE because the Fortran query callback leaves status.MPI_ERROR (which Fortran lacks) as garbage, propagating a bogus error. | fixed on all | 🗑️ Close | Verified on current main: ompi/request/grequest.c now seeds fstatus via MPI_Status_c2f and sets status->MPI_ERROR = rc on failure (lines 263-273), the fix from PR #6416 (which replaced #4597). Resolved; recommend closing. |
| [#5818](https://github.com/open-mpi/ompi/issues/5818) — TCP BTL: problems when there are multiple IP interfaces in the same subnet | TCP BTL fails/hangs when a host has multiple IP addresses in the same subnet or on the same interface, dropping inbound connections with an 'unexpected IP address' message; modex/split_and_resolve mismatch identified as root cause. | v4.0.x/v4.1.x (EOL); fixed on v5.0.x+ | 🗑️ Close | The most recent reporter (2024-01-22) confirmed the bug is resolved in Open MPI 5.0.1, and related PRs (#9681/#9718) for v5.0.x merged; jsquyres confirmed the original problem was never fixed in 4.0.x/4.1.x (now EOL). Recommend closing as fixed on supported branches. |
| [#6876](https://github.com/open-mpi/ompi/issues/6876) — mca/coll: segfault on v4.0.x | Segfault in ompi_coll_base_retain_datatypes_w during MPI_Ialltoallw, bisected to PR #6806 on v4.0.x, noted as a likely duplicate of #6870. | fixed on all | 🗑️ Close | Identified as a duplicate of #6870 and a regression from PR #6806; the retain_datatypes_w code on main has since been rewritten with REQUEST_COMPLETE and NULL guards, and #6870 was resolved. The crash is fixed on supported branches but this duplicate was left open; close. |
| [#7110](https://github.com/open-mpi/ompi/issues/7110) — Error in ompi_io_ompio_calcl_aggregator() running netcdf testsuite on 32-bit hardware | The netcdf parallel testsuite fails on 32-bit hardware (i686/arm) with an ompio aggregator error: rank_index(-2) >= num_aggregators(1), pointing to integer overflow in the ompio aggregator calculation. | fixed on all | 🗑️ Close | edgargabriel said a fix was coming and the reporter confirmed in Nov 2019 that the netcdf tests were fixed; the named function ompi_io_ompio_calcl_aggregator no longer exists on main (ompio aggregator code was refactored into common_ompio_aggregators.c). Fix landed but the issue was never closed; close. |
| [#7672](https://github.com/open-mpi/ompi/issues/7672) — Inconsistent & Perhaps buggy dynamic tuning behavior | The coll/tuned dynamic-rules 'message size' definition is inconsistent across collectives; notably MPI_Gather used datatype*comm_size with no element count. | fixed on all | 🗑️ Close | PR #7758 (merged 2020-05-22) fixed the gather/scatter message-size calculation; current main computes dsize*scount*comsize (includes element count), resolving the reporter's main complaint. The remaining cross-collective inconsistency is by design per bosilca. Close as fixed. |
| [#7809](https://github.com/open-mpi/ompi/issues/7809) — MPI_File_read_at_all hanging | OMPIO collective read (MPI_File_read_at_all via fcoll/vulcan) hangs with CGNS/HDF5 when ranks read uneven counts; reproduced by maintainer. | fixed on all | 🗑️ Close | Maintainer (edgargabriel) reproduced it, identified the root cause within days in 2020, and stated a fix was coming (trivial fix for 4.0.x plus the proper fix going forward); the OMPIO collective-read path has since been substantially reworked. Appears fixed long ago but the issue was never closed; recommend closing (verify the linked fix landed). |
| [#7813](https://github.com/open-mpi/ompi/issues/7813) — RMA Functionality | Global Arrays MPI-RMA test suite showed many failures/hangs with Open MPI (osc/ucx and osc/rdma), far more than other MPI implementations, on 4.0.x/4.1.x. | v4.0.x, v4.1.x (EOL); largely fixed on v5.0.x | 🗑️ Close | Extensive osc/ucx and osc/rdma fixes landed for v5.0.x (MamziB's work), and the reporter re-ran on v5.0.2 in 2024 with only 2-3 remaining failures (two being flaky thread-safety tests); maintainers stated they will not fix 4.x. The original broad regression is effectively resolved on supported versions; recommend closing (any residual osc/ucx nbput crash is better tracked as a fresh, narrow issue). |
| [#8051](https://github.com/open-mpi/ompi/issues/8051) — ofi_create_recv_tag mask hides ssend's "ack" bit | In the OFI MTL, the recv tag-matching ignore mask used sync_proto_mask instead of sync_send, so a user-posted receive could wrongly consume an ssend's internal ack message. | fixed on all | 🗑️ Close | The exact fix the reporter described is present on main: both mtl_ofi_create_recv_tag and _CQD now set *mask_bits = ompi_mtl_ofi.sync_send (mtl_ofi_types.h lines 253 and 184). Bug is fixed on main (and was requested for v4.1.x backport); close it. |
| [#8236](https://github.com/open-mpi/ompi/issues/8236) — When oversubscribing on a system with multiple NIC, the PMIx call for locality string from local peers returns a null locality string. | When oversubscribing (procs unbound, no locality), common/ofi package-rank calculation gets a null PMIX_LOCALITY_STRING and emits a misleading 'multiple equidistant NICs' warning even though the procid fallback works. | fixed on all | 🗑️ Close | Fix PR #8244 was posted and merged, and the show_help is now gated behind a verbosity check with a dedicated 'unbound_process' path and procid fallback in opal/mca/common/ofi/common_ofi.c (plus later package-rank fixes #11693/#11710/#12480). The reported behavior is resolved on main; recommend closing. |
| [#8261](https://github.com/open-mpi/ompi/issues/8261) — Compile: ARM cluster, Cray 10.0.1, error parsing compiler version during configure | The Cray C++ compiler version string contained unescaped parentheses that caused a shell 'eval' syntax error in configure when capturing opal_cv_compiler_VERSION_STR. | fixed on all (still on EOL 4.x if not backported) | 🗑️ Close | On main the compiler-version detection macro (config/opal_check_compiler_version.m4) only captures numeric FAMILYID/VERSION and no longer evals a free-text VERSION_STR; commits cb1756831bf and fc6328f583b fixed the escaping/handling, so the crash is resolved on main/v5.x. Recommend closing as fixed. |
| [#8398](https://github.com/open-mpi/ompi/issues/8398) — mpich testsuite MPI I/O fixes | Several mpich I/O testsuite cases (setviewcur, i_setviewcur, i_bigtype, tst_fileview) failed with ompio; the fix was prepared by the ompio maintainer. | fixed on all (v4.0.x/v4.1.x/main) | 🗑️ Close | Author (edgargabriel) stated the fix was available and would be committed to main and backported to v4.0.x/v4.1.x; this is a maintainer-tracked fix from 2021 that was almost certainly landed but the issue was left open. Recommend closing as fixed-not-closed. |
| [#8878](https://github.com/open-mpi/ompi/issues/8878) — Incompatibility between hwloc (internal) and system hwloc on Ubuntu 20.04 (Arm) | Open MPI 4.1.1's internal hwloc 2.0.x crashes when SLURM (srun --mpi=pmix) feeds it an XML topology containing the newer 'Die' element produced by system hwloc 2.1.0. | fixed on all | 🗑️ Close | rhc54 posted PRs #8880 and #8881 to let parsing continue without erroring out, and v5.0.x build/packaging guidance plus newer bundled hwloc address the version skew; the immediate crash mitigation landed, so close (root-cause prevention is an external-hwloc packaging recommendation). |
| [#8919](https://github.com/open-mpi/ompi/issues/8919) — NVIDIA 'nvfortran' cannot link libmpi_usempif08.la | Building Open MPI's Fortran usempif08 library fails to link with nvfortran because Libtool does not auto-add -fPIC for the unrecognized nvfortran compiler. | fixed on all | 🗑️ Close | PR #12722 patches autogen.pl/configure to inject -fPIC handling for nvfortran, and it was backported to v4.x and v5.x (PRs #12878, #12879) plus README updates; the build failure is resolved on main and the maintained release branches, so close. |
| [#9385](https://github.com/open-mpi/ompi/issues/9385) — Iallreduce gives an incorrect result for algorithms 1 and 3 | OMPI_MCA_coll_libnbc_iallreduce_algorithm=1 or 3 (ring) returns wrong results with MPI_IN_PLACE because the ring algorithm did not support MPI_IN_PLACE and silently produced garbage instead of erroring. | fixed on v5.0.x/v6.0.x/main and v4.1.x; v4.0.x had a merge conflict (EOL) | 🗑️ Close | Commit ff03f43b58 'Fix the libnbc MPILAllreduce ring algorithm' (Fixes #9385) is on main and v6.0.x and was reported merged to v5.0.x/v4.1.x; only v4.0.x (now EOL) had a conflict. Fix landed but issue was never closed; recommend closing. |
| [#9396](https://github.com/open-mpi/ompi/issues/9396) — client/server mechanism broken? | MPI_Comm_connect hangs in the client/server (MPI_Open_port / MPI_Comm_accept / MPI_Comm_connect) example on v3.1.4 and v4.0.x, while working on older v1.8.8. | fixed on all | 🗑️ Close | The reporter retested and found it works in v4.1.x and explicitly stated they are happy for the issue to be closed; older affected versions are EOL. Recommend closing. |
| [#9406](https://github.com/open-mpi/ompi/issues/9406) — btl/rdma warnings | The btl base am_rdma code emitted -Wpedantic warnings (flexible array member, braced-groups in OPAL_THREAD_SCOPED_LOCK) and -Wpointer-sign signedness warnings on the atomic helper calls. | fixed on all | 🗑️ Close | On current main opal/mca/btl/base/btl_base_am_rdma.c the macro now uses a plain ACTION argument (no braced-group) and the atomic call sites use properly-typed int32_t/int64_t temporaries, so all reported warnings are resolved. Recommend closing as fixed. |
| [#9511](https://github.com/open-mpi/ompi/issues/9511) — Coverity build failing due to common_ofi compile error | The Coverity build (against old Libfabric 1.4) failed because common_ofi.c used FI_ENOSYS without including rdma/fi_errno.h, which older Libfabric did not auto-include from fabric.h. | fixed on all | 🗑️ Close | Current main opal/mca/common/ofi/common_ofi.c explicitly includes <rdma/fi_errno.h> before using FI_ENOSYS, so the build error is resolved; the discussion was an old CI/infra failure tied to a now-superseded minimum Libfabric version. Recommend closing as fixed. |
| [#9629](https://github.com/open-mpi/ompi/issues/9629) — OpenBSD 7.0 statfs, no f_type | ROMIO fails to build on OpenBSD 7.0 because its file-system-type detection uses statfs f_type, which OpenBSD lacks; the fix is to use f_fstypename instead. | fixed on all | 🗑️ Close | Rob Latham produced a portable-stat fix and PR #9661, which is merged into the local tree (commit d66ac511153). The fix is present on main, so the issue can be closed as resolved. |
| [#9689](https://github.com/open-mpi/ompi/issues/9689) — mpi4py 3.1.2 test segfaults in UCX - ucp_mem_unmap | MPI_Win_free on a dynamic window in osc/ucx passes a NULL memh to ucp_mem_unmap (local_dynamic_win_info[idx].memh==NULL), causing a SIGSEGV in mpi4py's TestWinCreateDynamicWorld on v4.1.2. | EOL versions only | 🗑️ Close | Maintainer said PR #10126 fixed it; the osc/ucx free path on main was rewritten to use opal_common_ucx_wpmem_free over the dynamic_win_count entries, removing the buggy raw ucp_mem_unmap(NULL) loop. Fixed on main; the crash affected v4.1.x. Recommend closing. |
| [#9842](https://github.com/open-mpi/ompi/issues/9842) — old code for GPFS support in romio321 needs stat64=>stat in ad_gpfs_open.c | ROMIO321 GPFS support failed to compile because ad_gpfs_open.c used stat64 instead of stat. | fixed on v4.0.x/v4.1.x; moot on v5.0.x+/main (romio321 removed) | 🗑️ Close | The stat64->stat fix was merged to both v4.0.x (PR #9888) and v4.1.x (PR #9887); main no longer ships romio321 (only ompio remains in ompi/mca/io), so the issue is fixed where applicable and moot elsewhere. |
| [#9950](https://github.com/open-mpi/ompi/issues/9950) — mca_btl_smcuda.super.btl_cuda_eager_limit missing OPAL_CUDA_GDR_SUPPORT protection | btl_smcuda referenced CUDA-GDR-only struct members (btl_cuda_eager_limit, MCA_BTL_REG_FLAG_CUDA_GPU_MEM) without #if OPAL_CUDA_GDR_SUPPORT guards. | fixed on v5.0.x/v6.0.x/main; older only if present | 🗑️ Close | On main the smcuda code was refactored to the accelerator framework: the relevant assignment is now btl_accelerator_eager_limit wrapped in #if OPAL_CUDA_GDR_SUPPORT and the MCA_BTL_REG_FLAG_CUDA_GPU_MEM use in btl_smcuda.c is likewise guarded, so the reported unguarded references are resolved. |
| [#9976](https://github.com/open-mpi/ompi/issues/9976) — Option --preload-files causes segmentation fault | mpiexec --preload-files segfaults inside PRRTE's prte_set_attribute/prte_attr_load due to a bad data pointer. | fixed on all (PRRTE fix); EOL versions only if any | 🗑️ Close | Maintainer confirmed the bug is in PRRTE and fixed upstream in openpmix/prrte#1199; the fix has long since been pulled into the OMPI PRRTE submodule, so the issue is resolved and can be closed. |
| [#9988](https://github.com/open-mpi/ompi/issues/9988) — Warnings on master branch: vprotocol | vprotocol_pessimist_wait.c emitted -Wlanguage-extension-token warnings because its PREPARE_REQUESTS_WITH_NO_FREE macro used the GCC typeof() extension. | fixed on main | 🗑️ Close | On current main the macro uses standard 'size_t i' instead of 'typeof(count) i', so the language-extension warning no longer exists; the warning is fixed and the issue can be closed. |
| [#10027](https://github.com/open-mpi/ompi/issues/10027) — PBS library path (--with-tm) ignored during configure of PRRTE | configure --with-tm=/opt/pbs failed to find libtm because the libdir was omitted from the library search during the TM check in PRRTE. | fixed on all (PRRTE fix) | 🗑️ Close | Maintainer fixed it in PRRTE and the reporter confirmed it works after a submodule update; the fix was ported to PRRTE release branches, so the issue is resolved and can be closed. |
| [#10132](https://github.com/open-mpi/ompi/issues/10132) — Not able to set stripe and stripe size using mpi_info_set | Lustre striping_factor/striping_unit MPI_Info hints were ignored or swapped in ompio's fs/lustre component, so MPI_File_open did not set the requested stripe count/size. | fixed on v5.0.x and main; v4.x not back-ported | 🗑️ Close | edgargabriel confirmed the swapped info-object bug and the user verified the fix on a topic branch; it was merged to main and v5.0.x (PRs #10135/#11191/#11713). The defect is resolved on supported branches, so this can be closed. |
| [#10328](https://github.com/open-mpi/ompi/issues/10328) — Error with combination of MPI_Win_create_dynamic() and MPI_Get() using osc rdma | MPI_Get/Rget on a dynamic window over osc/rdma + btl/openib hangs or returns MPI_ERR_RMA_RANGE on v4.1.x, while osc/ucx works. | v4.1.x (EOL, openib); fixed/moot on v5.0.x/main | 🗑️ Close | Reporter said the fix landed (merged PR 10473 'Removed deadlock in find dynamic region'); the failing path used btl/openib which is removed in v5.x, so the dynamic-window deadlock is fixed/moot on supported branches; close. |
| [#10370](https://github.com/open-mpi/ompi/issues/10370) — segfault when trying to open significantly too many contexts | Segfault in mca_btl_ofi_context_finalize/alloc_scalable instead of a graceful abort when btl/ofi tries to open far too many HFI contexts on v4.1.2. | v4.1.x (EOL); higher unknown | 🗑️ Close | Cornelis (the HFI/OFI BTL owner) said they had a PR for the fix targeted at v4.1.x; with no reproducer hardware outside Cornelis and the target being EOL 4.1.x, the fix was driven by the responsible vendor; close (re-open only if Cornelis confirms it persists). |
| [#10629](https://github.com/open-mpi/ompi/issues/10629) — ompi_comm_finalize is called before osc ucx component_finalize | On main the finalize ordering changed so ompi_comm_finalize runs before osc/ucx component_finalize, differing from v4.1.x and potentially problematic. | fixed on main/v5.0.x | 🗑️ Close | A maintainer (janjust) stated it was fixed via merged PR #10709 and explicitly left the issue open as a reminder; the fix landed in 2022. Close as fixed but not closed. |
| [#11066](https://github.com/open-mpi/ompi/issues/11066) — OMPI main branch random hang/segfault | A late-2022 main-branch random hang/segfault in mpirun was traced to a PRRTE regression (node->available NULL in rmaps binding); fix was to bump the embedded PRRTE submodule pointer. | fixed on all | 🗑️ Close | Reporter opened PR 11068 to update the PRRTE pointer and noted the bug was already fixed in PRRTE master; this transient submodule-pointer issue from 2022 is long resolved on all current branches. Recommend close. |
| [#11295](https://github.com/open-mpi/ompi/issues/11295) — Hang/segfault in MPI_Waitany | On openSUSE with system libfabric, self-communication via the OFI MTL hangs/segfaults in MPI_Waitany/MPI_Wait because libfabric's experimental 'net' (and tcp) providers were being selected; workaround was forcing tcp or excluding ofi. | fixed on all | 🗑️ Close | Labeled question; root cause was the experimental libfabric 'net' provider. On current main the OFI common default_prov_exclude_list is "shm,sockets,tcp,udp,rstream,usnic,net", so 'net' is now excluded by default (the d7ef0d4a fix the maintainers referenced). Issue effectively resolved; recommend close. |
| [#11370](https://github.com/open-mpi/ompi/issues/11370) — MPI_Comm_Spawn doesn't work with intracomms that have ranks on multiple machines | With Open MPI v4.1.4 in a Kubernetes/TCP environment, MPI_Comm_spawn from an intracommunicator whose ranks span multiple pods failed with Unreachable/lost-daemon errors; single-host spawn worked. | EOL versions only | 🗑️ Close | Reporter confirmed the spawn bug is gone when retesting with v5.0.0rc10. The remaining MPI_Intercomm_create multi-host hang is a different problem (the reporter asked about filing a separate issue). The original v4.1.x spawn defect is fixed in v5.x and v4.x is EOL; recommend close (split out the intercomm hang if still relevant). |
| [#11485](https://github.com/open-mpi/ompi/issues/11485) — Segfault calling MPI_T_finalize after MPI_Finalize | Calling MPI_T_finalize() after MPI_Finalize() on Open MPI 4.1.5 segfaults inside mca_base_components_close. | v4.1.x only (EOL); fixed on main/5.0.x | 🗑️ Close | Reporter explicitly states the program works on current main; the MPI_T finalize path was reworked (finalize.c uses ref-counted ompi_mpit_init_count and guarded teardown). Only the EOL 4.1.x line was affected, so close as fixed on supported branches. |
| [#11604](https://github.com/open-mpi/ompi/issues/11604) — Segmentation fault on exit with oshmem 4.1.5 | OSHMEM 4.1.5 segfaults at exit in opal_mem_hooks_release_hook via UCX madvise interception; workaround was --mca opal_common_ucx_opal_mem_hooks 0. | fixed on all (4.1.6+, v5.0.x) | 🗑️ Close | Fixed by PR #11621 (merged 2023-05-15) and cherry-picked to v5.0; reporter confirmed the segfault is gone in 4.1.6rc1. Remaining UCX FIFO/mpool warnings are unrelated. Close as fixed. |
| [#11719](https://github.com/open-mpi/ompi/issues/11719) — mpirun interprets program's command line arguments as its own | In OMPI 4.1.x, mpirun parses the app's own arguments (e.g. after a lone ':' app-context separator) and the '--' end-of-options marker did not work. | EOL versions only (v4.x); fixed in v5.0.x+ via PRRTE | 🗑️ Close | The '--' marker bug was fixed upstream in PRRTE (openpmix/prrte#1746) and landed in OMPI v5+; only the legacy v4.x ORTE-based series, which won't get the backport, is affected. Recommend close as fixed in supported series. |
| [#12307](https://github.com/open-mpi/ompi/issues/12307) — mpi4py: Remaining spawn/accept/connect issues | High-stress MPI_Comm_spawn loops from the mpi4py test suite caused segfaults/deadlocks in ompi_comm_nextcid/dpm connect-accept on main and v5.0.x. | fixed on main; v5.0.x now passing | 🗑️ Close | rhc54's dpm.c rewrite (PMIx_Group) plus prrte fixes landed; reporter confirmed Jan 2025 that main and v5.0.x both pass the spawn tests in CI and re-enabled them. Fixed but left open, recommend close. |
| [#12349](https://github.com/open-mpi/ompi/issues/12349) — Singleton init mode, MPI_Comm_spawn, and OPAL_PREFIX | In singleton init mode, MPI_Comm_spawn could not locate the prte binary because dpm.c used opal_find_absolute_path('prte') which ignored OPAL_PREFIX, breaking relocated installs not on PATH. | fixed on main and v5.0.x | 🗑️ Close | PR #12363 added find_prte() in dpm.c honoring OPAL_PREFIX->PRTE_PREFIX/PMIX_PREFIX (present at ompi/dpm/dpm.c:1962-2031) and the reporter confirmed fixed in main (May 2024); remaining smcuda warning is an unrelated singleton issue. Fixed, recommend close. |
| [#12475](https://github.com/open-mpi/ompi/issues/12475) — job launch fails with srun: selected pml ob1, but peer selected pml | Launching with srun --mpi=pmix on Slurm 23.11 fails with a PML mismatch because the PML name returned from the modex is corrupted; mpirun works. | fixed on all | 🗑️ Close | Root-caused to a PMIx gds/shmem2 key-index bug fixed in PMIx 5.0.3 (the workaround PMIX_MCA_gds=^shmem2 was confirmed); current main bundles PMIx 6.1.1, so it is fixed. Recommend closing. |
| [#12677](https://github.com/open-mpi/ompi/issues/12677) — memory leaks calling mpi_file_write_all | Repeated MPI_File_write_all calls leaked memory (notably an unfreed fh->f_aggr_list and datatype refcount issues), eventually OOM-killing the process under Open MPI 4.1.5. | fixed on v5.0.x/main; likely still affects v4.1.x | 🗑️ Close | fcoll/vulcan memory-leak fixes landed (commits ff740b42/4bc0fb71/3fde6af4 Sep 2024 plus cleanup 6043ac55); fh->f_aggr_list is now freed in file_write_all.c, and the reporter confirmed reduced leakage. Fixed on main/v5.0.x; the 4.x branch likely still lacks the related CM refcount fix (PR #12544), so close after confirming. |
| [#12787](https://github.com/open-mpi/ompi/issues/12787) — --with-mpi-moduledir does not create the installation directory | Installing with a custom --with-mpi-moduledir fails because the Fortran install-exec-hook tries to copy .mod files into a directory that was never created. | fixed on main; likely still affects v5.0.x | 🗑️ Close | Commit 03fdd945cc (Jan 2026) added '$(INSTALL) -d $(DESTDIR)$(OMPI_FORTRAN_MODULEDIR)' to the install-exec-hook in all three Fortran Makefile.am files, which creates the directory. Fixed on main; close after confirming the cherry-pick to v5.0.x (reporter said it still failed on 5.0.5/5.0.6). |
| [#12802](https://github.com/open-mpi/ompi/issues/12802) — --without-knem (and --with-knem=no) builds knem anyways | Configuring with --without-knem / --with-knem=no still detected and built the smsc:knem component, later emitting /dev/knem open-failure warnings at runtime. | fixed on main; was broken on v5.0.x | 🗑️ Close | config/opal_check_knem.m4 now honors --with-knem=no (commit cf026165df 'configury: allow knem to be disabled', Aug 2025, plus the --with-knem=PATH fix 416d64a2b4 May 2026), both landing after the Sep 2024 report. Fixed on main; close after confirming v5.0.x cherry-pick. |
| [#12812](https://github.com/open-mpi/ompi/issues/12812) — MPI_Comm_split_type creates overlapping communicators (OMPI_COMM_TYPE_L3CACHE) | When ranks are bound to cores spanning two L3 cache domains, MPI_Comm_split_type with OMPI_COMM_TYPE_L3CACHE produced overlapping (non-disjoint) sub-communicators instead of disjoint ones. | fixed on main; likely v5.0.x | 🗑️ Close | Maintainers diagnosed the root cause (ranks bound across two L3 domains) and decided such ranks should yield MPI_COMM_NULL; rhc54 reported committing a fix upstream (in PRRTE/hwloc topology handling) in Oct 2024. Fix landed upstream; close after confirming it is in the bundled PRRTE on main/v5.0.x. |
| [#12837](https://github.com/open-mpi/ompi/issues/12837) — timeout always 14 seconds when MPIEXEC_TIMEOUT set to any value | Setting MPIEXEC_TIMEOUT to any value caused the job timeout to always be 14 seconds instead of the requested value; the --timeout CLI flag worked correctly. | v5.0.x (likely fixed in later PRRTE) | 🗑️ Close | Reporter identified this as a regression in PRRTE's 3.0.x branch (openpmix/prrte#2018) affecting OMPI v5.0.x; the bug lives in the bundled PRRTE and was almost certainly fixed in a later PRRTE/OMPI 5.0.x release, so this should be verified-fixed and closed. |
| [#12881](https://github.com/open-mpi/ompi/issues/12881) — PRRTE build failure with GCC 14.2.1 on Fedora 40 | Open MPI main failed to build because the OMPI fork of PRRTE passed a void** to PMIx_Get expecting pmix_value_t**, producing an incompatible-pointer-type error with GCC 14.2. | main (at the time) | 🗑️ Close | The bug was in the OMPI fork of PRRTE and the fix was made upstream (openpmix/prrte#1976) and cherry-picked into the fork (open-mpi/prrte#48); since the submodule pointer has long since advanced past that fix, this build failure is resolved and should be closed. |
| [#12891](https://github.com/open-mpi/ompi/issues/12891) — mpirun using application options as mpirun options | When no mpirun options are given, mpirun mis-parses the application's arguments (e.g. 'mpirun /bin/true --version') as its own options, printing mpirun's version instead of running the app. | v5.0.x (fixed in later release) | 🗑️ Close | A maintainer confirmed 'noted and fixed, just waiting for release' for this PRRTE CLI-parsing bug; the labeled v5.0.x fix has since shipped in a later 5.0.x point release, so this should be verified-fixed and closed. |
| [#13043](https://github.com/open-mpi/ompi/issues/13043) — static libraries, F90 module, and alignment warnings | Static linking with the mpi (F90) module emits many ld alignment-discrepancy warnings for Fortran common symbols (e.g. mpi_fortran_weights_empty_); using mpi_f08 avoids them. | fixed on all | 🗑️ Close | Fixed: PR #13240 ('fortran: fix common symbol sizes and alignments') is merged on v5.0.x (2025-05-12) and the fix was merged on main per jsquyres; the underlying common-symbol alignment was corrected. Recommend closing as fixed. |
| [#13048](https://github.com/open-mpi/ompi/issues/13048) — Device to Device transfers don't work with OpenMPI + LinkX provider on AMD GPUs | OpenMPI 5.0.6 with the shm+cxi:lnx LinkX provider fails GPU device-to-device transfers (buffer registration error) on AMD GPUs, traced to a fix in PR #12290 not being backported to 5.x. | v5.0.x (fixed via PR #13090) | 🗑️ Close | The requested backport (PR #12290) was created as PR #13090 and merged to v5.0.x on 2025-02-14, addressing the reported cxi case; a secondary commenter's opx+cuda variant is a separate matter. Recommend closing as the primary fix landed. |
| [#13076](https://github.com/open-mpi/ompi/issues/13076) — Freeze or error using function MPI_Comm_connect | An MPICH disconnect_reconnect spawn/connect/accept test hangs or errors with Open MPI 5.0.6, due to a NULL s_info memkind crash in ompi and a defect in the PRRTE data-server's publish/lookup handling. | v5.0.x, main (PRRTE/PMIx side) | 🗑️ Close | hppritcha added the memkind NULL guard and the PRRTE data-server was refactored upstream (PRRTE PR #2146 and follow-ons); he confirmed the test then works. The verified fixes exist, so recommend closing (once the fixed PRRTE/PMIx versions ship). |
| [#13190](https://github.com/open-mpi/ompi/issues/13190) — Issue with NVFORTRAN With NVHPC 25.3 | Building with NVHPC 25.3 fails compiling psizeof_f.f90 because Open MPI assumes complex(REAL16) is supported whenever real(REAL16) is, but NVFORTRAN only supports the latter. | fixed via configure (v5.0.x and newer) | 🗑️ Close | Maintainer identified the bad assumption, gave the workaround (ompi_cv_fortran_have_iso_fortran_env_real16=n) which the reporter confirmed works, and issued a configure PR that tests both real and complex REAL16; the fix is in hand so recommend Close. |
| [#13191](https://github.com/open-mpi/ompi/issues/13191) — SEGV in ompi_request_default_test_all() when triggering IPoIB networking problem during ucc_perftest run | A SEGV in the coll/ucc oob_allgather_test occurs because isend/irecv errors are not handled and reqs[] are left uninitialized when an IPoIB networking error occurs. | fixed on main; v4.1.x (reported) may still need backport | 🗑️ Close | The reporter's fix landed via PR #13238 which is MERGED to main on 2025-05-07; the defect is resolved on main (v4.1.x was the report target so confirm/cherry-pick if not already), so recommend Close. |
| [#13336](https://github.com/open-mpi/ompi/issues/13336) — MPI_Type_get_envelope_c is wrong | MPI_Type_get_envelope_c left 4 of 8 bytes of its MPI_Count output args uninitialized due to (int*) casts, causing valgrind errors and broken downstream PETSc/MOOSE code. | fixed on main/v6.0.x; v5.0.x may need check | 🗑️ Close | Local checkout shows type_get_envelope_c.c was rewritten (size_t locals, proper *num_large_counts set, embiggened ompi_datatype_get_args) by the 'Big count support for datatypes' commit, removing the buggy int casts; the bug is fixed on main, so recommend Close after confirming any needed v5.0.x backport. |
| [#13347](https://github.com/open-mpi/ompi/issues/13347) — osc/ucx does not honor thread level requested | The osc/ucx worker was always created with UCS_THREAD_MODE_SINGLE in common_ucx_wpool.c, ignoring MPI_THREAD_MULTIPLE and triggering UCX owner-thread assertions on UCX >= 1.18. | fixed on main | 🗑️ Close | On main, common_ucx_wpool.c:59 now sets thread_mode from opal_common_ucx_single_threaded (UCS_THREAD_MODE_SERIALIZED when multithreaded), and osc/ucx sets that flag from opal_single_threaded; the hardcoded SINGLE is gone, so this appears fixed on main and can be closed (verify the fix is on intended release branches). |
| [#12277](https://github.com/open-mpi/ompi/issues/12277) — nvcc undeclared builtins reporting failure -- configure test method for PMIx | Configuring Open MPI 5.0.x with CC=nvcc fails in the embedded OpenPMIx 'cannot make nvcc report undeclared builtins' check, plus downstream -Wno-unused-parameter/-MT and Fortran build issues with NVHPC. | v5.0.x (mostly fixed via OpenPMIx submodule advance) | ❓ Needs info | rhc54 removed -Wno-unused-parameter from PMIx under --disable-devel-check and jsquyres noted the core PMIx issue resolves once the v5.0.x OpenPMIx submodule advances to >=4.2.9 (long since done); remaining NVHPC nvcc/nvfortran problems are compiler-specific. Ping reporter to confirm with current 5.0.x, likely close. |
| [#13337](https://github.com/open-mpi/ompi/issues/13337) — OpenMPI main branch 3rd-party prrte points to obsolete/bugged version | The PRRTE submodule on main points to a prrte v2/v3-era commit that has a bug where '--map-by method:mod1,mod2' only honors the first modifier; fixed in PRRTE v3 and backported to the v5.0.x line. | main (v5.0.x fixed via PR 13263) | ✅ Keep open | Confirmed main's prrte submodule is still psrvr-v2.0.0rc1-5205-ga2d2b039a4 tracking master, which the reporter says still has the modifier bug; v5.0.x got PR 13263 but main did not, so the submodule pointer still needs updating on main. |
| [#13432](https://github.com/open-mpi/ompi/issues/13432) — MPI_Waitall doesn't return MPI_ERR_IN_STATUS for persistent request | Several completion functions (Waitall, Testall, Testany) fail to report MPI_ERR_IN_STATUS / error codes for persistent requests, and the error path may wrongly free persistent requests. | fixed on main; pending cherry-pick to v5.0.x, v6.0.x | ✅ Keep open | Confirmed valid by maintainer; fixed on main (commits 38a7fbb837, f22bc9de8b plus req_wait/req_test now handle req_persistent and errhandler_invoke skips persistent). Maintainer noted 2026-01-06 it still needs cherry-pick to v5.0.x and v6.0.x, so keep open until backported. |
| [#13451](https://github.com/open-mpi/ompi/issues/13451) — Regression: Spawned processes are not killed on timeout | With Open MPI 5, mpirun --timeout does not terminate spawned (MPI_Comm_spawn) processes; the job hangs until a ~1-hour fallback instead of the requested timeout, a regression from v4. | regression on v5.0.x; fixed in PRRTE master, pending OMPI release | ✅ Keep open | Maintainer says fixed upstream in PRRTE PR #2299 but unknown if/when it lands in an OMPI release; the PRRTE submodule shipped with v5.0.x still predates the fix. Keep open until the PRRTE bump is pulled into v5.0.x/v6.0.x. |

## Answered (1)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#9481](https://github.com/open-mpi/ompi/issues/9481) — make install fails relinking libmpi_usempi.so with LLVM flang | Building Open MPI 4.1.1 with LLVM 12 clang/flang fails at make install relinking libmpi_usempi because the f18/flang Fortran objects failed to compile (argument mismatch), tracing to flang directive incompatibilities. | EOL versions only | 🗑️ Close | A maintainer diagnosed it as a flang/f18 limitation, gave the workaround FCFLAGS=-fallow-argument-mismatch, noted it is unnecessary from Open MPI 5, and reported the directive issue upstream to LLVM; the v4.1.x target is now EOL. Recommend closing as answered/obsolete. |

## Awaiting reporter (6)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#8278](https://github.com/open-mpi/ompi/issues/8278) — Received msg header indicates a size that is too large - ptl_base_max_msg_size | Launching ~100k ranks across Docker Swarm overlay networking hit a PMIx ptl max-msg-size error, then after raising the limit hit BTL unreachable / MPI_Init failures, apparently a networking/scaling problem in v4.0.5. | EOL versions only (v4.0.x/v4.1.x; ORTE/PMIx3) | ❓ Needs info | Discussion pointed at the Docker overlay networking and old PMIx3/ORTE stack; the last maintainer comment (Apr 2021) asked the reporter if it can be closed and got no reply, and the stack is EOL. Recommend Needs info / close as stale; not relevant to supported branches. |
| [#8425](https://github.com/open-mpi/ompi/issues/8425) — MPI internal error in MPI_Dist_graph_create_adjacent | A Java MASON simulation got MPI_ERR_INTERN from MPI_Dist_graph_create_adjacent after many rebalancing calls, consistent with a communicator leak / running out of CIDs. | unknown (reported v5.0.0a1; likely communicator/CID exhaustion) | ❓ Needs info | Diagnosis converged on communicator-id exhaustion likely caused by the application not freeing communicators rather than an Open MPI defect, and the reporter never returned with a minimal reproducer. Recommend Needs info / close as awaiting reporter. |
| [#11165](https://github.com/open-mpi/ompi/issues/11165) — lustreapi.h is not useable | configure fails to use DDN-provided Lustre 2.14 headers because lustre/lustreapi.h nested-redefines enum llapi_json_types already defined in the kernel lustre_user.h, breaking ompi_check_lustre.m4. | v4.1.x, v5.0.x, v6.0.x, main | ❓ Needs info | Maintainer diagnosed it as a likely bug in the DDN lustreapi.h header (a duplicate enum definition) rather than in Open MPI's configure macro, and asked the reporter to follow up with DDN; reporter opened upstream DDN/GCP issues but never reported resolution back. Open MPI's macro is arguably correct. Recommend pinging the reporter and closing if no response. |
| [#11478](https://github.com/open-mpi/ompi/issues/11478) — Deadlock in mpi_bcast using openmpi 4.1.5 from fortran | A Fortran mpi_bcast deadlocks with >=2 ranks on 4.1.5 when a particular Ethernet interface is active; works over loopback/wifi or with PR #11472 applied. | v4.1.x (likely environment/network) | ❓ Needs info | Reporter confirmed the deadlock is tied to a problematic network interface and that PR #11472 (topology fix) resolves it; this is largely an environment/network issue masked by a patch. Unclear if #11472 landed for 4.1.x, so needs info/confirmation before closing. |
| [#12224](https://github.com/open-mpi/ompi/issues/12224) — OMPI 5.0 segfault at startup with uct_md.c:203 UCX ERROR error unless -mca btl ^uct is added | Open MPI 5.0.0 segfaults in mca_btl_uct_query_tls during MPI_Init with UCX 'Transport ... does not exist' errors that look like a transport-name parsing bug; workaround is --mca btl ^uct. | v5.0.x | ❓ Needs info | Maintainer asked the reporter to retry with v5.0.1 and the reporter agreed but never reported back (last activity Jan 2024); cannot confirm whether still reproducible on the current uct BTL. Recommend Needs info: ping reporter and close if no response. |
| [#13130](https://github.com/open-mpi/ompi/issues/13130) — CUDA-aware MPI build on fresh Ubuntu 24.04 LTS, MPIX_Query_cuda_support() returns zero | On a built-from-source 5.0.x with CUDA on Ubuntu 24.04, MPIX_Query_cuda_support() returns 0 under mpirun but 1 when run directly; the accelerator/cuda component is built and installed but the null accelerator is selected at runtime. | v5.0.x | ❓ Needs info | Likely an environment/CUDA-stubs/LD_LIBRARY_PATH issue (a similar case was resolved with --with-cuda-libdir); maintainers requested config.log and more diagnostics the reporter has not fully provided. Recommend needs-info: ping for the requested logs and close if no response. |

## Cannot reproduce / needs info (65)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#10047](https://github.com/open-mpi/ompi/issues/10047) — libopen-pal.so: undefined reference to PMIx_Value_unload/PMIx_Info_xfer/PMIx_Info_load | Building Open MPI master from git failed to link/configure due to a stale external PMIx install lacking devel headers being picked up over the embedded PMIx. | main (2022 git build only); not current | 🗑️ Close | Maintainers determined it was the reporter's local environment (an external /usr/local PMIx without devel headers); full configure output was never provided and the build system has changed substantially since 2022. Cannot reproduce and stale; recommend close. |
| [#10297](https://github.com/open-mpi/ompi/issues/10297) — mca_io_ompio_file_write_at_all() failed during parallel write of PnetCDF lib | Crash with UCX process_vm_readv/CMA and Transport-retry errors during a 512-rank PnetCDF collective write via ompio on Open MPI 4.1.3; failure tied to ompio (using io=^ompio works). | v4.1.x (EOL); higher unknown | 🗑️ Close | Extensive UCX/IO debugging never isolated a root cause; only a workaround (-mca io ^ompio) was found, the maintainer no longer has the platform or resources, and it is on EOL 4.1.x with no small reproducer; close as needs-info/cannot-progress. |
| [#10969](https://github.com/open-mpi/ompi/issues/10969) — MTT failure on IBM | Both v5.0 and main reported 'Unable to extract peer ... nodeid from the modex' errors from topo/treematch's distgraph1 test (mpirun -n 144), suggesting OMPI may pass an incorrect proc name when querying PMIX_NODEID. | unknown | 🗑️ Close | The emitting code (topo_treematch_dist_graph_create.c line ~177) still exists, but wenduwan confirmed in 2024 that the test has since been passing in MTT history, so the failure appears resolved. Cannot reproduce currently; recommend close. |
| [#11129](https://github.com/open-mpi/ompi/issues/11129) — Segmentation fault using openmpi 4.1.2 | User reports a segfault running a Perl-based application (maker) under mpiexec with Open MPI 4.1.2, with Perl signal handlers visible in the stack trace. | EOL versions only | 🗑️ Close | Maintainer noted the stack trace shows Perl, strongly suggesting an application bug rather than an Open MPI defect, and recommended upgrading; reporter said they would try and never followed up. v4.1.x is effectively EOL and the issue is unconfirmed against Open MPI. Recommend closing. |
| [#122](https://github.com/open-mpi/ompi/issues/122) — MPI_Scan with MPI_SUM on unsigned char overflows incorrectly | MPI_Scan with MPI_SUM on the reversed MPI_COMM_WORLD overflows/fails for MPI_UNSIGNED_CHAR (and other small int types), but works on the standard communicator and with MPICH2. | unknown | ❓ Needs info | A 2010 report with no comments, no reproducer attached, and no recent activity; arithmetic overflow of small unsigned types is arguably expected behavior, so this needs re-verification on a current branch before any action, recommend needs-info/re-test then close. |
| [#151](https://github.com/open-mpi/ompi/issues/151) — "ifort -i8" issues with MPI attributes | Compiling Open MPI with ifort -i8 (FCFLAGS=-i8) produces int*/long long* type-mismatch warnings in the MPI attribute callback machinery, suggesting attributes may be broken under 8-byte Fortran integers. | unknown | ❓ Needs info | Imported from a 2012 Trac ticket with no activity since; ifort/icc are now deprecated by Intel and the bindings were entirely rewritten (generated). Unverifiable on current toolchains, so ping for a current reproducer and close if none. |
| [#153](https://github.com/open-mpi/ompi/issues/153) — Another "ifort -i8" problem | When Open MPI is built with 8-byte default Fortran integers (-i8 / -fdefault-integer-8), MPI_STATUS_SIZE and MPI_Status_f2c/c2f mishandle the int-vs-Fortran-integer sizing, so accessing status fields like MPI_TAG from Fortran reads the wrong bytes. | unknown | ❓ Needs info | 2012-era report against v1.6/v1.7; the Fortran bindings and status conversion code have since been rewritten, but -i8 status handling was never explicitly confirmed fixed. Needs a current reproducer to verify; close if reporter cannot confirm. |
| [#174](https://github.com/open-mpi/ompi/issues/174) — MPI_Test fails for iBarrier if other requests for iSends/iRecvs are open | A server/client pattern mixing an MPI_Ibarrier with an outstanding any-source MPI_Irecv deadlocks in MPI_Test on the Irecv after the barrier (Open MPI 1.7.1), while MPICH 3.0.4 works. | unknown | ❓ Needs info | Reported against the very old v1.7.1; the nonblocking collective (libnbc) and request progress code have been heavily reworked since. No reproduction on a supported release exists, so request a current test or close as stale. |
| [#199](https://github.com/open-mpi/ompi/issues/199) — coll/tuned MPI_Bcast can crash or silently fail when using distinct datatypes accross tasks | With coll/tuned (default), MPI_Bcast where ranks use different datatypes whose byte sizes differ either crashes with MPI_ERR_TRUNCATE or silently produces wrong output, while coll/basic works. | unknown | ❓ Needs info | Reported against trunk in 2014; coll/tuned has been substantially reworked and the MPI standard requires matching type signatures across the broadcast, making the silent-failure case arguably user error. Needs re-verification on a current release to decide if a real defect remains; ping or close. |
| [#409](https://github.com/open-mpi/ompi/issues/409) — OPAL MCA memory leaks | valgrind showed two small never-freed MCA-var string allocations (opal_signal and opal net private_ipv4) registered in opal/runtime/opal_mca.c, suggesting the MCA var base did not free non-framework opal-group vars. | unknown | ❓ Needs info | opal/runtime/opal_mca.c no longer exists and the code has been substantially reorganized; hjelmn believed he fixed it years ago but never confirmed. Whether a tiny one-time leak remains needs a fresh valgrind run. Recommend pinging/needs info, close if not reproducible. |
| [#1620](https://github.com/open-mpi/ompi/issues/1620) — OMPI-1.7 MPI_Alltoallv hang | Indeterministic hang/crash in osu_alltoallv over the TCP BTL at ~512KB on a cluster; later narrowed to failing only when restricted to eth0, working on ib0. | EOL versions only | ❓ Needs info | Reported on EOL v1.7/v1.10; the trail ended pointing at an eth0-interface-specific issue without a clean reproducer on a supported version. Cannot reproduce on a maintained release without more info; close if reporter does not respond. |
| [#2203](https://github.com/open-mpi/ompi/issues/2203) — Segfault running mpich lock_dt rma test | The MPICH lock_dt RMA test segfaults during datatype unpack (opal_generic_simple_unpack) when run with ob1 over self,vader,sm, indicating memory corruption in the unpack path. | unknown | ❓ Needs info | Reproduced on master in 2017 with valgrind showing an invalid write in unpack_contiguous_loop, but the osc/rdma and BTL (vader to sm) code have been substantially rewritten since and there is no recent confirmation; re-test on a current branch before deciding, so needs info. |
| [#2284](https://github.com/open-mpi/ompi/issues/2284) — OpenMPI 2.0.x: bug: violent break at beginning with (sequential) runs... | Intermittent heap corruption (realloc invalid next size) in orted inside the pmix112 component during singleton/sequential launch on the v2.0.x branch. | EOL versions only | ❓ Needs info | Reported only against long-EOL v2.0.x with the embedded pmix112 component (since replaced by PMIx), was never reliably reproducible, and the reporter could not provide a core dump. The implicated code is gone from modern branches; close unless reproducible on a supported release. |
| [#2389](https://github.com/open-mpi/ompi/issues/2389) — MPI_Lookup_name fails if process for name has exited | With a standalone orte-server, MPI_Lookup_name fails (PACK-PMIX-VALUE UNSUPPORTED TYPE / PMIX error) because stale entries for exited publishers are zeroed but not pruned from the name registry. | unknown | ❓ Needs info | Reported against 2.0.1 using orte-server and the old ORTE/embedded-PMIx data-server path; the runtime is now PRRTE/PMIx and ORTE is gone, so the original code path no longer exists. Needs re-test against a current release before deciding; likely close as obsolete. |
| [#6001](https://github.com/open-mpi/ompi/issues/6001) — CUDA-aware MPI broken after calling MPI_Bcast | With CUDA-aware Open MPI 2.1.5/3.1.3, GPU-to-GPU communication produces garbage (-nan) after an intervening collective (MPI_Barrier/MPI_Bcast); workaround is to disable smcuda CUDA-IPC (btl_smcuda_use_cuda_ipc 0) or use the UCX PML. | unknown; smcuda CUDA-IPC path | ❓ Needs info | Reported against EOL 2.x/3.x with ancient CUDA 8/9; the smcuda BTL is largely unmaintained and still present on main but the CUDA-aware path has since moved to UCX/accelerator framework. The reporter found a UCX workaround and discussion stalled in 2018 with no maintainer reproduction; needs re-verification on a current version or should be closed for lack of activity. |
| [#6710](https://github.com/open-mpi/ompi/issues/6710) — MPI.COMM_SELF.Spawn() error | Repeated MPI_Comm_spawn (via mpi4py) of a Fortran executable fails after several thousand spawn/disconnect cycles with an 'Unreachable' error during the spawned process's MPI_Init. | EOL versions only | ❓ Needs info | Reported against the long-EOL Open MPI 3.1.4; spawn/dynamic-process handling was completely rewritten with PRRTE in v5.0+, so this is very likely obsolete on supported branches. No reproduction on a current version exists; ping reporter and close if no response. |
| [#6719](https://github.com/open-mpi/ompi/issues/6719) — Openmpi conflicting with wifi on macOS | On macOS, MPI_Comm_split (during startup) fails with MPI_ERR_ARG only when connected to certain enterprise WiFi networks; turning WiFi off makes the same program run fine. | unknown | ❓ Needs info | Reproduced on v4.0.0/v4.0.2 (both EOL) but never root-caused; likely a TCP/oob interface-selection issue tied to specific network interfaces. The networking and runtime stack changed substantially since (PRRTE/PMIx), so retest on v5.0.x/main is needed; ping reporter, close if no longer reproducible. |
| [#6818](https://github.com/open-mpi/ompi/issues/6818) — mpirun segfault in mpi_comm_connect/accept on multimachine jobs | Connect/accept between separately-launched ompi-server-coordinated jobs spanning multiple machines segfaults in orte_rml_base_parse_uris (NULL strdup); works on a single machine and on old v1.10.7. | EOL versions only | ❓ Needs info | Crash is in ORTE (orte_rml_base_parse_uris), which was entirely removed and replaced by PRRTE/PMIx in v5.0+, so the specific code path no longer exists on supported branches. Reported against EOL 4.0.1; wckzhang's 2023 follow-up questions went unanswered. Ping reporter and close if not reproducible on v5.0.x/main. |
| [#7460](https://github.com/open-mpi/ompi/issues/7460) — Weird alltoallw segfault when libcuda and btl smcuda are present | With CUDA-aware build and libcuda present, smcuda BTL gets selected for host transfers and segfaults during alltoallw/allgather; disabling smcuda or CUDA support avoids it. | EOL versions only | ❓ Needs info | Reported against v4.0.2; bosilca could not reproduce on v4.1.x or v5.x in 2023 even forcing smcuda. smcuda still exists on main, but no current reproducer; ping reporter to retest on a supported release and close if not reproducible. |
| [#7608](https://github.com/open-mpi/ompi/issues/7608) — Datatype engine segfaults in MTT scatter tests | An MTT run reported a datatype-engine segfault in scatter tests, linked only via a now-dead MTT URL. | unknown | ❓ Needs info | The issue body is just a dead MTT link with no reproducer, stack trace, or version; impossible to verify against current main. Needs concrete reproduction info; close if none can be supplied. |
| [#7782](https://github.com/open-mpi/ompi/issues/7782) — Investigate file munging issue found in openpmix | Investigate whether an OpenPMIx fix (improving MCA component file-search messages to print full filenames/suffixes) should be ported into OMPI's mca_base_component_repository code. | v5.0.x, v6.0.x, main | ❓ Needs info | Maintainer follow-ups in 2021 and 2024 show this was never ported and suggested reproducing first; it is a low-severity diagnostic-message improvement that likely still applies but is unverified on main. Recommend confirming whether it still reproduces; close if not actionable. |
| [#7965](https://github.com/open-mpi/ompi/issues/7965) — OMPI+UCX on GPUs : drop of performance compared with pt2pt | MPI_Put/MPI_Get RMA over UCX on GPUs (Summit) gives much lower bandwidth than faking RMA over pt2pt, because UCX RMA lacked the full CUDA protocol selection (e.g. cuda-ipc) available to the tag API. | unknown | ❓ Needs info | Root cause is a UCX-side limitation, not Open MPI code; later comment notes recent UCX adds CUDA RMA via UCX_PROTO_ENABLE=y and the reporter intended to retest. With osc/pt2pt removed in 5.0 and the fix living in UCX, ping reporter to confirm on current UCX/OMPI; close if no response. |
| [#8032](https://github.com/open-mpi/ompi/issues/8032) — Segfault with Openmpi-4.0.3 + ucx-1.8.1 when running IMB-MPI1 | IMB-MPI1 Alltoall over UCX 1.8.1 on a Broadcom 100G RoCE adapter aborts with a UCX rc_verbs assertion that a QP entered an invalid state, then segfaults. | unknown | ❓ Needs info | The failure is a UCX assertion (QP state corruption) on a specific RoCE NIC with old UCX 1.8.1, not Open MPI code; maintainer asked the reporter to investigate the QP state and there was no follow-up. Needs reporter info on current UCX, otherwise close as a likely UCX/driver-side issue. |
| [#8034](https://github.com/open-mpi/ompi/issues/8034) — MTL-OFI + verbs provider will crash during MPI_INIT on MTHCA type hardware. | Using pml cm + mtl ofi with libfabric's verbs provider segfaults during MPI_Init (inside libfabric rxm progress) on old Mellanox MT25208/mthca hardware; newer mlx4 hardware is unaffected. | unknown | ❓ Needs info | Crash backtrace is inside libfabric's rxm/verbs provider on very old/unsupported mthca hardware; no comments and no follow-up since 2020. Likely a libfabric/provider issue on EOL hardware that no one can reproduce; ping reporter for current libfabric results, else close. |
| [#8050](https://github.com/open-mpi/ompi/issues/8050) — MPI_Put fails for trivial example on v4.0.2 | A trivial MPI_Put example segfaults in mca_btl_vader_put_cma (osc/rdma) on an ARM InfiniBand cluster running Open MPI v4.0.2 with two ranks. | unknown | ❓ Needs info | Reported only against the year-old v4.0.2; maintainer immediately asked the reporter to retest with v4.0.5 (noting prior vader fixes) and there was no response. Cannot confirm it reproduces on any supported branch; ping/await retest and close if no reply. |
| [#8080](https://github.com/open-mpi/ompi/issues/8080) — Potential GPUDirect RMDA bug | CUDA-aware MPI_Sendrecv on GPU buffers crashed for mid-size messages with Open MPI 3.1.6 and 4.0.5 (worked in 3.0.6) on an IB system without GDRCopy/nv_peer_mem installed. | EOL versions only | ❓ Needs info | Reported against EOL 3.x/4.0.x with no GPUDirect support libraries installed and never triaged by a developer; the CUDA datapath has been entirely reworked (smcuda/accelerator framework) since then. Cannot verify on current code without hardware; ping reporter / close if no response. |
| [#8098](https://github.com/open-mpi/ompi/issues/8098) — BTL UCT got active message id 26, but no handler installed | Running the full IMB suite with OB1 over the btl/uct component triggers UCX 'got active message id N, but no handler installed' warnings followed by a segfault during Alltoall, suggestive of resource exhaustion. | unknown | ❓ Needs info | Labeled Target:main but only one external me-too comment in 2023 and no developer reproduction; btl/uct is a niche, non-default transport and the failure is plausibly in UCX active-message handling. Needs a current-version reproduction; recommend pinging and closing if no info. |
| [#8114](https://github.com/open-mpi/ompi/issues/8114) — MPI_Init_thread provided status | With the UCX PML on v3.1.6, requesting MPI_THREAD_MULTIPLE prints 'UCP worker does not support MPI_THREAD_MULTIPLE' yet MPI_Init_thread returns provided=MPI_THREAD_MULTIPLE instead of a lower supported level. | EOL versions only | ❓ Needs info | Reported against EOL v3.1.6 with no comments and no developer follow-up; modern UCX builds support THREAD_MULTIPLE and the pml/ucx init path has changed significantly. Cannot reproduce as described; recommend pinging for a current-version retest and closing if no response. |
| [#8120](https://github.com/open-mpi/ompi/issues/8120) — shmem_alltoall32() segfaulted when test run on two nodes | OpenSHMEM shmem_alltoall32() segfaults in mca_spml_ucx_get during a barrier when run across two nodes with uneven rank placement (31+1), on POWER9 with Open MPI 4.0.3rc4 from Mellanox HPC-X. | EOL versions only | ❓ Needs info | A maintainer could not reproduce on x86 across v4.0.x/v4.1.x/v5.x in 2022 and asked the reporter to retest with v4.0.7; no further response. Original build was an EOL rc from a vendor stack; recommend pinging and closing if no reproduction on a supported version. |
| [#8266](https://github.com/open-mpi/ompi/issues/8266) — Apparent bug in MPI_File_seek() using MPI_SEEK_END | MPI_File_seek with MPI_SEEK_END on an ompio file lands at a fixed offset (0x400000) instead of the file end, possibly tied to mca_io_ompio_file_get_eof_offset and the file view. | unknown (v4.0.x/v4.1.x reported; possibly v5.0.x/v6.0.x/main) | ❓ Needs info | Maintainers flagged it as deserving investigation but never confirmed whether it is an ompio bug or a missing-file-view/usage issue, and no fix was identified; reported on 4.0.x/4.1.x (EOL) but the eof-offset logic may persist in ompio on newer branches. Recommend Needs info: request a clean reproducer/file-view confirmation, close if no response. |
| [#8350](https://github.com/open-mpi/ompi/issues/8350) — Crash with Open MPI 4.1.0 on macOS, but not 4.0.5: Symbol not found: _preadv | On macOS with a hand-built GCC 10 stack, a model crashed at runtime with 'Symbol not found: _preadv' in the ompio fbtl posix component (and a static build then hit a spurious MPI_Comm_f2c-before-MPI_INIT abort); using romio321 worked around it. | unknown (reported v4.1.x macOS; ompio may still be involved on newer branches) | ❓ Needs info | Suspected to be a gcc/clang module/library-path mixup specific to the user's hand-built toolchain rather than a clear ompio defect; maintainers could not reproduce and the reporter went quiet (later activity only in 2025). Recommend Needs info: ask whether it reproduces on a supported branch/clean toolchain, close if not. |
| [#8467](https://github.com/open-mpi/ompi/issues/8467) — Compiling with AOCC (Clang-9) | Building Open MPI 4.1.0 with the AMD AOCC (clang-9/ftn) compilers on a Cray EX system fails in libtool linking of the Fortran usempi_ignore_tkr shared library (-soname/-Wl handling). | EOL versions only (v4.1.x; AOCC/Cray-specific build env) | ❓ Needs info | Appears to be a libtool/AOCC-on-Cray toolchain integration problem (missing -Wl wrapping) that maintainers could not reproduce with clang+flang and is tied to the user's Cray PE module setup and EOL 4.1.0. Recommend Needs info: confirm against a current branch/libtool, close if not reproducible. |
| [#8573](https://github.com/open-mpi/ompi/issues/8573) — configure script strips out multiple -isystem flags from CFLAGS and CXXFLAGS | configure reportedly drops the -isystem token but keeps the directory when multiple -isystem flags are passed in CFLAGS/CXXFLAGS, causing a configure failure (similar to old issue #324). | unknown | ❓ Needs info | No -isystem-specific handling exists in the config m4, and the only maintainer reply could not reproduce it (works with -isysroot); the reporter (a vcpkg-patched build) never followed up, so the report is unconfirmed and should be pinged and closed if no response. |
| [#8754](https://github.com/open-mpi/ompi/issues/8754) — One-sided bandwidth drop of performance for non-align data | Reported one-sided (RMA) bandwidth degradation for non-aligned data on a 5.1.0a1 master build on Summit (CUDA/UCX). | unknown | ❓ Needs info | The issue has no comments and no reproducer details beyond the ompi_info dump; it was filed against a 2021 dev master and never triaged, so it cannot be verified and should be pinged for a current reproducer (close if none). |
| [#9599](https://github.com/open-mpi/ompi/issues/9599) — mca_pml_ucx_preq_completion: Assertion `preq->tmp_req != ((void *)0)' failed | Random crash/assertion failure in pml/ucx persistent request completion (preq->tmp_req==NULL) during MPI_Wait/MPI_Allreduce on v4.1.x with UCX over ud_verbs; appears to be a race condition with no shared-memory transport. | EOL versions only | ❓ Needs info | Reporter could never produce a standalone reproducer and the UCX team needs one; reported against v4.1.x (now EOL) and never confirmed on v5.0/v6.0/main. Recommend pinging for a reproducer on a current release and closing if none. |
| [#9624](https://github.com/open-mpi/ompi/issues/9624) — segfault in vmc_bcast_multiroot when running OpenFoam | Segfault inside HCOLL (vmc_bcast_multiroot, via mca_coll_hcoll_allreduce) running OpenFOAM with HPC-X 2.9; goes away with coll_hcoll_enable 0. Likely an HCOLL/MOFED firmware issue, not Open MPI itself. | unknown | ❓ Needs info | The crash is in NVIDIA's closed-source HCOLL library, not Open MPI code; reporter suspected firmware mismatch and planned to retest after upgrades but never confirmed, and crashes became non-reproducible. Recommend pinging for status and closing as an external-library issue if no update. |
| [#9761](https://github.com/open-mpi/ompi/issues/9761) — Problems with collectives (HCOLL and `coll_hcoll_enable 0`) | Multiple HCOLL/UCX collective failures (Bus errors, segfaults in reduce-scatter, alltoall hangs/perf issues) on an HDR cluster with MOFED 5.4 and HPC-X 2.9/2.10 running OSU benchmarks; reporter later traced Bus errors to NUMA node-0 memory exhaustion with DC transport. | unknown | ❓ Needs info | Failures are predominantly in NVIDIA's HCOLL/UCX libraries (one case fixed in HPC-X 2.10) and the remaining Bus errors traced to a UCX/DC memory-allocation issue, not Open MPI code; reported on EOL v4.1.1 with no Open MPI fix. Recommend pinging/closing as an external-library and environment issue. |
| [#9957](https://github.com/open-mpi/ompi/issues/9957) — OpenMPI + UCX fails in MPI_Win_post and works with MPI_fence | PSCW one-sided synchronization (MPI_Win_post) crashes in a UCX flush-state assertion (ucp_ep.inl:231) on InfiniBand with osc/ucx, while fence-based RMA works. | unknown; reported on v4.1.x (UCX) | ❓ Needs info | Likely a UCX-side bug (old rdma-core/UCX 1.11) that morphed into an MPI_Allgather truncation after driver/UCX updates; never reproduced with a standalone reproducer and stalled in 2022, with a 2025 me-too. Needs current-version retest before keeping open. |
| [#10026](https://github.com/open-mpi/ompi/issues/10026) — armci-mpi fails in multi-node execution with openmpi | armci-mpi/nwchem fail multi-node with MPI_ERR_WIN under the non-UCX OSC component, and osc/ucx works but is extremely slow. | v4.1.x (osc/pt2pt); largely obsolete on v5.0.x/main | ❓ Needs info | Reporter found OMPI_MCA_osc=ucx avoids the crash but performance is unusable; debugging stalled without a debug build. The failing osc/pt2pt component was removed after v4.x (replaced by osc/rdma), so this is largely moot on v5.0.x/main and needs retest on a current release. |
| [#10550](https://github.com/open-mpi/ompi/issues/10550) — Sigbus in UCX call inside opal_common_ucx_del_procs_nofence | SIGBUS during shutdown in opal_common_ucx_del_procs_nofence with pml/ucx at larger scale; UCX_TLS=^xpmem/^sm avoids the SIGBUS but then runs hang at finalize. | v5.0.x, main (UCX-dependent) | ❓ Needs info | The crash is inside UCX's uct_mm_ep_flush and the UCX maintainer pointed to a known UCX SIGBUS issue (binaries on certain filesystems); root cause appears to be UCX-side and unconfirmed against current Open MPI. Ping reporter to retest with current UCX/Open MPI; close if no response. |
| [#10559](https://github.com/open-mpi/ompi/issues/10559) — osc/ucx: MPI_Win_flush sometimes hangs on intra-node | Non-deterministic hang in MPI_Win_flush with osc/ucx on intra-node runs using MPI_Win_create plus a second window; does not reproduce with MPI_Win_allocate or a single window. | v5.0.x, main (UCX/HW-dependent) | ❓ Needs info | A maintainer could not reproduce the hang with v5.0rc7 and the reporter found v4.1.x did not hang, suggesting an environment/hardware-specific (older MLNX_OFED) osc/ucx issue that was never confirmed fixed. Ping reporter to retest on current v5.0.x/main; close if no response. |
| [#10573](https://github.com/open-mpi/ompi/issues/10573) — MPI-RMA - performance issues with `MPI_Get` | Severe MPI_Get RMA performance degradation scaling from one node to multiple nodes on a Cray (ugni) network with Open MPI 4.1.2 (IB-side issues were separately resolved). | v4.1.x (EOL); main status unknown | ❓ Needs info | The IB-side problems were resolved by site support and the remaining Cray/ugni concern is on EOL v4.1.x with no reproduction on current branches and only a single FYI comment since 2022. Ping to confirm whether it reproduces on current Open MPI; close if no response. |
| [#10639](https://github.com/open-mpi/ompi/issues/10639) — osc/ucx: internal error in MPI_Get (resource temporarily unavailable) | MPI_Get aborts with UCX ibv_exp_reg_mr 'Resource temporarily unavailable' memory-registration failure under specific large-array/block-size/alignment/interleave conditions across nodes with osc/ucx. | v5.0.x, main (UCX-dependent) | ❓ Needs info | The failure originates in UCX memory registration (ibv_exp_reg_mr) and was only FYI'd to the UCX team with no confirmed fix or reproduction on current branches; likely a UCX-side resource/registration limit. Ping reporter to retest with current UCX/Open MPI; close if no response. |
| [#10687](https://github.com/open-mpi/ompi/issues/10687) — UCX Error: failed to parse address: number of ep addresses exceeds 6 | Intermittent UCX endpoint-creation failures (failed to parse address) when running with MPI_THREAD_MULTIPLE and ~127 threads/node over UCX/InfiniBand; setting mpi_preconnect_mpi avoids it, suggesting a race in async modex/dynamic connection setup. | v4.1.x; likely v5.0.x/main | ❓ Needs info | Reported on v4.1.4 + UCX; developers requested a reproducer and ob1 comparison (last comment 2022) but reporter never responded, so it stalled awaiting info. Root cause was speculated to be an async-modex/connection race, plausibly still present on later branches; recommend pinging reporter and closing if no reproducer. |
| [#10759](https://github.com/open-mpi/ompi/issues/10759) — Collective CUDA operations have dangerous pointer operation that could corrupt memory | Reviewer flagged that coll/cuda reduce/scan/allreduce routines compute sbuf = sbuf1 - gap on a malloc'd buffer, which could overwrite memory before the buffer when gap > 0 for device-memory buffers. | main (coll/accelerator); all branches with the module | ❓ Needs info | bosilca argued the gap region is never actually accessed (intentional true-extent trick), so it may not be a real defect, but the reporter noted the GPU-buffer path may be untested; the same sbuf1 - gap pattern still exists in the renamed coll/accelerator module on main, so this needs a maintainer decision/verification rather than closing outright. |
| [#11065](https://github.com/open-mpi/ompi/issues/11065) — mmap failure on i686 during vader init | On 32-bit i686 (Fedora Rawhide, newer glibc) Open MPI 4.1.4's memory/patcher mmap interception issues a raw SYS_mmap syscall that returns EFAULT, breaking vader shared-memory init; the issue traced to memory_patcher_component.c's intercept_mmap. | v5.0.x, v6.0.x, main | ❓ Needs info | The patcher's syscall-based intercept_mmap path still exists on main; on i686 SYS_mmap2 (not SYS_mmap) is the correct call, so the bug likely persists wherever 32-bit + memory patcher is built. Niche 32-bit platform, stale since 2022; recommend pinging for confirmation on a current release, close if no interest. |
| [#11193](https://github.com/open-mpi/ompi/issues/11193) — Wrong received values when using 2 nodes and MPI_indexed datatype | MPI_Alltoallw (and equivalent Isend/Irecv) with indexed datatypes silently leaves some receive-buffer entries unwritten on a 2-node InfiniBand/UCX setup, but works on Ethernet and with older Open MPI 3.1.6. | unknown | ❓ Needs info | Maintainers could not reproduce on current master with recent UCX; bisection pointed to a regression between 4.0.5 and 4.0.6 (the user found 4.0.6 worked), and bosilca questioned the datatype construction in the reproducer. Inconclusive and likely fixed/UCX-related; tested versions are EOL. Recommend pinging reporter for confirmation, close if no response. |
| [#11235](https://github.com/open-mpi/ompi/issues/11235) — pmix error / libfabric call failed on Fedora i686 with 5.0.0-rc9 | On 32-bit Fedora i686 with 5.0.0-rc9, MPI_Init segfaults/aborts in the OFI MTL (fi_fabric) and later in btl select; a maintainer attributed it largely to a libfabric problem and a separate #11065 issue. | EOL versions only | ❓ Needs info | Reported against an old 5.0.0 release candidate on niche i686 hardware; a maintainer judged it a libfabric (not Open MPI) issue and the reporter intended to file upstream. No reproduction on current versions and the rc is long superseded; recommend pinging for confirmation on a current release and closing if stale. |
| [#11444](https://github.com/open-mpi/ompi/issues/11444) — MPI_Graph_map with no local nodes did not return MPI_UNDEFINED | An MPICH purified test reports that MPI_Graph_map for a singleton graph does not return MPI_UNDEFINED for non-root ranks on Open MPI 4.1.2. | code appears correct on v4.1.x, v5.0.x, main | ❓ Needs info | mca_topo_base_graph_map in v4.1.2, v4.1.x, v5.0.x, and main all return MPI_UNDEFINED when myrank>=nnodes (so rank 1 with nnodes=1 yields MPI_UNDEFINED); the current code looks correct and the report is uncorroborated, so ping reporter to re-verify and close if not reproducible. |
| [#11463](https://github.com/open-mpi/ompi/issues/11463) — Random SIGBUS error with xpmem on openmpi4.1.4 | Random SIGBUS crashes (apparently in MPI_Finalize) occur with pml/ob1 + btl/vader and xpmem on Open MPI 4.1.4/4.1.5, but not on 5.0.x. | v4.1.x; not reproduced on v5.0.x | ❓ Needs info | Reporter confirmed 5.0.0rc10 does not crash and the issue bisects to a 4.1.x-only change; possibly related to #9868. It is a 4.1.x (now near-EOL) defect with no root cause found and stalled since 2023; needs info / likely close as EOL-only unless a maintainer pursues it. |
| [#11873](https://github.com/open-mpi/ompi/issues/11873) — MTT: random/attr-error-code failures | An MTT nightly run on ppc64le with UCX 1.11.2 showed a segfault in opal_common_ucx_support_level during mca_pml_base_select at MPI_Init for the random/attr-error-code test. | EOL versions only (v5.0.x era, ppc64le/UCX 1.11) | ❓ Needs info | No comments, no reproduction outside the dated MTT run, and the crash is in old UCX 1.11.2 ppc64le path on a 2023 v5.0.x nightly; cannot confirm it still affects current main, so ping for current-version reproduction and close if none. |
| [#11874](https://github.com/open-mpi/ompi/issues/11874) — MTT: onesided/c_lock_illegal failures | An MTT run on ppc64le with the XL compiler and UCX 1.11.2 segfaulted in mca_pml_base_select during MPI_Init for the onesided/c_lock_illegal test. | EOL versions only (v5.0.x era, ppc64le/UCX 1.11) | ❓ Needs info | Like its sibling 11873, this is a 2023 MTT-only crash on old ppc64le/UCX 1.11.2 with no comments or current-version reproduction; request a repro on current main/UCX and close if none is provided. |
| [#12052](https://github.com/open-mpi/ompi/issues/12052) — v5.0.0: Default build on MacOS with OneAPI compilers fails | Build on macOS with Intel OneAPI classic icc fails compiling openpmix pmix_path.c with 'cannot open source file ... #include <sys/statfs.h>', though the include is properly macro-guarded. | v5.0.x and later (environment-specific; OneAPI classic only) | ❓ Needs info | The HAVE_SYS_STATFS_H guard is present in the snapshot, so the failure looks like an icc/XCode header-path quirk specific to the reporter's environment; maintainers could not reproduce and the reporter never supplied the complete all-internal configure+make logs. Recommend Needs info; the obsolete icc classic compiler makes it nearly moot, close if no response. |
| [#12053](https://github.com/open-mpi/ompi/issues/12053) — No output from mpirun parallel execution | User reports LAMMPS hangs/produces no output with mpirun -np 4 (works with -np 1) on Open MPI 4.1.5 from Arch packages. | unknown | ❓ Needs info | No reproducer, no logs, zero comments, and the report lacks any diagnostic detail; likely an application or local TCP/firewall issue. Recommend Needs info; close if no response. |
| [#12124](https://github.com/open-mpi/ompi/issues/12124) — [5.0.0] MPI communication peer process has unexpectedly disconnected | A long-running k8s/TCP training job aborts mid-run with 'An error occurred in Socket closed' / 'peer process has unexpectedly disconnected' on a NULL communicator under Open MPI 5.0.0. | unknown | ❓ Needs info | Symptom strongly suggests an underlying peer process crash or network/k8s disruption rather than an Open MPI defect; maintainer asked whether it is 5.0-specific and the reporter never replied (no comments since Nov 2023). Recommend Needs info; close if no response. |
| [#12146](https://github.com/open-mpi/ompi/issues/12146) — trouble running openmpi+pmix in rootless podman-hpc container | Open MPI+PMIx fails under srun (UNPACK-PMIX-VALUE UNSUPPORTED TYPE / OUT-OF-RESOURCE) at NERSC, both in and out of rootless podman containers, while mpirun works. | v4.1.x, v5.0.x (environment-specific NERSC/Slurm PMIx) | ❓ Needs info | Long thread traced the errors to PMIx version confusion between Slurm's slurmd PMIx and Open MPI's PMIx (plus a NERSC shmem/TMPDIR issue); updating the system PMIx to 4.2.7 cleared the main PMIx error, leaving an environment-specific container/Slurm problem with no Open MPI-side fix pending and no activity since Jan 2024. Recommend Needs info; close as environment-specific/stale if no follow-up. |
| [#12240](https://github.com/open-mpi/ompi/issues/12240) — Cannot launch mpirun on raspberry pi from Mac (tcp_peer_recv_connect_ack: received unexpected process identifier) | Launching from a 64-bit ARM Mac (OMPI 4.1.5) onto a 32-bit ARM Raspberry Pi fails in the OOB TCP connect handshake with a mangled process identifier, suggesting a heterogeneous 32/64-bit word-length bug. | EOL versions only | ❓ Needs info | Discussion suspects a long-untested heterogeneous 32-vs-64-bit path in the v4.1.x ORTE OOB TCP layer; ORTE was replaced by PRRTE in v5.0+, and heterogeneous 32/64-bit launching is unsupported/untested, so this only affects EOL v4.x. Recommend Needs info / close given the EOL, niche, unreproduced configuration. |
| [#12265](https://github.com/open-mpi/ompi/issues/12265) — MPI apps segfault on Mac OSX (missing mca_common_monitoring symbol) | A vanilla configure/build on macOS produced an MPI binary missing the _mca_common_monitoring_output_stream_id symbol at runtime; a maintainer asserted the lib is linked via the monitoring components. | unknown | ❓ Needs info | Only two comments, never resolved; maintainer says libmca_common_monitoring is pulled in by the monitoring components, contradicting the report, and no reproduction confirmed. Stale since early 2024; ping reporter to confirm against current main, close if unreproducible. |
| [#12308](https://github.com/open-mpi/ompi/issues/12308) — mpi4py: Memory corruption with pack/unpack external and MPI_C_LONG_DOUBLE_COMPLEX on macOS arm64 | MPI_Pack_external/Unpack_external of MPI_C_LONG_DOUBLE_COMPLEX corrupts the heap on macOS arm64 (Open MPI 5.0.1 via Homebrew), aborting with malloc heap-corruption. | v5.0.x (macOS arm64); main unknown | ❓ Needs info | No comments, no investigation since Feb 2024, likely an external32 long-double-complex datatype packing bug specific to arm64 long double; needs reverification on current main/v5.0.x. Ping reporter / re-test, then close if gone. |
| [#12414](https://github.com/open-mpi/ompi/issues/12414) — Non-blocking point-to-point performance issue (MPI_Isend, MPI_Irecv) | User observes large, message-size-dependent latency spikes (~200ms) for a many-neighbor Isend/Irecv exchange across two nodes, not reproduced by single-node runs or OSU benchmarks. | unknown | ❓ Needs info | Never root-caused; maintainers offered diagnostic benchmarks but the thread went silent in 2024 and the behavior appears tied to a specific incast/communication pattern and network. Cannot verify against current main; recommend pinging reporter, close if no reproducer. |
| [#12448](https://github.com/open-mpi/ompi/issues/12448) — frozen ring_c sanity test on ubuntu 22.04 laptop, not cloud VM | ring_c hangs on a single laptop using the OFI/libfabric (cm PML) path while a near-identical cloud VM works; selecting ob1/vader avoids the hang. | unknown | ❓ Needs info | Diagnosis pointed to the OFI libfabric provider failing to initialize on a laptop with multiple (docker/virbr/wifi) interfaces, an environment/libfabric issue Open MPI does not recommend on a single node, not an Open MPI defect. Thread stalled in 2024; recommend pinging reporter, close if no response. |
| [#12746](https://github.com/open-mpi/ompi/issues/12746) — occasional deadlock on mpi_barrier; what to do? | User reports sporadic MPI_Barrier deadlocks over TCP under heavy network load/oversubscription with Open MPI 4.1.4, with one rank stuck in mca_pml_ob1_send and others in request wait. | v4.1.x (EOL); unknown on newer | ❓ Needs info | No reproducer and the report is against the now-EOL 4.1.4 with no maintainer diagnosis; not reproducible without more info. Recommend pinging the reporter to retest on a supported release (v5.0.x); close if no response. |
| [#12947](https://github.com/open-mpi/ompi/issues/12947) — MPI 5.0.x can't initialize on x86 host on heterogeneous system | On a heterogeneous x86-host/aarch64-DPU system, Open MPI 5.0.x hangs during MPI_Init on the x86 host (host-to-DPU launch) while 4.1.7 works; the hang follows a dmodx key exchange in PMIx/PRRTE fence handling. | v5.0.x, v6.0.x, main | ❓ Needs info | Diagnosis points to a key not being provided across the heterogeneous launch (rhc54 suspected version/key mismatch); no fix landed and the specialized heterogeneous DPU setup cannot be reproduced locally. Needs OMPI UCX/heterogeneous-launch expert input or confirmation the reporter still hits it; ping reporter and close if no further info. |
| [#12988](https://github.com/open-mpi/ompi/issues/12988) — Relocation of OpenMPI prefix using slurm doesn't work - wasn't this issue fixed? | Relocating an Open MPI 5.0.5 install (built on a headnode, repacked as RPM, installed to a different shared-drive prefix) fails multi-node launches; PMIx/PRRTE still reference the original build prefix despite OPAL_PREFIX/PMIX_PREFIX/PRTE_PREFIX and --prefix. | v5.0.x, v6.0.x, main | ❓ Needs info | Relocation handling lives in embedded PMIx/PRRTE; no comments or fix followed and the reporter's exact RPM-repack/diskless setup cannot be reproduced locally. Likely still valid but needs reproduction/triage details; ping reporter, close if no response. |
| [#13064](https://github.com/open-mpi/ompi/issues/13064) — UCX nonblocking accumulate leads to program freeze | With osc_ucx_enable_nonblocking_accumulate true, MPI_Accumulate hangs/aborts after a threshold amount of data over a UCX/InfiniBand window; reporter later saw UCX rc_verbs remote-access/QP errors with newer UCX. | v5.0.x; main unknown | ❓ Needs info | Behavior shifted from a hang to a UCX-level QP/RDMA error across UCX versions, suggesting a possible UCX or hardware interaction; no OMPI fix identified and reporter left it inconclusive. Recommend pinging the reporter for a current 5.0.x/latest-UCX retest before deciding. |

## Obsolete / moot (69)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#95](https://github.com/open-mpi/ompi/issues/95) — MPI-2 dynamic openib/rdmacm work request queue flush error | Intermittent WORK REQUEST FLUSHED error (and a rare segv in rdmacm_component_finalize) when running the C++ dynamics test over the openib BTL with rdmacm CPC. | EOL versions only | 🗑️ Close | The openib BTL and its rdmacm CPC have been removed from the tree (InfiniBand now goes through UCX), so this defect only ever affected long-EOL 1.4/1.6-era versions; recommend closing as obsolete. |
| [#98](https://github.com/open-mpi/ompi/issues/98) — #procs limit hardcoded in Intel tests and not checked | The Intel Fortran test MPI_Alltoallv_f hangs above the hardcoded MAX_RANKS limit with no runtime check; request to add a runtime size check and scale-down for large runs. | n/a | 🗑️ Close | This concerns the external Intel test suite (not part of the ompi repo), the limit was already bumped in r1760, and the item is unactionable here; recommend closing. |
| [#161](https://github.com/open-mpi/ompi/issues/161) — FCA - problem with MPI_Allgather(v) when sendbuf is MPI_IN_PLACE | With Mellanox FCA acceleration enabled, MPI_Allgather/Allgatherv ignored MPI_IN_PLACE and passed the stale sendcount (-1), corrupting results in Open MPI 1.6.x. | EOL versions only | 🗑️ Close | The coll/fca component no longer exists in the tree (FCA was removed years ago), and the report targeted the long-EOL v1.6 series. The defect is moot on supported releases; recommend closing. |
| [#163](https://github.com/open-mpi/ompi/issues/163) — Support make check with --with-libltdl=/usr | When building (as Fedora does) against the system libltdl via --with-libltdl=/usr, make check failed; a patch against 1.6.3 was attached. | EOL versions only | 🗑️ Close | Open MPI dropped the embedded/external libltdl dependency entirely (libtool ltdl is no longer used for DSO loading), so --with-libltdl no longer exists and the make check failure is moot. Recommend closing. |
| [#184](https://github.com/open-mpi/ompi/issues/184) — Fix NetBSD on AMD64 and i386 when g95 is in path | On NetBSD-6/AMD64, libtool's a.out-vs-ELF probe runs $FC (g95/f95) as a C compiler; g95 does not predefine __ELF__, so libtool mis-detects a.out and breaks Fortran linking. | unknown | 🗑️ Close | Root cause is an upstream libtool m4 bug and the reporter concluded no clean OMPI-side fix was feasible; g95 is long dead and a working gfortran is the documented workaround. Stale niche-platform issue against a defunct compiler; recommend closing. |
| [#193](https://github.com/open-mpi/ompi/issues/193) — coll/sm has memory leak(s) in v1.6 | A reproducer shows three memory leaks in the coll/sm shared-memory collective module on the v1.6 branch, including a circular-refcount leak between coll/sm and coll/sync. | EOL versions only | 🗑️ Close | The coll/sm component has been removed from the tree entirely, and the report targeted the long-EOL v1.6 series. The leaks are moot on supported releases; recommend closing. |
| [#196](https://github.com/open-mpi/ompi/issues/196) — coll/sm has memory leak(s) | Trunk-era follow-up to #4429 showing several memory leaks in the coll/sm shared-memory collective module, with a series of patches to fix them. | EOL versions only | 🗑️ Close | The coll/sm component no longer exists in the source tree, so these leaks are moot on all supported releases; the related rml_oob leak is also gone with the OOB rework. Recommend closing. |
| [#209](https://github.com/open-mpi/ompi/issues/209) — Memchecker - valgrind: the 'impossible' happened | On 1.9-trunk built with memchecker, valgrind crashed during MPI_Finalize; disabling the coll/ml component avoided the crash, pinning it to coll ml. | EOL versions only | 🗑️ Close | The coll/ml (and bcol) components no longer exist on main; the implicated code is gone and the trunk/v1.8 era is EOL, so this is moot. Recommend closing. |
| [#210](https://github.com/open-mpi/ompi/issues/210) — Some dynamic tests fail when coll ml is enabled | After the 2014 BTL-to-OPAL move, several ibm dynamic spawn tests segfaulted in bcol/basesmuma due to an opal_process_name_t vs orte_process_name_t mismatch when coll/ml was enabled. | EOL versions only | 🗑️ Close | The coll/ml and bcol/basesmuma components have been removed from main, so the crashing code no longer exists; the bug is obsolete. Recommend closing. |
| [#286](https://github.com/open-mpi/ompi/issues/286) — Cuda-aware OpenMPI fails on Jetson K1 | CUDA-aware MPI on a Jetson K1 (ARMv7, CUDA 6.5) emitted cuMemHostRegister error 801 because cudaHostRegister is unsupported on that ARM platform; a related x86 cuMemHostUnregister segfault was fixed in 1.8.6. | EOL versions only | 🗑️ Close | The 801 error was an unfixable CUDA-on-ARMv7 limitation on long-EOL hardware/toolkits, and the entire CUDA support path has since been rearchitected into the opal accelerator framework (mca/common/cuda removed). The report is obsolete. Recommend closing. |
| [#978](https://github.com/open-mpi/ompi/issues/978) — Default binding in heterogenous bindings is weird | Default bind-to-socket behavior misbehaved on hosts with non-homogeneous sockets; flagged in the v1.10/2.x ORTE era and repeatedly punted. | EOL versions only | 🗑️ Close | Reported against the old ORTE runtime (v1.x/2.x); process binding is now handled by PRRTE/hwloc and the original ORTE code is gone, so the report is almost certainly obsolete. Could not reproduce the original ORTE path on current main, so recommending close as moot. |
| [#1364](https://github.com/open-mpi/ompi/issues/1364) — EXC_BAD_ACCESS when calling MPI_Finalize after main() returns (OS X) | Calling the C++ MPI::Finalize() from a global object's destructor after main() returns segfaults on OS X because OPAL's exit destructor runs before the C++ static destructors. | EOL versions only | 🗑️ Close | Uses the C++ MPI:: bindings, which were removed from the MPI standard (MPI-3) and dropped from Open MPI long ago; the scenario can no longer occur on current versions. Obsolete/moot, recommend close. |
| [#1635](https://github.com/open-mpi/ompi/issues/1635) — romio build failure on OpenBSD-5.7 | Embedded ROMIO failed to build on OpenBSD 5.7 because struct statfs lacks f_type in ad_fstype.c. | fixed on all | 🗑️ Close | The embedded ROMIO io component has been removed from the tree entirely (only ompio remains under ompi/mca/io/), so the failing ad_fstype.c code no longer exists in Open MPI. Obsolete/moot; close. |
| [#2106](https://github.com/open-mpi/ompi/issues/2106) — v2.1.x: Open BSD 6.0 and patcher problems | On OpenBSD 6.0 the memory patcher's mprotect call is disallowed (W^X), breaking Open MPI; this was a duplicate of #2049 filed as a v2.1.0 blocker. | EOL versions only | 🗑️ Close | This targeted the long-EOL v2.0.x/v2.1.x branches; subsequent work added an OpenBSD README/config note (PR #2687) and patcher behavior has changed substantially since. The original v2.1.x blocker is moot, so close. |
| [#2430](https://github.com/open-mpi/ompi/issues/2430) — Configuration of openmpi's 2.0.0 romio results in pread and pwrite being added to libmpi | ROMIO's configure failed to detect pread/pwrite (HAVE_DECL_PWRITE) and provided its own non-thread-safe definitions, which were embedded in libmpi and could shadow glibc's symbols at link time. | v5.0.x and earlier (romio); fixed on main | 🗑️ Close | ROMIO has been removed from current main (only the ompio component remains), so the offending romio utils.c symbols no longer ship; the issue only affected branches that bundled romio (through v5.0.x). Close as moot on main. |
| [#2433](https://github.com/open-mpi/ompi/issues/2433) — NAG | NAG Fortran v5/v6 rejected the generated ompi/mpi/fortran/use-mpi-tkr/mpi_testall_f90.f90 in the 2.0.x era. | EOL versions only | 🗑️ Close | The entire use-mpi-tkr implementation and mpi_testall_f90.f90 no longer exist in the current tree (the old TKR 'use mpi' layer was replaced), so the reported compile failure is moot on supported branches; recommend Close. |
| [#2984](https://github.com/open-mpi/ompi/issues/2984) — Something odd about cuda.h configury checking | Reporter saw cuda.h usability/presence checks fail despite --with-cuda; suspected CPPFLAGS not being set for the configure tests. | n/a | 🗑️ Close | In-thread the failures were identified as the embedded hwloc configure (CUDA support was actually enabled), so it was a non-issue; embedded hwloc and the whole CUDA/configure setup have since changed completely, making this moot. Recommend Close. |
| [#3145](https://github.com/open-mpi/ompi/issues/3145) — Getting Segmentation fault when run OpenFoam with openib on aarch64 | Segfault in btl_openib_handle_incoming (corrupted hdr->tag, NULL cbfunc) running OpenFOAM over openib on aarch64. | EOL versions only | 🗑️ Close | The openib BTL has been removed entirely from the tree (replaced by UCX), so this openib-specific aarch64 crash cannot occur on supported branches; recommend Close. |
| [#4610](https://github.com/open-mpi/ompi/issues/4610) — [v2.1.1] Crash at opal_convertor_generic_simple_position while sending/receiving GPU memory with multi-threads at comm_dup channel | Multi-threaded (THREAD_MULTIPLE) send/recv of CUDA GPU memory over the smcuda/tcp BTLs crashes after many iterations, apparently due to fragmented-receive convertor position handling not being thread-safe. | EOL versions only | 🗑️ Close | Reported against v2.1.1/3.0.0 using the smcuda BTL, which (along with the entire legacy CUDA/smcuda fragmentation path) has been superseded; modern GPU support goes through accelerator framework and OFI/UCX, and smcuda BTL is gone or deprecated on current main. Reproduction relies on EOL components and went stale; recommend closing as moot (could ask reporter to retest if still relevant). |
| [#5206](https://github.com/open-mpi/ompi/issues/5206) — Java bindings vs mpool/memkind | Java MPI tests crash at exit because libmemkind's pthread_key destructor runs after libmemkind.so is unmapped; workaround is --mca mpool ^memkind or --disable-dlopen. | EOL versions only (Java bindings removed from main) | 🗑️ Close | The crash was specific to the Java bindings interacting with mpool/memkind, and the Java bindings have been removed from the tree (recent remove-java merge), so the scenario can no longer occur on current main. Recommend closing as moot. |
| [#5478](https://github.com/open-mpi/ompi/issues/5478) — Segfault in MPI_Barrier | Non-deterministic crash / udcm 'could not find endpoint' errors when launching via SLURM srun on InfiniBand with the openib BTL; mpirun launch avoided it. | EOL versions only (openib BTL removed) | 🗑️ Close | The failure path is entirely in the openib BTL's UD connection manager (btl_openib_connect_udcm.c), and the openib BTL has been removed (use UCX on IB now), so this code no longer exists on main. Reported on EOL 2.1.x/3.0.x; recommend closing as obsolete. |
| [#5839](https://github.com/open-mpi/ompi/issues/5839) — Compilation against HCOLL<=3.0 fails | Open MPI fails to build against HCOLL <= 3.0 (MOFED 2.2) because coll/hcoll references collectives (coll_allgatherv, coll_alltoallv, coll_gatherv, coll_igatherv) that only exist in HCOLL >= 3.1; reporter proposed HCOLL_VERSION guards. | EOL versions only (hcoll component removed) | 🗑️ Close | The coll/hcoll component no longer exists in the tree (ompi/mca/coll/hcoll is absent on main), so this build failure cannot occur in current Open MPI, and HCOLL 3.0/MOFED 2.2 is long obsolete. Recommend closing as moot. |
| [#5946](https://github.com/open-mpi/ompi/issues/5946) — Running osu_acc_latency errors out with 3.1.2 | osu_acc_latency segfaults via openib/osc_rdma, hangs with osc ucx, and aborts inter-node with UCX endpoint/iallreduce errors on Open MPI 3.1.2 over InfiniBand. | EOL versions only (openib removed; 3.1.x EOL) | 🗑️ Close | The crashing path is in the openib BTL (mca_btl_openib_get) and osc/rdma against very old UCX on the EOL 3.1.x series; openib has since been removed and one-sided over IB now uses UCX, so the specific failures no longer apply to current Open MPI. Recommend closing as obsolete. |
| [#6060](https://github.com/open-mpi/ompi/issues/6060) — v3.1.3 Non-fatal "make all" warnings | Building Open MPI 3.1.3 on macOS 10.13 produces numerous non-fatal compiler/ranlib warnings (pragma-pack from net/if.h, va_start with bool, deprecated syscall in memory_patcher, empty .a table of contents, basename const qualifier). | EOL versions only | 🗑️ Close | Filed against EOL v3.1.3; most cited code lives in the long-removed embedded pmix2x and other since-rewritten files, and macOS toolchains/SDKs have changed greatly since 2018. No comments, stale; recommend closing as obsolete though some categories of macOS build warnings may persist. |
| [#6143](https://github.com/open-mpi/ompi/issues/6143) — When using libtool to compile against Open MPI, libopen-pal and libopen-rte are explicitly linked in | Libtool .la files cause downstream packages to explicitly link libopen-rte and libopen-pal (whose ABI is not stable), risking breakage across Open MPI versions. | v4.0.x/v4.1.x and earlier; moot on v5.0.x/v6.0.x | 🗑️ Close | libopen-rte no longer exists on v5.0.x/main (ORTE was replaced by the external PRRTE), removing the central concern; the proposed fixes all had drawbacks and many sites simply delete the .la files. Largely moot on modern branches; recommend closing. |
| [#6231](https://github.com/open-mpi/ompi/issues/6231) — libevent warnings with MPI_THREAD_MULTIPLE still occurring in head of v3.0.x release | Harmless 'event_base_loop: reentrant invocation' libevent warnings emitted by hybrid OpenMP+MPI codes using MPI_THREAD_MULTIPLE on v3.0.x. | EOL versions only | 🗑️ Close | Filed against EOL v3.0.x; the embedded libevent/runtime stack has been substantially reworked (PRRTE, newer event handling) since 2018 and the warnings were already described as harmless. No comments, stale; recommend closing as obsolete. |
| [#6242](https://github.com/open-mpi/ompi/issues/6242) — Memory Leaks in pmix4x & mpool | AddressSanitizer/valgrind report small one-time memory leaks at MPI_Init/Finalize in the embedded pmix4x (pmix_control.c) and in the hugepage mpool component. | EOL versions only for pmix4x; hugepage mpool on all | 🗑️ Close | The embedded pmix4x copy no longer exists (PMIx is now a separate submodule maintained upstream), making the PMIx half moot; the hugepage mpool still exists but the cited leaks are small one-time init allocations. Stale since 2019; recommend closing, refiling any current leak against the live tree if still observed. |
| [#6243](https://github.com/open-mpi/ompi/issues/6243) — PGF90-S-0081-Illegal selector with PGI 18.10 and OpenMPI master (v4.0.x) | Building Open MPI master (~v4.0.x era) with PGI 18.10 fails, including Fortran KIND errors and memkind.h redefinition errors in mpool/memkind; building with gfortran then swapping wrapper compilers is unsupported. | EOL versions only | 🗑️ Close | Filed against a 2019 master snapshot with the legacy PGI compiler (since superseded by NVHPC); maintainer gave the --with-memkind=no workaround and the Fortran issue stemmed from an unsupported mixed-compiler build. Stale and tied to obsolete compiler/version; recommend closing. |
| [#6829](https://github.com/open-mpi/ompi/issues/6829) — Error running HPL on Cori | HPL on Cray Aries (UGNI) with 256 ranks aborts with btl_ugni descriptor errors and a pml_ob1 assertion failure (sendreq->rdma_frag) mid-run. | EOL versions only | 🗑️ Close | This is specific to the btl/ugni Cray Gemini/Aries transport, which has been removed from Open MPI (Cray support dropped in 5.0); the system (Cori) is also decommissioned. The bug cannot affect current supported branches; close as obsolete. |
| [#6960](https://github.com/open-mpi/ompi/issues/6960) — IPv4 vs. IPv6 issues | On an IPv4-only system, an Open MPI build configured with --enable-ipv6 aborts at startup with an OOB/TCP 'Address family not supported' / 'No network interfaces' error. A partial fix was cherry-picked to v3.1.x/v4.0.x but 4.0.2rc2 still failed, so it was reopened. | EOL versions only | 🗑️ Close | The failing code lived in ORTE's oob_tcp_listener.c; ORTE was entirely replaced by PRRTE in v5.0+ (no orte/ tree exists on main), so this bug cannot persist on supported v5.0.x/v6.0.x/main. Affected branches (v3.1.x, v4.0.x) are EOL, so recommend closing. |
| [#6982](https://github.com/open-mpi/ompi/issues/6982) — User's PMIx call fails after MPI_Init + UCX/HCOLL | A user PMIx_Get for a key Put after an intervening PMIx_Fence returns error -46 when running multi-node, traced to the ORTE PMIx server returning 'not found' instead of caching the request until the data arrives. PRRTE/PMIx master was shown to behave correctly. | EOL versions only | 🗑️ Close | rhc54 demonstrated the bug was in ORTE's PMIx server code path and that PRRTE (which replaced ORTE in Open MPI v5.0+) handles it correctly; ORTE no longer exists on main. The affected v4.0.x/master(ORTE) lines are EOL, so the issue is moot on supported branches; close. |
| [#7044](https://github.com/open-mpi/ompi/issues/7044) — v3.1.x hangs at startup when openib BTL provider is active since commit 8be0a233c87d | On v3.1.x, mpirun hangs at startup during BTL/openib initialization (stuck after 'Checking distance from this process to device') since a specific bisected commit, on IB hardware. | EOL versions only | 🗑️ Close | This is a v3.1.x-specific hang in the deprecated/removed BTL openib component, which was removed in favor of UCX and is gone in v5.0+/v6.0+. v3.1.x is long EOL and got no developer follow-up; the affected code no longer exists on supported branches, so close as obsolete. |
| [#7301](https://github.com/open-mpi/ompi/issues/7301) — Broken collectives on Cray when built with Intel compiler | On a Cray XC40 with the Intel compiler, collectives (Bcast, Comm_split) hang due to C11 atomics; disabling C11 atomics or using GCC works around it. | EOL versions only | 🗑️ Close | Root cause was buggy Intel C11 atomics on Cray XC40/ugni hardware (an EOL platform/BTL); the affected master is now several major releases old and ugni is gone, so this is moot and should be closed. |
| [#7354](https://github.com/open-mpi/ompi/issues/7354) — Compilation warnings with GCC 4.8.5 on RHEL 7.5 | Numerous compiler warnings (sign-compare, _Static_assert, #ident extension, unused vars) building 2020-era master with the old GCC 4.8.5 on RHEL 7.5 aarch64. | EOL versions only | 🗑️ Close | Much of the warned code no longer exists (ORTE removed, vader renamed to sm) and GCC 4.8.5 predates the now-required C11 toolchain; warnings against 2020 master are obsolete, so close. |
| [#7469](https://github.com/open-mpi/ompi/issues/7469) — psm2 support causes mtl/ofi, btl/ofi conflict | Using PSM2 both through libfabric (ofi MTL/BTL) and directly conflicts because PSM2_finalize cannot be called more than once. | EOL versions only | 🗑️ Close | An old (2020, master-era) PSM2/libfabric interaction issue with no activity; PSM2 hardware is effectively retired and modern OFI/PSM2 paths differ. Likely moot on current branches; close as obsolete unless a current user reports it. |
| [#7526](https://github.com/open-mpi/ompi/issues/7526) — Disable cuda coll parameter doesn't work | The MCA parameter coll_cuda_disable_cuda_coll is exposed by ompi_info but is not actually referenced in the code, so it has no effect. | EOL versions only | 🗑️ Close | Reported against v4.0.3; the coll/cuda component has since been replaced by coll/accelerator on main (no coll_cuda_* references remain), so the dead parameter is gone. Close as obsolete. |
| [#7568](https://github.com/open-mpi/ompi/issues/7568) — osu_acc_latency stalls on opempi 3.1.x branch | osu_acc_latency over InfiniBand (openib BTL, osc/rdma) hangs at larger message sizes on Open MPI 3.1.4/3.1.6. | EOL versions only | 🗑️ Close | Reported on the EOL 3.x series; maintainers attribute it to an osc/rdma fix not backported to 3.1.x and recommend 4.0.x/UCX. The openib BTL has since been removed from main, so the path is gone on current branches. Close as obsolete. |
| [#7988](https://github.com/open-mpi/ompi/issues/7988) — OpenMPI 4.0.1 cannot build using PMIx 3.1.5 | Building Open MPI 4.0.x against an external PMIx 3.1.5 fails in the opal/mca/pmix/ext3x glue with 'pmix_server.h: No such file or directory' due to an include-path bug in the glue code. | EOL versions only | 🗑️ Close | Bug was in the ext3x external-PMIx glue, which only exists in v4.0.x/v4.1.x; v5+ embeds PMIx and has no ext3x (opal/mca/pmix on main has no ext3x dir). rhc54 stated the glue fix was committed and v5 only supports PMIx. Moot on supported branches; close as obsolete. |
| [#8061](https://github.com/open-mpi/ompi/issues/8061) — ORTE linker issue with test/monitoring and test/spc observed the Mentor CodeBench GNU compilers | Building Open MPI v4.0.x with Mentor CodeBench GNU 9.3 compilers on Ubuntu failed to link test/monitoring, test/spc, and ompi/debuggers because ORTE libs were not pulled in by the Makefiles. | EOL versions only | 🗑️ Close | A maintainer noted this is expected automake behavior, and it only ever reproduced with one exotic compiler on Ubuntu (worked on CentOS); ORTE was removed entirely in v5.0+ (replaced by PRRTE), so the underlying ORTE-linking concern is moot on supported branches. Recommend closing. |
| [#8106](https://github.com/open-mpi/ompi/issues/8106) — oshmem_info link failure for 4.0.x and 4.1.x | Configuring v4.0.x/v4.1.x with --with-ompi-pmix-rte disables ORTE, after which liboshmem still references orte symbols and oshmem_info fails to link. | EOL versions only | 🗑️ Close | Root cause is the v4.x-era --with-ompi-pmix-rte option turning off ORTE while oshmem still depended on it; fix PRs were closed over release-branch side effects. ORTE and the --with-ompi-pmix-rte option no longer exist on v5.0+/main, so this is moot on all supported branches. Recommend closing. |
| [#8166](https://github.com/open-mpi/ompi/issues/8166) — Minor memory leak in init | Valgrind reports small definitely-lost blocks during MPI_Init originating in the embedded PMIx (pmix3x) dstore/PMIx_Get path; reporter proposed freeing ival and rhc54 offered an alternate patch. | EOL versions only | 🗑️ Close | The leak is in the v4.0.x-era embedded pmix3x component, which no longer exists (Open MPI now uses an external/3rd-party OpenPMIx via submodule); any remaining fix belongs upstream in OpenPMIx, not this tree. Obsolete on supported branches; recommend closing. |
| [#8268](https://github.com/open-mpi/ompi/issues/8268) — orted crash with v4.0.5 and BUILTIN_GCC (GCC v7.3.1) | Non-deterministic orted segfault on AWS Graviton2/ARM where cls_constructor_array is NULL, only with GCC 7.3.1 builtin atomics; disabling builtin atomics or using newer GCC avoids it. | EOL versions only (v4.0.x; ORTE-based) | 🗑️ Close | Crash is in ORTE (libopen-rte), which was removed when v5.x moved to PRRTE, and it depends on an old GCC 7.3.1 builtin-atomics path; the reporter never confirmed a root cause and it only affects EOL v4.0.x. Recommend closing as moot on supported branches. |
| [#8290](https://github.com/open-mpi/ompi/issues/8290) — Memory leak with openib component when using MPI_AllToAllW | MPI_Alltoallw with subarray datatypes leaks memory when using the openib BTL; a Gilles workaround patch hid the leak but the root cause was never found. | EOL versions only (v4.0.x/v4.1.x via openib) | 🗑️ Close | Maintainers note the issue is specific to openib, which was purged from main starting with v5.0.x (confirmed: opal/mca/btl/openib does not exist on main), so it only affects EOL v4.0.x/v4.1.x. Recommend closing as obsolete since the offending component is gone on supported branches. |
| [#8596](https://github.com/open-mpi/ompi/issues/8596) — 4.1.0: mpirun crashes when trying to schedule a task on a foreign host | mpirun --host <remote> crashes with ORTE_ERROR_LOG on Debian's Open MPI 4.1.0 built against external PMIx 4.0.0, due to the pmix3x ext3x glue failing to convert the new pmix.topo2 key. | EOL versions only | 🗑️ Close | Root cause was a known external-PMIx-4.0.0 incompatibility in the v4.1.0 ext3x component; a working workaround patch was confirmed and 4.1.1 was recommended, and the entire ORTE/ext3x stack was replaced in v5.0.x, so the issue is moot on supported branches (affects only EOL 4.1.x). |
| [#8820](https://github.com/open-mpi/ompi/issues/8820) — Building of openmpi rpm hits issue with python scripts in 3rd-party modules | RPM %install fails with 'ambiguous python shebang' on the bundled libevent's event_rpcgen.py (#!/usr/bin/env python) when building Open MPI 5.0.0a1 with internal libevent. | v5.0.x (EOL pre-release) | 🗑️ Close | This is a shebang in the embedded libevent (an upstream-bundled 3rd-party file) tripping a distro RPM brp-python check; the reporter found a workaround (use system libevent-devel) and the bundled libevent has since been updated, so the original 2021 pre-release report is effectively moot and should be closed. |
| [#9004](https://github.com/open-mpi/ompi/issues/9004) — MPI Allreduce test stops on RHEL8.2 | Allreduce hangs at 131072 bytes; root cause was identified as the coll/hcoll component (NVIDIA libhcoll), with workarounds -mca coll ^hcoll or -x HCOLL_ALLREDUCE_ZCOPY_TUNE=static. | not Open MPI (HPC-X/libhcoll) | 🗑️ Close | Diagnosed as an external libhcoll bug (NVIDIA) with a fix slated for HPC-X 2.9; not an Open MPI defect, and hcoll has since been removed from Open MPI on main. Resolved/obsolete; recommend closing. |
| [#9030](https://github.com/open-mpi/ompi/issues/9030) — OPAL ERROR: Unreachable in file pmix3x_client.c at line 112 | MPI_Init fails with 'PMIX ERROR ... gds_ds12_lock_pthread.c' and 'OPAL ERROR: Unreachable in pmix3x_client.c' when running two procs inside a PyTorch Docker image with Open MPI 4.0.5. | EOL versions only (v4.0.x) | 🗑️ Close | The pmix3x shim/component no longer exists on main (Open MPI 5.0+ uses external/embedded PMIx 4.x via pmix-internal.h); no comments, likely a container/PMIx-version mismatch on an EOL release. Obsolete on current versions; recommend closing. |
| [#9123](https://github.com/open-mpi/ompi/issues/9123) — Crash in Open MPI v4.1.x in MPI_Win_lock_all when used with libfabric < 1.12 | MPI_Win_lock_all crashes in osc/rdma over btl/ofi with libfabric 1.11 on Mellanox IB because OFI components are selected over UCX by default; root cause is a libfabric 1.11 bug fixed in 1.12. | v4.1.x (EOL); much improved on v5.0.x/main | 🗑️ Close | Crash itself is an old-libfabric bug (works with libfabric >= 1.12); bwbarrett retargeted to v5.0 noting OSC/UCX selection is much better there and the complex fix would not be backported to 4.1.x. v4.1.x is now EOL and selection behavior improved on v5.0.x/main, so recommend closing. |
| [#9171](https://github.com/open-mpi/ompi/issues/9171) — orte-clean broken in v4.0.6 and v4.1.1 | orte-clean/ompi-clean fails with 'opal_pmix.init failed / Unreachable' because the tool cannot connect to an Open MPI daemon (ORTE-era runtime). | v4.0.x/v4.1.x (EOL) only; not applicable to v5.0.x+ | 🗑️ Close | ORTE was removed in v5.0 (no orte dir or ext3x_client.c on main); the runtime is now PRRTE and orte-clean no longer exists. The bug only affected EOL v4.0.x/v4.1.x, so it is moot on supported branches; recommend closing. |
| [#9390](https://github.com/open-mpi/ompi/issues/9390) — portals4: fix compiler warnings in all the Portals4 components | Compiler warnings (loop variables, atomics, debug-only) appeared in the Portals4 components after upgrading to gcc 10.2.1. The master PR was filed but the issue tracked back-porting to v4.0.x/v4.1.x where it did not cherry-pick cleanly. | EOL versions only | 🗑️ Close | The master fix landed; the issue was left open only to track back-port to v4.0.x and v4.1.x, both now EOL, so the remaining work is moot. Recommend closing. |
| [#9402](https://github.com/open-mpi/ompi/issues/9402) — external32 test fails with NVHPC due to misplaced static array | The test/datatype/external32 test fails when built with NVHPC/nvc because the statically-initialized opal_datatype_local_sizes array is shifted, causing wrong endianness conversion. | EOL versions only | 🗑️ Close | Root cause was confirmed by NVIDIA to be an nvc compiler bug (statically-initialized arrays), not an Open MPI defect, and was reported upstream; affected NVHPC versions and the v4.1.x line are obsolete. Recommend closing as a compiler bug outside Open MPI's scope. |
| [#9860](https://github.com/open-mpi/ompi/issues/9860) — A few minor configure errors | Configuring main/5.0.0rc emitted stderr noise (rm conftest.dSYM directories) and an MPL/ROMIO configure syntax error causing the embedded ROMIO distribution to fail to configure. | moot on main (romio321/MPL removed) | 🗑️ Close | The MPL/ROMIO failure was inside the embedded romio321 ROMIO tree, which no longer exists on main (only ompio remains in ompi/mca/io), so the ROMIO configure error is moot; the remaining cosmetic rm/dSYM noise is trivial and the issue is effectively obsolete. |
| [#10158](https://github.com/open-mpi/ompi/issues/10158) — Java Segmentation Fault | MPI Java bindings segfault during String operations (e.g., log4j init) when running a Java MPI program on HPC clusters under Open MPI 4.1.2. | v4.x only; Java removed from v5.0.x/main | 🗑️ Close | The Java bindings have been removed from Open MPI (ompi/mpi/java no longer exists on main), so the feature this bug concerns is gone on current/supported releases; the issue is moot. Close. |
| [#10223](https://github.com/open-mpi/ompi/issues/10223) — Java Segmentation Fault in ping-pong-mpi-tcp | MPI Java bindings segfault in JIT-compiled String operations under UCX (and even btl/tcp) on HPC clusters; a workaround was disabling the JVM JIT, and the reporter ultimately abandoned Java. | v4.x only; Java removed from v5.0.x/main | 🗑️ Close | Related to #10158; the Java bindings have been removed from Open MPI (no ompi/mpi/java on main), so the affected feature no longer exists on current/supported releases. The interaction was never root-caused but is now moot. Close. |
| [#10245](https://github.com/open-mpi/ompi/issues/10245) — Default mpirun behavior for Java apps incorrect if PRTE libdir != OMPI libdir | Regression vs v4.x where mpirun did not add the correct libdir to the Java classpath because PRTE's libdir could differ from OMPI's. | fixed on all / moot (Java removed) | 🗑️ Close | PR 10246 (merged 2022-05-17) addressed classpath cleanups, and Java bindings have since been removed entirely from main (no ompi/mpi/java directory), making this moot; close. |
| [#10307](https://github.com/open-mpi/ompi/issues/10307) — OMPI v4.x fails when built against PMIx v5 | OMPI v4.x apps fail under srun/mpirun with PMIx v5 (psec/munge component not found); embedded PMIx v3 errors appear even when configured against external PMIx v5. | v4.0.x, v4.1.x (both EOL) | 🗑️ Close | Issue is specific to the now-EOL OMPI v4.0.x/v4.1.x series (v5/main worked from the start); the open external-vs-internal PMIx question concerns only v4.x code that is no longer maintained; close as obsolete. |
| [#10313](https://github.com/open-mpi/ompi/issues/10313) — Segfault in MPI_Finalize if MPI_Barrier isn't called first | Intermittent segfault in libibverbs ibv_create_comp_channel via the btl/openib component during MPI_Finalize on v4.1.x; --mca btl ^openib avoids it. | v4.0.x, v4.1.x (EOL); fixed on v5.0.x/main (openib removed) | 🗑️ Close | The crash is in the legacy btl/openib component, which is removed on main/v5.0.x (no opal/mca/btl/openib), so it cannot recur there; only EOL 4.x is affected; close. |
| [#10389](https://github.com/open-mpi/ompi/issues/10389) — Return error from node failure | User reports node/network failure during MPI_Get with ULFM is not returned as an error and the job hangs, plus a missing 'node-died' help message. | v5.0.x rc-era; ULF/RMA experimental | 🗑️ Close | Reported against v5.0.0rc7; the missing help-message root cause was fixed upstream in PRRTE (master and release branches per rhc54), and the ULFM+RMA limitation is a known experimental-support caveat rather than a defect. Recommend closing as the concrete bug (missing help text) is fixed and the rest is expected experimental behavior. |
| [#10418](https://github.com/open-mpi/ompi/issues/10418) — Possible heap-use-after-free reported by AddressSanitizer inside PMPI_Init call | AddressSanitizer reports a heap-use-after-free in opal_pmix_base_partial_commit_packed during MPI_Init when using the s2/pmi2 path on Open MPI 4.1.3. | v4.1.x (EOL versions only) | 🗑️ Close | The reporter notes the offending function and the entire s2 pmix component were removed (PR 7202) for 5.0; the legacy pmi2 path does not exist on main. The bug is confined to the now-removed 4.1.x s2 code, so recommend closing as obsolete. |
| [#10453](https://github.com/open-mpi/ompi/issues/10453) — ompi 4.1.4 fails to compile in oshmem/mca/spml/ikrit/spml_ikrit.c: too few arguments to memheap_find_segnum | Build fails in the oshmem ikrit SPML component because memheap_find_segnum was given one argument but is declared with two, triggered by configuring with deprecated --with-mxm. | v4.1.x (EOL versions only) | 🗑️ Close | The ikrit SPML component depends on deprecated mxm and was told to be dropped from configure; on main oshmem/mca/spml/ contains only base and ucx (ikrit removed), so the error cannot occur on v5.0+. Workaround (remove --with-mxm) given; recommend closing as obsolete. |
| [#10718](https://github.com/open-mpi/ompi/issues/10718) — HCOLL fails to save coll handlers and ends in Segfault when used with HAN | Using coll/hcoll as a HAN backend triggers mca_coll_hcoll_save_coll_handlers failures and a segfault in hcoll_context_free at MPI_Finalize, on main and 5.0.x at the time. | v5.0.x only (hcoll removed on main/v6.0.x) | 🗑️ Close | The coll/hcoll component was removed entirely from main and v6.0.x (commits 4920f9e/87335ba) and only remains in v5.0.x; the proprietary hcoll layer is the segfaulting code, so the defect is now moot on current main and recommend closing (the analogous UCC issue 9885 remains separately tracked). |
| [#11088](https://github.com/open-mpi/ompi/issues/11088) — 3rd-party/romio cannot be built with RHEL7 GNU compilers from Open MPI v5 | In Open MPI v5.0, the embedded ROMIO requires stdatomic.h (GCC >= 4.9) so it fails to build with RHEL7's GCC 4.8, and --enable-mca-no-build=io-romio341 does not actually disable it. | EOL versions only | 🗑️ Close | RHEL7/GCC 4.8 is EOL and the whole Open MPI tree now requires C11/stdatomic.h; on current main the romio io component is gone entirely (only ompio remains) and the romio421 PR 12640 was closed. The build-config defect is confined to old toolchains; recommend close as obsolete. |
| [#11470](https://github.com/open-mpi/ompi/issues/11470) — OMPI v 5.0 was not built with internal PMI, SLURM, libevent, hwloc | Build of a Feb-2023 main nightly fails linking prted with undefined reference to PMIx_Session_control when configured against an external PMIx v4.2. | main at the time (2023); obsolete now | 🗑️ Close | This was a transient bundled-PRRTE/PMIx version mismatch (PRRTE calling PMIx_Session_control not present in the chosen external PMIx v4.2 branch) in a 2023 main nightly; 5.0.x shipped long ago and the embedded submodules are now consistent, so the issue is obsolete and can be closed. |
| [#11506](https://github.com/open-mpi/ompi/issues/11506) — Error writing shared file using ROMIO321 (openMPI 4.1.5 with UCX 1.14.0) | process_vm_readv 'Operation not permitted' crash in UCX CMA when running MPI in an Apptainer container without proper privileges, surfacing during allreduce. | n/a (external/UCX-apptainer) | 🗑️ Close | Reporter confirmed this is not an Open MPI bug but an Apptainer/UCX namespace permission issue (openucx/ucx#8958) and that it was solved; multiple users confirmed resolution. Should be closed. |
| [#11636](https://github.com/open-mpi/ompi/issues/11636) — `mpif90` shows an invalid command on Ubuntu 22.04 + gcc | On Ubuntu 22.04's apt-packaged Open MPI 4.1.2, 'mpif90 -show' emits a -L flag pointing at a nonexistent /openmpi/lib/fortran/gfortran directory, breaking manual link commands. | n/a (Debian/Ubuntu packaging) | 🗑️ Close | This is a Debian/Ubuntu distro packaging defect (wrapper-data flags injected by the package), not an upstream Open MPI source bug, on an old apt build; should be reported to the distro. Recommend closing as out-of-scope/distro packaging. |
| [#11677](https://github.com/open-mpi/ompi/issues/11677) — included ROMIO Intel GPU (ZE) doesn't build | The embedded romio341's Intel GPU (ZE) source (mpl_gpu_ze.c) fails to compile against newer Level Zero headers, and --with-io-romio-flags=--without-ze did not avoid it. | v4.0.x, v4.1.x, v5.0.x (EOL/old); fixed on main/v6.0.x | 🗑️ Close | The embedded ROMIO (romio341) is entirely removed from main and v6.0.x (only ompio remains), so the build failure cannot occur there; it persists only in branches that still ship romio341 (v4.x, v5.0.x). Recommend close as moot on supported newer branches, with note that older branches keep the workaround --disable-io-romio. |
| [#12247](https://github.com/open-mpi/ompi/issues/12247) — problem linking mpi memory routines from C++ | Including mpi.h in C++ pulls in undefined references to the removed MPI C++ binding symbols (e.g. MPI::Win::Free) even when only the C bindings are used, breaking links unless -DOMPI_SKIP_MPICXX or mpicxx is used. | v4.1.x | 🗑️ Close | The behavior stems from the MPI C++ bindings, which were removed in Open MPI v5.0 (no ompi/mpi/cxx in current main), so the issue only affects v4.1.x; workarounds (-DOMPI_SKIP_MPICXX, mpicxx) confirmed working and upgrading to v5 resolves it. Recommend close as moot on supported branches. |
| [#12814](https://github.com/open-mpi/ompi/issues/12814) — [patch] openmpi 5.0.5 header relative path correction | On AIX (no asprintf/vasprintf), the bundled OpenPMIx pmix_config_bottom.h includes 'util/pmix_printf.h' with a relative path missing the leading 'src/', breaking the configure header check. | v5.0.x (AIX, out of scope) | 🗑️ Close | This is a bug in embedded OpenPMIx (3rd-party submodule) that must be fixed upstream, not hand-edited here; AIX is unsupported and the related companion issue (#12815) was wontfixed since AIX does not even run correctly. Best closed as moot/out-of-scope, optionally after confirming the trivial path fix went upstream to PMIx. |
| [#12886](https://github.com/open-mpi/ompi/issues/12886) — Jobs started with srun using cm/PSM2 fail | Open MPI 5.0 jobs over OmniPath/PSM2 (PML cm/MTL PSM2) launched via srun under Slurm fail with broken-pipe/connection-reset, while mpirun works; root cause is the missing OMPI_MCA_orte_precondition_transports security token under srun. | fixed externally (Slurm 24.11.x) | 🗑️ Close | The reporter confirmed that upgrading Slurm (to 24.11.x, which injects OMPI_MCA_orte_precondition_transports for PMIx per Slurm 24.11.1 release notes) resolves the failure; the fix lives in Slurm rather than Open MPI, so this is moot and should be closed. |

## Duplicate / tracked elsewhere (8)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#7217](https://github.com/open-mpi/ompi/issues/7217) — MPI_Comm_split crashes with Portals4 | MPI_Comm_split and MPI_Iallreduce crash with an assertion failure inside the Portals4 reference implementation (coll/portals4 component). | fixed on all | 🗑️ Close | A maintainer confirmed the root cause is a bug in the external Portals4 reference implementation and opened an upstream tracking issue (Portals4/portals4#82); this is not an Open MPI code defect, so close as tracked elsewhere. |
| [#9316](https://github.com/open-mpi/ompi/issues/9316) — Spawning process not working with multiple MPI processes | MPI_Comm_spawn with np>1 plus MPI_Win_allocate_shared over UCX triggers UCX wireup.c ERROR messages on Power9/Mellanox, whereas it worked with older Open MPI 4.0.x and UCX 1.8. | unknown (UCX-side issue) | 🗑️ Close | Maintainers identified this as a UCX wireup problem tracked in openucx/ucx#7316 and indicated it could be closed once that is resolved; root cause is in UCX, not Open MPI. Recommend closing here as it is tracked upstream in UCX. |
| [#10908](https://github.com/open-mpi/ompi/issues/10908) — OFI BTL/MTL and efficacy of disable setting | Setting btl_ofi_disable_sep/mtl_ofi_enable_sep does not prevent libfabric's PSM2 provider from forcing PSM2_MULTI_EP=1, and having both OFI BTL and MTL active consumes double the hardware PSM2 contexts, causing context-exhaustion crashes on Omni-Path. | v4.1.x (PSM2/Omni-Path, largely EOL hardware) | 🗑️ Close | A commenter identified this as a duplicate of the still-open issue 9575 (OFI BTL+MTL endpoint/hardware-resource doubling), which remains untracked-to-fixed; OMPI's OFI components still set no PSM2_MULTI_EP and the SEP params persist, but on largely-EOL Omni-Path hardware, so recommend closing in favor of the tracking issue 9575. |
| [#11585](https://github.com/open-mpi/ompi/issues/11585) — Comm_split_type with COMM_TYPE_SHARED not working with PMIx 4.2.3 | MPI_Comm_split_type(MPI_COMM_TYPE_SHARED) returns one-process communicators for same-node ranks when using external PMIx 4.2.3, regressing from 4.2.2. | fixed via PMIx 4.2.4+ | 🗑️ Close | Maintainer identified this as a duplicate of #10416, a PMIx 4.2.3 regression fixed in PMIx 4.2.4 (workarounds: downgrade, internal PMIx, or PMIx patch). Tracked elsewhere and resolved upstream; close as duplicate. |
| [#10221](https://github.com/open-mpi/ompi/issues/10221) — Assertion `reserve > 0' failed running collective-big-count tests using v4.1.x branch and adapt component | collective-big-count tests abort on the assertion `reserve > 0' in coll_base_util.h's ompi_coll_base_nbc_reserve_tags when using the adapt coll component on v4.1.x. | v4.1.x (EOL); assert still present on main | ❓ Needs info | Reporter said this assert was largely not seen on main (possibly fixed there) and overlaps with #10186; the assert still exists in coll_base_util.h on main as a guard. v4.1.x is EOL. Treat as duplicate of the big-count set; ping/close given EOL. |
| [#10191](https://github.com/open-mpi/ompi/issues/10191) — Multiple failures running collective-big-count tests with OMPI main branch and 'ftagree' collective component | collective-big-count self-checking tests fail with wrong data, SIGSEGV, and double-free using the ftagree coll component on main; reporter notes errors look identical to the tuned-component report. | main | ✅ Keep open | The reporter states these are the same failures as #10190 (likely common base/big-count code), so this is effectively a duplicate of the broader big-count issue set; still valid on main but should be consolidated/tracked with #10190/#10214. |
| [#11501](https://github.com/open-mpi/ompi/issues/11501) — High latency sending from device memory on Summit | Very high CUDA device-memory point-to-point latency with pml/ucx on Summit (POWER9), and a segfault with pml/ob1+btl/uct above 4KB; workaround is UCX_MEM_CUDA_HOOK_MODE=reloc. | main (PowerPC/Summit-specific, UCX upstream) | ✅ Keep open | Root cause is an upstream UCX bistro CUDA hook bug on PowerPC (openucx/ucx#8761); reporter explicitly asked to keep this open until that upstream issue is resolved. It is tracked upstream and Summit is now decommissioned, but per the reporter's request keep open pending the UCX fix. |
| [#13397](https://github.com/open-mpi/ompi/issues/13397) — cm/psm2 fails to get key from PMIx | cm/psm2 aborts MPI_Init with 'Error obtaining unique transport key from PMIX' because the PMIx OPA (pnet/opa) component that set OMPI_MCA_orte_precondition_transports was removed in PMIx 5.0.8. | v5.0.x, main | ✅ Keep open | Root cause is in PMIx (OPA component removal); Cornelis restored a pnet/opa component upstream in openpmix (April 2026) pending user verification, and the psm2 MTL still depends on this key on main (mtl_psm2.c:117) including a noted singleton bug; keep open until the upstream PMIx fix is confirmed and the singleton path is addressed. |

## Wontfix / out of scope (15)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#134](https://github.com/open-mpi/ompi/issues/134) — Fujitsu: MPI_GATHER (linear_sync) can be truncated with derived datatypes | Tuned collective pipelining (e.g. linear_sync MPI_Gather) splits messages by whole-datatype repetitions, causing truncation/corruption when send and receive type signatures differ even with the same typemap. | v5.0.x, v6.0.x, main | 🗑️ Close | Maintainer (bosilca) stated in 2020 this is a fundamental pipelining limitation that 'isn't fixed and will not be'; the pipelining message-splitting logic is still in current tuned collectives, so it persists but is effectively a wontfix, recommend closing (or keep open as a known limitation). |
| [#1462](https://github.com/open-mpi/ompi/issues/1462) — Invalid memory access inside allreduce_intra_recursivedoubling | A user MPI_Op that copies the full sizeof() of a padded type reads past the reduction temp buffer, which is sized true_extent+(count-1)*extent; valgrind flags an invalid read. | n/a | 🗑️ Close | Maintainers concluded the buffer sizing is correct per the standard and the user op must not touch memory outside the datatype's layout; this is user-code behavior, not an Open MPI defect, with workarounds provided. Answered/wontfix, recommend close. |
| [#2289](https://github.com/open-mpi/ompi/issues/2289) — openmpi 2.0.1 over/under linkage | rpmlint reports undefined-non-weak-symbol (under-linkage) warnings for Open MPI's common/plugin shared libraries that resolve their opal_/ompi_ symbols only at dlopen time. | EOL versions only | 🗑️ Close | This is intentional by Open MPI's DSO/plugin design (symbols resolved at runtime when dlopened), not a real defect; the maintainer's only comment explains it and notes --disable-dlopen as a workaround. It is out of scope as a bug, so close. |
| [#2366](https://github.com/open-mpi/ompi/issues/2366) — MPI_Get_elements for data types used in reduction functions | MPI_Get_elements returns different values than MPICH for the predefined reduction pair-types (e.g., MPI_DOUBLE_INT), because Open MPI treats them as unitary predefined types while MPICH decomposes them. | main | 🗑️ Close | Maintainer determined this is a legitimate difference of standard interpretation (a one-line behavior choice) and suggested asking the Forum; it is not clearly an Open MPI defect and went stale in 2017. Recommend closing as interpretation/out-of-scope unless a Forum ruling is pursued. |
| [#7091](https://github.com/open-mpi/ompi/issues/7091) — Wrong values for MPI_Datatype with armflang | With the flang-based armflang (and PGI) Fortran compiler, mpi_f08 named constants like MPI_INTEGER come back as 0 because the compiler/linker mishandles the weak symbols, while gfortran works. | unknown | 🗑️ Close | Maintainers diagnosed this as a flang/armflang compiler-and-linker defect (weak symbol resolution), reported it to ARM, and noted there is essentially nothing for Open MPI to do. A workaround PR (#7105) was filed; the issue has been dormant since 2019 with a maintainer asking to close. Recommend close as an upstream compiler issue. |
| [#8334](https://github.com/open-mpi/ompi/issues/8334) — AVX-based MPI_OP performance regression | LAMMPS allreduce regressed ~10-25% in 4.1.0 vs 4.0.5 on one Skylake/EFA setup attributed to the AVX MPI_OP component (AVX512 frequency scaling), but could not be reproduced in standalone benchmarks. | EOL versions only (v4.1.x; behavior may persist where op/avx exists) | 🗑️ Close | After extensive investigation the regression was confined to one app/processor/compiler combination and never reproduced in microbenchmarks; maintainers leaned toward documenting it rather than fixing, and op_avx_support already lets users restrict AVX levels. Recommend closing as no-clear-fix/wontfix (op/avx still exists on main but no actionable bug was confirmed). |
| [#9580](https://github.com/open-mpi/ompi/issues/9580) — MPI_Win_allocate / MPI_Get RMA fails or is slow on ConnectX-3 (mlx4) | Multi-node RMA (MPI_Win_allocate + MPI_Get/Rget with passive synchronization) either runs slowly or aborts with MPI_ERR_WIN when forcing osc/rdma on a ConnectX-3 (mlx4) Infiniband cluster. | v5.0.x, v6.0.x, main | 🗑️ Close | Root cause is that the ConnectX-3 (mlx4) hardware lacks the 32/64-bit hardware atomics that osc/ucx's lock/Rget path requires (per yosefe), so this is a hardware limitation rather than an Open MPI defect; an osc/rdma + btl_openib workaround was found. The legacy mlx4 hardware is largely obsolete. Recommend closing as out of scope / hardware limitation. |
| [#10678](https://github.com/open-mpi/ompi/issues/10678) — perf: hcoll: mpi_get_accumulate test slows down in mpi_win_fence when hcoll is enabled | With coll/hcoll enabled, a get_accumulate test in MPI_Win_fence is ~10x slower, traced to mca_coll_hcoll_barrier spinning in hmca_bcol_basesmuma barrier; HCOLL_ML_USE_SHMSEG_BARRIER=0 mitigates. | main (HCOLL-dependent) | 🗑️ Close | The overhead lives entirely in the closed-source NVIDIA/Mellanox HCOLL library's barrier (hmca_bcol_basesmuma), not in Open MPI code, and the workaround is an HCOLL env var, making it out of Open MPI's scope to fix. Close as out of scope (HCOLL-side); no maintainer engagement in years. |
| [#11133](https://github.com/open-mpi/ompi/issues/11133) — Build failure with Intel compiler (main branch of open mpi) | Build of embedded OpenPMIx fails under the Intel classic compiler (icc) with error #188 'enumerated type mixed with another type' due to PMIx's picky-mode flags. | fixed on all | 🗑️ Close | Resolved in-thread: the failure was a known Intel classic-compiler quirk that PMIx maintainers consider a compiler bug; the user fixed it by switching to the oneAPI compilers (icx/icpx), and the classic compilers are EOL. This is a PMIx/3rd-party concern, not OMPI code. Recommend closing as resolved/out of scope. |
| [#11201](https://github.com/open-mpi/ompi/issues/11201) — HCOLL doesn't handle custom data types | MPI_Allgather with a duplicated (MPI_Type_dup) datatype segfaults inside libhcoll's hcoll_create_mpi_type when HCOLL is enabled; disabling HCOLL avoids it. | external (libhcoll) | 🗑️ Close | The crash is in the proprietary Mellanox/NVIDIA libhcoll library (hcoll_create_mpi_type in the backtrace), not in Open MPI code; janjust confirmed 'HCOLL has an issue' and there is no hcoll source in the OMPI tree to fix. A workaround (disable HCOLL or set HCOLL_CONVERT_DTE_TO_BYTE) exists. Recommend closing as an external/out-of-scope issue. |
| [#11845](https://github.com/open-mpi/ompi/issues/11845) — MPMD MPI_APPNUM equivalent with srun | Under srun MPMD, MPI_APPNUM is always 0 (or otherwise wrong) because Slurm's PMIx plugin hard-codes PMIX_APPNUM to 0, so Open MPI cannot distinguish app numbers as it does with mpirun. | all (depends on Slurm) | 🗑️ Close | Root cause is in Slurm's PMIx plugin (a SchedMD ticket was opened, schedmd 13019); maintainers concluded Open MPI cannot fix it correctly without extra info from Slurm and recommended documenting only, so close as out of Open MPI's scope. |
| [#12051](https://github.com/open-mpi/ompi/issues/12051) — v5.0.0: Default configure on MacOS with OneAPI compilers fails | Default configure on macOS Sonoma with Intel OneAPI classic compilers (icc/ifort) fails because PMIx cannot link external libevent; icc needs -L/usr/local/lib that gcc/clang do not. | v5.0.x and later (workaround exists; Intel-side bug) | 🗑️ Close | Diagnosed by the reporter (jsquyres) as a quirk/bug in the Intel OneAPI classic compilers, with clear LDFLAGS and --with-libevent=internal workarounds documented; the now-removed icc/icpc classic compilers make this largely moot. Recommend Close as Intel-side/wontfix with workaround. |
| [#12194](https://github.com/open-mpi/ompi/issues/12194) — openmpi-4.1.6: ldflags in pkg-config incompatible with CMake's pkg_check_modules(... IMPORTED_TARGET ...) | Open MPI's generated .pc files use the space-separated form '-Wl,-rpath -Wl,/path' which CMake's pkg_check_modules de-duplicates incorrectly, breaking links. | v4.1.x, v5.0.x, v6.0.x, main | 🗑️ Close | Maintainers concluded this is a CMake bug (CMake mis-dedups valid Libs tokens) and the flags come from Libtool, not Open MPI directly; disinclined to fix and tracked upstream in CMake. Effectively wontfix on Open MPI's side, so recommend close. |
| [#12815](https://github.com/open-mpi/ompi/issues/12815) — make MPIx and PRRTE more portable for AIX/HP-UX/MSVC (remove getopt.h/ifaddrs.h) | Patches to drop getopt.h/ifaddrs.h includes and inline GNU libc struct definitions so PMIx/PRRTE/MPL build on AIX, HP-UX, and MSVC. | n/a (AIX/HP-UX/MSVC, out of scope) | 🗑️ Close | Maintainers (rhc54) declined: copying GNU libc code raises licensing concerns, these platforms are unsupported with no developers/users, and the reporter confirmed it segfaults on AIX anyway even when it compiles. Explicitly wontfix/out of scope; close. |
| [#13358](https://github.com/open-mpi/ompi/issues/13358) — mpirun is terminated saying stack smashing detected when hostname is long | On Open MPI 4.1.x a hostname longer than 56 characters causes a stack buffer overflow in regx_fwd nidmap_create, crashing mpirun. | EOL versions only (v4.1.x) | 🗑️ Close | The bug is in the ORTE/regx_fwd code that exists only in the v4.x series (removed in v5's PRRTE-based runtime); a maintainer noted v4.x is effectively closed and the user should upgrade or use --mca regx naive, so this is wontfix on the EOL branch and can be closed. |
