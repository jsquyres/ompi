# Open documentation / other issues older than 6 months

Triage of open `open-mpi/ompi` issues opened more than 6 months ago (created before 2025-12-20), as of 2026-06-20. Classification and status are a **reasoned determination** from each issue's text, discussion, labels, and linked/merged PRs, plus spot-checks of current `main` — not a per-branch build-and-reproduce test. Treat "Close" as *recommended for maintainer confirmation*, not automatic.

Issues are grouped by **why they are still open**. See [issues-overview.md](issues-overview.md) for totals and cross-tabs, and the companion files: [bugs](issues-bugs.md) · [feature requests](issues-feature-requests.md) · [questions](issues-questions.md) · [other](issues-other.md).

This file holds **Documentation** issues and everything that isn't a bug, feature request, or question (RFCs, meta/tracking, CI/infra, refactors). The **Type** column distinguishes them.


**67 issues** — 🗑️ Close 27 · ❓ Needs info 3 · ✅ Keep open 37.


## Unresolved, still valid (37)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#5599](https://github.com/open-mpi/ompi/issues/5599) — BML (and BTL) framework(s) initialized even when they are not used | Other | Asks whether the BML/BTL frameworks can be opened lazily rather than unconditionally in ompi_mpi_init, since PMLs/OSCs like UCX do not use them, to avoid needless component init overhead. | main (optimization, likely still applicable) | ❓ Needs info | This is a startup-cost optimization/refactor question; bwbarrett noted it is more than an if-statement due to historic ompi_proc_t coupling. No code change is referenced and BML init in MPI_Init likely remains, so it is still valid but low priority; ping to confirm relevance against current init path. |
| [#11288](https://github.com/open-mpi/ompi/issues/11288) — CI for runtime testing (starter) | Other | Meta/tracking issue (help-wanted) to add CI runtime tests for Open MPI that exercise mpirun/PRRTE behavior (map/bind, spawn, MPMD) when bumping the openpmix/prrte submodule pointers, captured via a runtime test harness in ompi-tests-public. | n/a | ❓ Needs info | Infra/CI tracking issue from 2023; jjhursey reported a backend runtime CI harness PR (ompi-tests-public#25) and IBM CI monitoring was being set up, but the issue's checklist was never closed. Needs a maintainer to confirm whether the runtime CI is now in place and close it; recommend pinging. |
| [#150](https://github.com/open-mpi/ompi/issues/150) — Make better help messages for BTL base MCA params | Documentation | Request to add guidance in the help text of the MCA parameters registered by mca_btl_base_param_register() (e.g. which values must be less/greater than others). | v5.0.x, v6.0.x, main | ✅ Keep open | Labeled a bug but is really a documentation/help-text improvement; it was repeatedly deferred over years and never done, and the BTL base params still lack such guidance, so it remains valid but low priority, recommend keep open or close as stale. |
| [#189](https://github.com/open-mpi/ompi/issues/189) — Remove PERUSE in favour of upcoming MPI_T pvar implementation | Other | RFC to delete the PERUSE interface once MPI_T (pvars/events) can provide equivalent fine-grained network instrumentation. | n/a | ✅ Keep open | PERUSE still exists in ompi/peruse on main, and the referenced replacement PR #8057 (MPI_T Events) was closed unmerged in Jan 2026, so the precondition for removal is not met. The decision (keep PERUSE until MPI_T matches it) is still pending; keep open. |
| [#860](https://github.com/open-mpi/ompi/issues/860) — OpenMPI cannot handle standard UNIX filename | Documentation | MPI_File_open fails on filenames containing ':' under the ROMIO IO component because ROMIO interprets a prefix before the colon as a filesystem type, while OMPIO does not; the request is to document this reserved-character behavior. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | ✅ Keep open | The colon-prefix behavior is MPI-standard-sanctioned ROMIO behavior (not a code bug), the doc PR #861 was closed without merging, and the current MPI_File_open.3.rst still does not document reserved characters / IO-component differences, so the doc gap stands. Recommend keeping open as a small documentation task. |
| [#1141](https://github.com/open-mpi/ompi/issues/1141) — corrected/expanded message about shmem temporary directory | Documentation | The shmem/mmap help text references $TEMPDIR but the Unix temp dir is $TMPDIR, and it should also mention the relevant MCA parameter. | main | ✅ Keep open | opal/mca/shmem/mmap/help-opal-shmem-mmap.txt on current main still reads '$TEMPDIR, $TEMP, and $TMP' and does not mention the MCA parameter, so the doc fix was never applied; still valid. |
| [#2159](https://github.com/open-mpi/ompi/issues/2159) — Audit OB1 + BTLs for multi-threaded out-of-sequence performance degredation | Other | Placeholder/tracking issue to investigate and reduce out-of-sequence fragment handling performance degradation in ob1+BTLs under MPI_THREAD_MULTIPLE. | n/a | ✅ Keep open | This is an open-ended optimization/tracking placeholder with no comments and no concrete deliverable; no evidence the OOS redesign was completed. It remains a valid long-term improvement task, so keep open (or close as stale per maintainer preference). |
| [#4845](https://github.com/open-mpi/ompi/issues/4845) — Document binding behavior (especially w.r.t. threads) | Documentation | Documentation task to explain default process binding (bind-to-package for np>2) and its interaction with progress threads / MPI_THREAD_MULTIPLE, and how to change it. | n/a | ✅ Keep open | Labeled documentation; the agreed action was to document current binding behavior in README/FAQ. With the move to docs.open-mpi.org RST docs, this may be partially addressed, but I could not confirm a dedicated thread-binding section. A legitimate doc gap; keep open (or close if the maintainer confirms docs now cover it). |
| [#6422](https://github.com/open-mpi/ompi/issues/6422) — Add a test to verify that next_cid works after unbalanced communicator creation | Other | Follow-up task (from PR 6417) to add a regression test that creates unbalanced communicators to exercise the next_cid code path that previously had a critical bug. | n/a | ✅ Keep open | This is a test-coverage enhancement task (labeled enhancement/test) rather than a defect; no evidence such a test was added. Low-priority but still-valid backlog item; keep open unless maintainers decide it is no longer worth tracking. |
| [#7695](https://github.com/open-mpi/ompi/issues/7695) — OMPI_Affinity_str and friends need to be updated | Other | The OMPI_Affinity_str MPI extension uses the old OPAL output/affinity code that was slated for replacement by PRRTE-equivalent code; it needs updating or removal. | main | ✅ Keep open | The extension still exists in ompi/mpiext/affinity/ on main and was never reworked; maintainers noted it is a public extension some users may depend on (so not simply dead code to delete) and is won't-fix for v5.0.x but may be cleaned up on main. Genuine, unresolved refactor item; keep open. |
| [#9307](https://github.com/open-mpi/ompi/issues/9307) — Lazy initialization of frameworks is not thread safe | Other | Design/refactor issue (spun off from PR #9302) to create a unified thread-safe mechanism for lazy initialization of MCA frameworks. | n/a (refactor/design) | ✅ Keep open | This is a developer design/refactor discussion with only a draft proposal branch and no merged resolution; thread-safe lazy framework init has not obviously been generalized. Still a valid internal tracking item; recommend keeping open (could be revisited for relevance). |
| [#9332](https://github.com/open-mpi/ompi/issues/9332) — What to do with OMPI_MPI_THREAD_LEVEL env variable? | Other | Discussion/RFC about whether and how to keep the OMPI_MPI_THREAD_LEVEL override (env var vs MCA param), which functions it should apply to (MPI_Init/MPI_Init_thread/Sessions), and spec-compliance concerns. | n/a (policy/RFC; v4.0.x/v4.1.x EOL) | ✅ Keep open | PRs landed for master/v5.0.x but the agreed-upon separate handling/backport for v4.0.x/v4.1.x and the broader decision (MCA param conversion) appear unfinished, and the discussion is the open deliverable. Recommend keeping open, though it may be ping-able to confirm whether anything remains given v4.x is EOL. |
| [#9773](https://github.com/open-mpi/ompi/issues/9773) — Wrong default binding in Open MPI v4 on AMD EPYC with NSP4 partitioning | Documentation | On AMD EPYC with NPS4, Open MPI v4.0.x/v4.1.x default-bind to NUMA partitions rather than the socket that the mpirun manpage documents; the v4 binding behavior was correct on master but the manpage was never updated. | v4.0.x, v4.1.x | ✅ Keep open | rhc54 confirmed the manpage is simply out of date (master/PRRTE behaves correctly); a 2024 commenter confirmed the v4 docs were never fixed. This is a documentation defect limited to the v4.0.x/v4.1.x manpages; v5+ uses PRRTE docs. Recommend keeping open until the v4 docs are corrected, or closing as EOL-only. |
| [#10402](https://github.com/open-mpi/ompi/issues/10402) — ompi_info_t does not follow OMPI object semantics | Other | ompi_info_t is an opal_object usable via OBJ_NEW but must really be created via ompi_info_allocate to retain the ompi instance, an awkward dual-allocation semantics that maintainers wanted to revisit. | main, v5.0.x | ✅ Keep open | The user-facing bug was fixed by PR 10349/10420, but devreal explicitly asked to keep it open to address the underlying object-semantics discrepancy; ompi_info_allocate still exists in ompi/info/info.c on main, so the design wart remains. Keep open as a still-valid refactor/design item. |
| [#10459](https://github.com/open-mpi/ompi/issues/10459) — List of issues with OMPI master+ULT+PartCom | Other | Tracking issue listing ULT (qthreads/argobots) build and hang problems plus partitioned-communication deadlocks and a request to add ULT CI coverage. | main, v5.0.x | ✅ Keep open | A meta/tracking issue; several checkboxes were fixed but the partitioned-communication unequal-partition deadlock and ULT CI coverage remained unchecked, and the qthreads/argobots threads components still exist on main. Keep open as an active tracker of outstanding items. |
| [#10481](https://github.com/open-mpi/ompi/issues/10481) — Documentation: v5.0.x man page items | Documentation | Checklist of RST man-page polish items: token formatting, 'See the ___ section' cross-references, OpenSHMEM list indentation, missing OSHMEM cross-linking, code-block line numbers, and documenting locked-memory limits. | main, v5.0.x | ✅ Keep open | Doc tracking checklist with all boxes unchecked; the OpenSHMEM man-page RST sources still exist under docs/man-openshmem and the items appear unaddressed. Keep open as a still-valid documentation task list. |
| [#10526](https://github.com/open-mpi/ompi/issues/10526) — Unify integer constants between mpi.h and the Fortran interfaces | Other | Code-cleanup request to unify the two sources of truth for MPI public integer constants (mpi.h.in and the Fortran constant generator) into a single source. | main, v5.0.x, v6.0.x | ✅ Keep open | On current main mpi.h.in still hardcodes constants (e.g. MPI_ANY_SOURCE -1) independently of mpif-values.py (which defines the same values), so the dual source of truth persists and the cleanup is not done. Keep open as a still-valid refactor/cleanup task. |
| [#10962](https://github.com/open-mpi/ompi/issues/10962) — OSC header has outdated doxygen comments | Documentation | The ompi/mca/osc/osc.h component query and module function doxygen comments are incomplete: parameters base, size, disp_unit, and flavor of the query API are not documented. | main | ✅ Keep open | Verified on current main: the component_query_fn_t doxygen still omits base/size/disp_unit/flavor, so the documentation gap remains. Genuine, low-priority doc task; recommend keep open. |
| [#11148](https://github.com/open-mpi/ompi/issues/11148) — mpirun can now invoke mpirun | Documentation | Request to document that, as of Open MPI v5, mpirun can recursively invoke mpirun (the prior restriction was removed via PRRTE), e.g. in docs or the mpirun man page. | not done | ✅ Keep open | A grep of docs/ found no mention of nested/recursive mpirun, so the requested documentation appears never to have been added; labeled documentation/Target v5.0.x. The behavior change is real and valid. Recommend keeping open as a still-pending doc task. |
| [#11239](https://github.com/open-mpi/ompi/issues/11239) — ompi_mpi_instance_init_common needs to be local | Other | Tracking/refactor issue: the shared init path (used by both MPI_Init and MPI_Session_init) performs a global PMIx fence even for purely-local Session semantics; the fence/add_procs should be moved to MPI_Init and to the *_create_from_group paths, partly blocked by smsc components using the PMIx IMMEDIATE attribute which forces a fence. | main | ✅ Keep open | Related #11212 (remove redundant PMIx fence calls) is closed, but the smsc components (xpmem, cma) still use OPAL_MODEX_RECV_IMMEDIATE on current main and the underlying session-locality refactor is incomplete. This is a developer architectural-tracking item still valid on main; keep open. |
| [#11340](https://github.com/open-mpi/ompi/issues/11340) — Documentation beautification on the rst pages | Documentation | Tracking issue with a checklist of RST man-page beautification tasks: listify ERRORS.rst and See Also sections, add/normalize man-page links, backtick constants, move section-5 pages, copy examples from deprecated to new routines, etc. | n/a | ✅ Keep open | Much of the checklist is done (e.g. ERRORS.rst now uses bullet lists with :ref: links and no (3) suffix) and jsquyres explicitly re-opened it for remaining work; some items (constants linking per #11359, easier man-page linking) remain unchecked. Still a valid docs tracking item; keep open. |
| [#11556](https://github.com/open-mpi/ompi/issues/11556) — MPI_2COMPLEX and MPI_2DOUBLE_COMPLEX should be removed | Other | Non-standard datatypes MPI_2COMPLEX/MPI_2DOUBLE_COMPLEX are exposed in mpi.h but have no usable reduction ops (MAXLOC/MINLOC on complex is undefined); request to remove them from the C header or mark deprecated. | main, v5.0.x, v4.1.x | ✅ Keep open | Confirmed still present in ompi/include/mpi.h.in (lines 1333-1334) and mpif-values.py on current main; an RFC-style cleanup tied to Fortran ABI compatibility that remains unaddressed. Affects all current branches. |
| [#11680](https://github.com/open-mpi/ompi/issues/11680) — Clean up MCA var de-registration of common components | Other | Internal cleanup request: MCA variable de-registration for common components is fragile, forcing components to do awkward things (e.g. malloc string values) to avoid segfaults at finalize. | main, v5.0.x | ✅ Keep open | This is a known MCA architectural cleanup task acknowledged by maintainers and explicitly deferred past v5.0.0; no PR has resolved it, so it remains a valid open refactor item on main. |
| [#11692](https://github.com/open-mpi/ompi/issues/11692) — Document new "soft breakpoint" functionality | Documentation | Request to document the PRRTE/PMIx soft-breakpoint (stop-in-app) capability, which OMPI uses via ompi_rte_breakpoint in MPI_Init for debugger attach. | main, v5.0.x | ✅ Keep open | Labeled documentation with State: Awaiting code; the OMPI-side code (ompi_rte_breakpoint) exists, but maintainers debated whether/where to document it and no docs were added. Still a valid open doc task. |
| [#12038](https://github.com/open-mpi/ompi/issues/12038) — mtl/ofi: call to fi_domain fails on Crusher/Frontier | Documentation | On Cray Slingshot (cxi) systems MPI_Init fails in fi_domain unless PRTE_MCA_ras_slurm_use_entire_allocation and PRTE_MCA_ras_base_launch_orted_on_hn are set; tracked as a docs/auto-detection issue. | v5.0.x, main | ✅ Keep open | Earlier PRs (12162/12163) addressed docs but a 2026 comment reports hitting the same problem on Polaris and requests auto-setting PRTE_MCA_ras_base_launch_orted_on_hn at configure time; the usability gap remains open, so keep open. |
| [#12088](https://github.com/open-mpi/ompi/issues/12088) — MPI 4.1 - MPI_Win_free is synchronizing, except when it isn't | Other | Tracking item noting the MPI 4.1 clarification that MPI_Win_free is synchronizing except for windows created with MPI_Win_create_dynamic. | not implemented | ✅ Keep open | An MPI 4.1 spec-compliance/tracking item with no associated comments or visible code change; whether Open MPI's MPI_Win_free behavior matches the refined wording is unconfirmed. Recommend Keep open as an MPI 4.1 tracking item. |
| [#12186](https://github.com/open-mpi/ompi/issues/12186) — MPI 4.1 - need to check if open mpi T is handling counter overflow correctly | Other | Tracking task to verify Open MPI's MPI_T performance variable counter overflow behavior matches the clarifications added in the MPI 4.1 standard (forum issue 414). | main, v5.0.x, v6.0.x | ✅ Keep open | No comments and no linked PR; this is an open MPI 4.1 conformance audit task that has not been demonstrably completed, so it remains valid for current branches. Keep open as ongoing conformance work. |
| [#12187](https://github.com/open-mpi/ompi/issues/12187) — MPI 4.1 - need to check MPI_WIN_SYNC for correct behavior for windows using shared memory | Other | Tracking task to verify MPI_WIN_SYNC behaves correctly for shared-memory windows per MPI 4.1 clarification (forum issue 553). | main, v5.0.x, v6.0.x | ✅ Keep open | No comments and no evidence of completion; an MPI 4.1 conformance verification task that appears still open and valid. Keep open as ongoing conformance work. |
| [#12204](https://github.com/open-mpi/ompi/issues/12204) — MPI 4.1 - opaque data handles and state preservation across sessions | Other | MPI 4.1 wording implies user-created datatypes (and similar handles) should survive finalization of all sessions/WPM and be usable in a new session, which Open MPI may not currently guarantee. | main, v5.0.x, v6.0.x | ✅ Keep open | 2025 discussion concludes datatypes likely need to retain a reference to the OMPI instance and that current behavior is an oversight; no fix landed. Genuine conformance/design work still open; keep open. |
| [#12205](https://github.com/open-mpi/ompi/issues/12205) — MPI 4.1 - error handler argument to MPI_COMM_CREATE_FROM_GROUP | Other | Tracking task to align handling of the error-handler argument to MPI_COMM_CREATE_FROM_GROUPS and MPI_INTERCOMM_CREATE_FROM_GROUPS with MPI 4.1 changes (forum issues 511, 588). | main, v5.0.x, v6.0.x | ✅ Keep open | Only a cross-reference comment; no evidence the conformance check/fix was completed. Valid open MPI 4.1 conformance audit task; keep open. |
| [#12479](https://github.com/open-mpi/ompi/issues/12479) — Revisit the request management logic for the nonblocking resilience support | Other | Maintainer tracking task to correctly handle and document user-visible requests in the nonblocking fault-tolerance (ULFM) functions, starting with MPI_Comm_ishrink. | main | ✅ Keep open | Internal refactor/tracking item opened by a maintainer based on discussion in #12384; no comments and no evidence the ULFM ishrink/nonblocking request handling was reworked. Keep open as a developer task. |
| [#12773](https://github.com/open-mpi/ompi/issues/12773) — m4 cleanup on main since we will not be supporting external PRRTE | Other | Maintainer note that ompi_setup_prrte.m4 can be simplified on main/6.0.x now that external PRRTE will no longer be supported (related to #12684). | n/a | ✅ Keep open | This is a refactor/cleanup task; updated as recently as May 2026 with no indication the m4 simplification was completed, so it remains a valid open cleanup item. |
| [#13074](https://github.com/open-mpi/ompi/issues/13074) — RFC: Provide equivalence of MPICH_ASYNC_PROGRESS | Other | RFC proposing to re-enable an OPAL software-based async progress thread (configure --enable-progress-threads plus runtime env var) to accelerate intra-node nonblocking collectives, similar to MPICH_ASYNC_PROGRESS. | not implemented | ✅ Keep open | An RFC with maintainer feedback and caveats (binding/contention) but no merged implementation; the feature is not in the tree. Recommend keeping open as a design discussion/feature tracker. |
| [#13300](https://github.com/open-mpi/ompi/issues/13300) — Need to update release notes for 6.0.x | Other | Tracking/meta task to populate the v6.0.x release notes changelog from the 6.0.x feature list and other items before release. | v6.0.x | ✅ Keep open | This is an internal release-management tracking task with no fix to verify in code; it remains relevant until the v6.0.x release notes are completed, so keep open as a tracking item. |
| [#13341](https://github.com/open-mpi/ompi/issues/13341) — mpirun man page needs to be audited for the 6.0.0 release | Documentation | The mpirun man page RST renders many unhelpful 'admonition' blocks that need auditing and correcting or removal before the 6.0.0 release. | main, v6.0.x | ✅ Keep open | This is a maintainer-filed doc cleanup task labeled documentation and Target: v6.0.x with no resolution comments; the cleanup is still pending, so it remains a valid open documentation task for the v6.0 release. |
| [#13482](https://github.com/open-mpi/ompi/issues/13482) — Connect-accept between multiple mpirun instances | Other | RFC/discussion asking whether cross-allocation connect-accept between multiple mpirun instances is truly needed by the community, or whether it is a documentation/education gap. | n/a | ✅ Keep open | Open-ended design/requirements discussion authored by a maintainer seeking community guidance; no comments and no decision. Not a defect. Keep open as a tracking/discussion item until requirements are gathered. |
| [#13485](https://github.com/open-mpi/ompi/issues/13485) — v6.0.0 checklist | Other | Meta release-tracking checklist of steps to build and publish the v6.0.0 release. | n/a | ✅ Keep open | This is a release-process tracking issue, not a defect or feature; it stays open until the v6.0.0 release work is fully complete and signed off. Keep open as meta/tracking. |

## Fixed, not closed (8)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#46](https://github.com/open-mpi/ompi/issues/46) — Add FAQ entry about firewalls | Documentation | Request to document firewall behavior: that firewalls (e.g. iptables) block OMPI's TCP OOB, and how to run through a firewall using configurable port ranges. | n/a | 🗑️ Close | Firewall guidance now exists in the migrated docs (docs/launching-apps/troubleshooting.rst advises disabling firewalling between hosts, and docs/app-debug/serial-debug.rst notes intermediate firewalls), so the documentation request is effectively addressed; recommend closing. |
| [#258](https://github.com/open-mpi/ompi/issues/258) — Performance of freelists and other common OPAL classes with OPAL_ENABLE_MULTI_THREADS==1 | Other | Tracking/design discussion to recover performance lost when OPAL_ENABLE_MULTI_THREADS was set to 1 by default, by providing thread-safe and non-thread-safe flavors of hot OPAL classes such as free lists. | n/a | 🗑️ Close | The proposed work was largely done: opal_free_list and ompi_free_list were consolidated into a single opal_free_list with opal_using_threads()-gated locking, so the design goal has been addressed. Recommend closing as a stale design/RFC tracking issue. |
| [#8752](https://github.com/open-mpi/ompi/issues/8752) — Stop using Perl Smartmatch in autogen.pl | Other | Request to stop using Perl's experimental smartmatch operator in autogen.pl's --disable-3rdparty handling to avoid deprecation warnings. | fixed on all | 🗑️ Close | autogen.pl no longer uses smartmatch; the 3rd-party package list lookups now go through a plain list_contains() helper (defined at line 127), so this cleanup is done and the issue can be closed. |
| [#9802](https://github.com/open-mpi/ompi/issues/9802) — SMSC/knem throws error messages referencing BTL/knem | Documentation | The knem smsc help/error messages incorrectly referenced the old BTL/sm 'btl_sm_single_copy_mechanism' MCA variable and 'sm shared memory BTL' rather than the smsc framework. | fixed on all | 🗑️ Close | Local main opal/mca/smsc/knem/help-smsc-knem.txt now tells users to set the smsc MCA variable to '^knem' with no BTL/sm wording, so the reported message is corrected; the maintainer left it open only as a tracking reminder. |
| [#11363](https://github.com/open-mpi/ompi/issues/11363) — Docs: Fold all legacy "FAQ" content into the rest of the document | Documentation | Tracking task to audit the legacy FAQ content under docs/faq/, fold the still-relevant parts into the main RST docs, drop the obsolete parts, and remove the FAQ section. | n/a | 🗑️ Close | PRs #11486/#11531 (by qkoziol) addressed this; on current main the docs/faq/ directory is gone and docs/index.rst now only references the external "legacy Open MPI FAQ" link, so the FAQ section was removed as planned. Work appears complete but the issue stayed open; recommend close. |
| [#11878](https://github.com/open-mpi/ompi/issues/11878) — [5.0.0rc12] can't decide between rocm and cuda accelerator | Documentation | On a node with both ROCm and CUDA stacks installed, Open MPI aborts saying only one accelerator framework is supported per node; reporter requested better docs and DSO behavior. | fixed on all (5.0.1+) | 🗑️ Close | From 5.0.1+ all accelerator components build as DSOs by default so the abort no longer occurs in practice; maintainers judged the only remaining ask (a doc section on excluding a component) redundant with general MCA docs, so the substance is resolved and it can be closed. |
| [#12547](https://github.com/open-mpi/ompi/issues/12547) — dead coll tuned alltoall mca parameters | Documentation | Several coll_tuned_alltoall_* MCA params are reported by ompi_info but do nothing, complicating tuning; reporter asked to remove them or mark their descriptions as no-ops. | fixed on main | 🗑️ Close | On main the truly-dead params (alltoall_small_msg, intermediate_msg, large_msg, min_procs) are now registered with MCA_BASE_VAR_FLAG_DEPRECATED, and the fanout/segmentsize params are now actually used per the linked PR. The core ask (clarify/deprecate) is addressed; recommend close. |
| [#12936](https://github.com/open-mpi/ompi/issues/12936) — Fix Documentation for environment variables | Documentation | The v5.0.x mpirun man page incorrectly used PRRTE_MCA... instead of PRTE_MCA... for setting environment variables. | fixed on all | 🗑️ Close | PR #12937 (merged on main 2024-12-02) fixed the typo; the local checkout shows PRTE_MCA in docs/man-openmpi/man1/mpirun.1.rst and no PRRTE_MCA in docs/. The reporter's secondary prte_info/hwloc question is a separate, minor support matter; close as fixed. |

## Feature now exists (3)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#7311](https://github.com/open-mpi/ompi/issues/7311) — FAQ on using single socket | Documentation | Request for documentation/FAQ on how to run one socket per node from the command line without a hostfile, plus a confusing not-bound binding message and a --rank-by crash. | v5.0.x, main | 🗑️ Close | The PMIx --rank-by crash was fixed and a display=cpus / show-cpus feature was added in PRRTE for OMPI v5 (openpmix/prrte#1634); mapping/binding is now documented in docs scheduling/mpirun man page, so the actionable items are resolved and it can be closed. |
| [#11107](https://github.com/open-mpi/ompi/issues/11107) — List the available MPI variables in the documentation | Documentation | Request to document, in one place, the Open MPI environment variables (e.g., OMPI_COMM_WORLD_*, OMPI_UNIVERSE_SIZE) and MCA-parameter env equivalents, which were hard to find in the old website docs. | v5.0.x, v6.0.x, main | 🗑️ Close | The new Sphinx docs now include docs/tuning-apps/environment-var.rst listing OMPI_COMM_WORLD_*/OMPI_UNIVERSE_SIZE, and MCA params are covered via ompi_info and the tuning page, substantially addressing the core ask. Recommend close (could keep open only if a fully exhaustive grouped list is desired). |
| [#11359](https://github.com/open-mpi/ompi/issues/11359) — Documentation of global variables | Documentation | Feature request to add a documentation page/section (likely under section 7) describing MPI constants such as MPI_COMM_WORLD, MPI_STATUS_IGNORE, MPI_ANY_SOURCE, and the predefined datatypes, since they have no dedicated docs entry. | not implemented | ✅ Keep open | On current main there is still no dedicated Constants page or section in the man pages (no Constants heading; only Open-MPI.7.rst exists without a constants list); the desired page was never implemented and the discussion concluded it was blocked on upstream MPI-Forum Python constants support. Still a valid unimplemented docs request; keep open. (No constant doc page exists.) |

## Answered (1)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#13215](https://github.com/open-mpi/ompi/issues/13215) — How is PRTE's failure detector supposed to work? | Documentation | User asks why PRRTE heartbeat-based failure detection (errmgr_detector_heartbeat_period/timeout) does not detect link failures; maintainer explains there are no heartbeats and detection is via socket closure. | n/a | ✅ Keep open | The usage question was answered, but the maintainer explicitly stated the Open MPI docs are 'outdated and incorrect' and reference options that do not exist; the doc fix has not been made, so keep open as a documentation cleanup task (or convert to a docs issue). |

## Obsolete / moot (16)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#34](https://github.com/open-mpi/ompi/issues/34) — Add FAQ item re: flowchart of which send protocol is used | Documentation | Request to add an FAQ flowchart explaining how the openib/ob1 BTL chooses among eager/rendezvous/RDMA send protocols. | n/a | 🗑️ Close | The openib BTL it references has been removed entirely (replaced by UCX/uct) and the old FAQ has been migrated to Sphinx docs, so the specific openib protocol-flowchart request is obsolete; recommend closing. |
| [#77](https://github.com/open-mpi/ompi/issues/77) — Create FAQ entry for --preload-binary and --preload-files mpirun options | Documentation | Request to document the --preload-binary and --preload-files mpirun options, including path-resolution and file-placement rules. | n/a | 🗑️ Close | Both options are now documented in docs/man-openmpi/man1/mpirun.1.rst (the old web FAQ has been replaced by the modern docs); the doc request is effectively satisfied, recommend closing. |
| [#78](https://github.com/open-mpi/ompi/issues/78) — Update "amount of registered memory" FAQ item | Documentation | Request to update the OpenFabrics 'limiting registered memory usage' FAQ item that was written for the 1.2 era for the 1.3 series. | n/a | 🗑️ Close | The openib BTL has been removed from the tree (replaced by UCX) and the old web FAQ is gone, so this OpenFabrics-specific 1.3-era doc item is moot; recommend closing. |
| [#156](https://github.com/open-mpi/ompi/issues/156) — Remove OPAL_UNIQ from configury | Other | An RFC/refactor request to reconsider de-duplicating compiler/linker flags via OPAL_UNIQ, which was hacked around to support repeated -Xclang flags from clang/Homebrew. | n/a | 🗑️ Close | On current main OPAL_UNIQ is merely an alias for OAC_UNIQ (m4_copy) used in only a couple of probes, and the original -Xclang/Homebrew motivation is long obsolete. A decade-old build-system cleanup musing with no action; recommend closing. |
| [#284](https://github.com/open-mpi/ompi/issues/284) — FAQ needs core binding update | Documentation | The affinity/core-binding entries in the old www.open-mpi.org/faq tuning section were out of date relative to the then-current binding scheme. | n/a | 🗑️ Close | The legacy FAQ site has been retired and documentation migrated to docs.open-mpi.org, where current affinity/binding content lives in docs/tuning-apps/affinity.rst, so this specific FAQ-update request is moot. Recommend closing. |
| [#6366](https://github.com/open-mpi/ompi/issues/6366) — Unexpected behavior of DataType.getSize in Java | Documentation | The Open MPI Java binding Datatype.getSize() (and getExtent/getLb) returns a count of elements rather than bytes, inconsistent with MPI_TYPE_SIZE and with byte-based methods like createResized, plus misleading Javadoc. | feature removed | 🗑️ Close | The Open MPI Java MPI bindings were entirely removed from the tree (commit 3fe7451 'Remove the Open MPI Java MPI bindings'; no ompi/mpi/java directory on main), so this binding-specific inconsistency no longer exists. Recommend closing as moot. |
| [#6768](https://github.com/open-mpi/ompi/issues/6768) — hwloc symbol conflicts | Other | RFC/discussion proposing to rename Open MPI's external hwloc library and/or symbol-prefix it to avoid symbol clashes when other libraries (e.g. Mellanox hcoll) embed a different hwloc version. | n/a | 🗑️ Close | Mellanox confirmed they addressed the conflict in hcoll; the discussion stalled and the build/embedding model (internal hwloc slurped into libopen-pal, PMIx/PRRTE restructuring) has changed substantially since 2019. This is a stale design discussion with the root trigger resolved; close. |
| [#6831](https://github.com/open-mpi/ompi/issues/6831) — Coding Style | Other | Meta/discussion issue about removing tab characters from the code base, enforcing whitespace style, and adding a CI/git-hook check for tabs in PR diffs. | n/a | 🗑️ Close | A 2019 process/RFC discussion; the project has since moved to documented coding-style rules (no tabs, 4-space indents) captured in docs/developers and AGENTS.md/CLAUDE.md, and CI moved off Travis. The discussion is stale and largely superseded; close as obsolete. |
| [#7042](https://github.com/open-mpi/ompi/issues/7042) — Improve map-by, rank-by, bind-to docs | Documentation | Request to improve the mpirun map-by/rank-by/bind-to documentation, which users found confusing; also notes the old man page listed an incorrect default bind-to policy. | n/a | 🗑️ Close | The doc target was the legacy ORTE mpirun man page; in v5.0+ mpirun is an alias for PRRTE's prterun and jjhursey rewrote the mapping/ranking/binding docs upstream in PRRTE (openpmix/prrte#773). The original ORTE-era man page no longer exists in Open MPI, so this specific request is obsolete; close. |
| [#7347](https://github.com/open-mpi/ompi/issues/7347) — Trigger Mellanox CI with bot:[mellanox:]retest PR comments | Other | CI/infra task to investigate triggering the Azure-Pipelines-based Mellanox CI with legacy bot:retest PR comments. | n/a | 🗑️ Close | Microsoft confirmed Azure Pipelines did not support custom PR-comment triggers; the Azure-based Mellanox CI has since been retired/replaced, making this infra task obsolete, so close. |
| [#7686](https://github.com/open-mpi/ompi/issues/7686) — Cleanup PGI Compiler Warnings | Other | Tracking issue to sweep up assorted PGI/NVHPC compiler warnings in OMPI, embedded PMIx atomics, hwloc, and PRRTE on ppc64le. | n/a | 🗑️ Close | Most warnings cited come from embedded upstreams (OpenPMIx/hwloc/PRRTE), which are now updated submodules not hand-edited here, and the remaining cleanup was largely done; the underlying compiler (PGI/classic) is effectively retired. Open-ended cleanup tracker stale since 2021; recommend closing as obsolete. |
| [#9397](https://github.com/open-mpi/ompi/issues/9397) — v4.0.x and v4.1.x: document that --cpu-set doesn't work | Documentation | Task to document in v4.0.x and v4.1.x that --cpu-set accidentally behaves like --cpu-list (the correct behavior was deferred to v5.0.0). The volunteer ultimately un-volunteered. | EOL versions only | 🗑️ Close | This was a doc task scoped specifically to v4.0.x and v4.1.x, both now EOL; main/v5.0.x use the PRRTE mpirun where --cpu-set is documented and works correctly. The remaining work is moot. Recommend closing. |
| [#10868](https://github.com/open-mpi/ompi/issues/10868) — Check to avoid duplicate sorting of arrays passed to PMIx | Other | A note that PMIx v4.2.2+ now sorts proc arrays internally, so OMPI no longer needs to pre-sort arrays before calling PMIx functions like PMIx_Fence/PMIx_Connect; a possible minor optimization to remove redundant sorting. | n/a | 🗑️ Close | This is a stale (2022) minor optimization/cleanup note from rhc54 with no activity; the redundant sort is at most a negligible performance nicety against old PMIx versions OMPI no longer ships, so it is effectively moot and can be closed. |
| [#11184](https://github.com/open-mpi/ompi/issues/11184) — osc/ucx: optimizations | Other | A short developer to-do list of two follow-up optimizations for osc/ucx flush handling (store rank with base/tail to avoid false-positive flushes; reset entries in the completion callback instead of after a flush). | n/a | 🗑️ Close | This is a tracking/enhancement note tied to PR #11178 (now merged); it is an internal optimization wishlist with no further activity since 2022 and no indication the follow-ups were ever required. Recommend closing as a stale, low-value tracking item. |
| [#13078](https://github.com/open-mpi/ompi/issues/13078) — v4.1.9 release checklist | Other | A meta/tracking checklist issue enumerating the steps to build, publish, and announce the Open MPI v4.1.9 release. | n/a | 🗑️ Close | This is a release-process tracking issue for the v4.1.x branch, which is at or past end-of-life; such checklists are routinely closed once the release ships or the series winds down. Recommend closing as a stale tracking issue. |
| [#8016](https://github.com/open-mpi/ompi/issues/8016) — mpirun --help: --tune segment vs --am segment | Documentation | User finds the --tune option misplaced in mpirun --help output (it appears under a different segment than the related --am aggregate-MCA option) in v4.0.4. | n/a | ❓ Needs info | This concerns the ORTE-era mpirun --help layout in v4.0.x; v5+ uses PRRTE with completely revamped help text and option grouping, so the specific complaint is likely obsolete. No comments and unverifiable against current help; ping to confirm whether it still applies on a current release, else close. |

## Wontfix / out of scope (2)

| Issue | Type | Summary | Relevance | Recommend | Why still open / notes |
|-------|------|---------|-----------|-----------|------------------------|
| [#97](https://github.com/open-mpi/ompi/issues/97) — Add FAQ entries about non-MPI performance issues | Documentation | Suggestion to add FAQ entries discussing general HPC performance tuning differences between MPI implementations (wrapper opt flags, default binding, malloc behavior). | n/a | 🗑️ Close | The original creator already flagged this as a slippery-slope of generic HPC tuning guidance not inherent to MPI; no work in 14+ years and the FAQ has been superseded by the new docs site, recommend closing. |
| [#11949](https://github.com/open-mpi/ompi/issues/11949) — ob1 pml with smcuda btl doesn't work with slurm gpu binding | Documentation | With Slurm GPU binding (cgroups ConstrainDevices), cuIpcOpenMemHandle fails and smcuda falls back to host-buffered copies, emitting warnings; the same is observed across MPI implementations. | all (Slurm cgroup behavior) | 🗑️ Close | Root cause is Slurm cgroup device isolation preventing CUDA IPC (SchedMD tickets opened); consensus is this affects all MPI implementations and the only Open MPI action is documentation/clearer warnings, so close as effectively out of scope/documentation. |
