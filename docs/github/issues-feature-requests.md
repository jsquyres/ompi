# Open feature-request issues older than 6 months

Triage of open `open-mpi/ompi` issues opened more than 6 months ago (created before 2025-12-20), as of 2026-06-20. Classification and status are a **reasoned determination** from each issue's text, discussion, labels, and linked/merged PRs, plus spot-checks of current `main` — not a per-branch build-and-reproduce test. Treat "Close" as *recommended for maintainer confirmation*, not automatic.

Issues are grouped by **why they are still open**. See [issues-overview.md](issues-overview.md) for totals and cross-tabs, and the companion files: [bugs](issues-bugs.md) · [feature requests](issues-feature-requests.md) · [questions](issues-questions.md) · [other](issues-other.md).

**Relevance** = whether/where the requested capability now exists.


**71 issues** — 🗑️ Close 31 · ❓ Needs info 5 · ✅ Keep open 35.


## Unresolved, still valid (39)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#139](https://github.com/open-mpi/ompi/issues/139) — Use new Autoconf 2.69 Fortran m4 macros | Suggestion to adopt new AC 2.69 Fortran macros (AC_FC_MODULE_EXTENSION/FLAG/OUTPUT_FLAG, AC_FC_PP_SRCEXT/DEFINE, AC_FC_IMPLICIT_NONE) once the minimum Autoconf requirement is raised. | not implemented | 🗑️ Close | configure.ac still declares AC_PREREQ(2.60) and none of the suggested AC_FC_* macros appear in config/ or configure.ac, so this cleanup was never done; low value internal build refactor, recommend closing as stale (or needs-info). |
| [#4423](https://github.com/open-mpi/ompi/issues/4423) — Improve OPAL_CHECK_PACKAGE | Enhancement idea to make the OPAL_CHECK_PACKAGE m4 macro more robust by testing libraries via absolute paths so the configure-time library matches the build-time library. | not implemented | 🗑️ Close | A speculative enhancement (labeled enhancement/help wanted) with no comments and no follow-up since 2017; the configure macro machinery has since evolved with oac. Low value, no champion, no clear current defect; recommend closing as stale/out of scope. |
| [#4675](https://github.com/open-mpi/ompi/issues/4675) — MPI_PROC_NULL in Topology Creation | Request to allow MPI_PROC_NULL as a neighbor in MPI_Dist_graph_create_adjacent so generic graph topologies can have placeholder neighbors like non-periodic cartesian topologies. | not implemented | 🗑️ Close | This is fundamentally an MPI standardization question (Open MPI rejects MPI_PROC_NULL per the current spec wording); a trial PR #4898 was opened and the reporter took it to the MPI Forum. Resolution depends on the standard, not Open MPI; the experimental PR did not land. Recommend closing as a forum/standard matter unless the standard mandates it. |
| [#140](https://github.com/open-mpi/ompi/issues/140) — Detect / ignore multi-address IP devices | The TCP BTL does not support multi-address (multiple IPs on one interface) IP devices; request to detect, warn about, and safely ignore such devices. | not implemented | ❓ Needs info | No multi-address detection/warning logic was found in opal/mca/btl/tcp on current main, so the enhancement appears unimplemented; still potentially valid but very low recent interest, recommend keep open or needs-info. |
| [#5172](https://github.com/open-mpi/ompi/issues/5172) — Need a way to limit vma tree size (specifically for rgpusm if that's possible) | Requests restoring runtime MCA parameters (rcache_base_vma_tree_items_min/max) to bound rcache VMA tree size and curb GPU memory growth from stale cuIpcMemHandles in smcuda. | not implemented (runtime rcache vma_tree_items_min/max not exposed) | ❓ Needs info | hjelmn agreed to look but no fix landed; mca_rcache_base_vma_tree_items_min/max are only declared in base.h with no MCA registration in the current tree, so the runtime control still does not exist. Reported against EOL 2.0.x/3.0.x and inactive since 2018; ping reporter and close if stale. |
| [#6494](https://github.com/open-mpi/ompi/issues/6494) — Reproducible Linear Algebra through posit-enabled MPI reductions | Request/research-collaboration proposal to add posit-arithmetic MPI reduction operations to enable bitwise-reproducible reductions independent of process count/ordering. | not implemented | ❓ Needs info | No posit reduction support exists in the tree; this was framed as a research collaboration and a maintainer offered to discuss privately in 2019 with no follow-up implementation. Niche, unimplemented feature with stalled discussion; ping reporter and close if no continued interest. |
| [#24](https://github.com/open-mpi/ompi/issues/24) — Implement MPIX_GREQUEST_START | Request to expose MPIX_Grequest_start (extended generalized requests with a user-supplied progress/poll function) as a user-facing MPIX API. | not implemented (internal ompi_grequestx_* exists; no user-facing MPIX API) | ✅ Keep open | The internal infrastructure exists in ompi/request/grequestx.h (ompi_grequestx_start, guarded by --enable-grequest-extensions), but grep finds no user-facing MPIX_Grequest_start symbol anywhere, so the requested public API is still not implemented; devreal expressed willingness to add it, keeping it valid. |
| [#32](https://github.com/open-mpi/ompi/issues/32) — Add support for blocking progress | Placeholder/tracking ticket from a 2008 meeting for adding blocking-progress support to the progress engine so idle processes need not busy-poll. | not implemented (partial: opal_progress event poll rate exists) | ✅ Keep open | opal_progress has event-poll-rate tuning but still no true blocking-progress mode; the issue is a 2008 placeholder with no concrete implementation, so it is still technically valid though effectively a stale design wishlist item mislabeled as a bug. |
| [#53](https://github.com/open-mpi/ompi/issues/53) — Debugging output directory of useful environment information | Enhancement to dump a directory of diagnostics (stacktraces, env vars, config.log, OS/arch, mpirun command line, aggregated per-process output) on error in a production build to aid debugging. | not implemented | ✅ Keep open | No evidence such a consolidated crash-diagnostics dump feature was implemented; it remains an unrealized 2008 enhancement, so still technically valid though long dormant. |
| [#59](https://github.com/open-mpi/ompi/issues/59) — Aggregate stack trace messages | Enhancement to aggregate per-process stack traces on a crash without calling malloc() in the failing MPI process (since malloc may itself fail during memory errors). | not implemented | ✅ Keep open | No malloc-free stack-trace aggregation mechanism was identified; this remains an unimplemented 2008 wishlist item, still valid but dormant. |
| [#60](https://github.com/open-mpi/ompi/issues/60) — Enhance error messages in MPI_ABORT sequence | Enhancement to include more context (e.g. message tag, possibly a stack trace) in the centralized error/abort reporting so users can locate where in their app the error occurred. | not implemented | ✅ Keep open | An enhancement to enrich abort-sequence error messages; no specific implementation found and it was last just re-milestoned to 3.x in 2017, so it remains an open, still-valid enhancement that has stalled. |
| [#1403](https://github.com/open-mpi/ompi/issues/1403) — "make check" can't run any tests that use components (without installing first) | Proposal to make 'make check' stronger by running component-dependent tests when Open MPI is installed/reachable or built with --disable-dlopen. | not implemented | ✅ Keep open | An enhancement to the test harness; no evidence it was systematically implemented. Genuine standing improvement idea, so keep open (low priority). |
| [#1441](https://github.com/open-mpi/ompi/issues/1441) — Update usnic BTL to handle unknown senders | Enhancement to make the usnic BTL handle unknown senders via opal_proc_for_name/btl_add_procs so it can drop MCA_BTL_FLAGS_SINGLE_ADD_PROCS. | main | ✅ Keep open | The usnic BTL still exists and still sets MCA_BTL_FLAGS_SINGLE_ADD_PROCS in opal/mca/btl/usnic/btl_usnic_module.c, so the enhancement was never done; still valid but a niche/low-priority improvement (could also be closed as stale). |
| [#2056](https://github.com/open-mpi/ompi/issues/2056) — [feature request] packaging for multiple Fortran compilers | Request to build Open MPI Fortran bindings/modules for several Fortran compilers from a single C-library build, instead of rebuilding the whole library per toolchain. | not implemented | ✅ Keep open | Only a proof-of-concept (PR #2070) was ever made and it still required two full builds; no single-build multi-Fortran-compiler support has landed in the tree. The enhancement remains unimplemented, so keep open. |
| [#7123](https://github.com/open-mpi/ompi/issues/7123) — Detect when running in a containerized environment | Enhancement request for mpirun to detect when it is running inside a container so it can adapt default MCA parameters (e.g., plm_rsh_chdir) and add consistency checks, possibly as a new schizo component. | not implemented | ✅ Keep open | This is an open-ended enhancement (labeled enhancement/help-wanted) about container detection, which involves PRRTE's schizo/runtime layer; there is no indication a general container-detection mechanism was implemented. Still a valid, unimplemented feature request; keep open (a maintainer may reasonably retire it as low-priority). |
| [#7381](https://github.com/open-mpi/ompi/issues/7381) — OpenMPI OSHMEM 4.0.2 missing some pshmem functions | OSHMEM lacks profiling-layer entry points pshmem_info_get_version and pshmem_info_get_name, forcing profiling tools to special-case OSHMEM. | not implemented | ✅ Keep open | Confirmed on main: oshmem defines shmem_info_get_version/shmem_info_get_name but no pshmem_ variants and has no profile entries for them, so the gap is still valid; labeled Target main/v5.0.x, keep open. |
| [#7766](https://github.com/open-mpi/ompi/issues/7766) — Option to enable two level namespace on Mac OS | On macOS, Open MPI's build forces -Wl,-flat_namespace into the link/wrapper flags, which breaks two-level-namespace symbol resolution (e.g. mixed Clang C++/GFortran unwind, RTLD_DEEPBIND-like behavior); request a configure option to enable two-level namespace. | not implemented | ✅ Keep open | config/ompi_setup_fc.m4 on main still unconditionally adds -Wl,-flat_namespace for the Fortran wrapper with no opt-out for two-level namespace, so the requested option still does not exist. Legitimate, still-valid macOS build enhancement; keep open. |
| [#7900](https://github.com/open-mpi/ompi/issues/7900) — Terminal size under OpenMPI | MPI processes launched via mpirun report a terminal size of 0 columns/0 lines (vs 80x24 under MPICH), breaking code that formats output by terminal width. | not implemented | ✅ Keep open | Maintainers labeled it a feature request and judged it a non-trivial job (propagate ioctl size through all launch back-ends, now PRRTE, plus resize handling); not implemented. Genuine, still-valid request, so keep open. |
| [#8439](https://github.com/open-mpi/ompi/issues/8439) — Detect (and ignore) stale Open MPI components | Enhancement to embed a version/configure signature in component DSOs so the MCA base can detect and safely ignore stale components left over from a previous Open MPI install. | not implemented | ✅ Keep open | Labeled enhancement; the MCA base still only checks major/minor MCA version (no per-build signature), so the requested capability does not exist on main. Recommend keeping open as a still-valid, low-priority feature request (or closing as wontfix at maintainer discretion). |
| [#9022](https://github.com/open-mpi/ompi/issues/9022) — Need a way to disable use of -mcx16 compile flag | Distro builds compiled on cx16-capable x86 hardware emit CMPXCHG16B via the configure-detected -mcx16 flag and crash with SIGILL on older CPUs (e.g. Celeron N4000); requests a way to disable -mcx16 for portable packaging. | not implemented (--disable-cx16-atomics PR #9023 never merged) | ✅ Keep open | config/opal_config_asm.m4 on main still unconditionally tries -mcx16 with no --disable-cx16-atomics option; the proposed PR #9023 was never merged. The build-host run test does not help the build-here-run-there distro case, so the request stands, though it is a fading concern as 5.x/6.x default to builtin atomics. |
| [#9050](https://github.com/open-mpi/ompi/issues/9050) — OMPI_{CC,CXX}=mpi{cc,cxx} is a fork-bomb | Setting OMPI_CC=mpicc (or OMPI_CXX=mpicxx) makes the compiler wrapper recursively spawn itself unbounded, fork-bombing the machine; reporter requests a simple env-var recursion guard in the wrapper. | not implemented | ✅ Keep open | No recursion check exists in opal/tools/wrappers on main (grep for RECURSION found nothing); the suggested guard was never added. Low-risk, still-valid hardening request; keep open. |
| [#9461](https://github.com/open-mpi/ompi/issues/9461) — pml/ucx: support for MPI 4.0 assertions? | Request to add support in pml/ucx for the MPI-4.0 communicator info assertions (mpi_assert_allow_overtaking, mpi_assert_no_any_tag, etc.), which pml/ob1 already partially supports. | not implemented | ✅ Keep open | A maintainer pinged the UCX team but no answer or implementation followed; this is a legitimate still-open enhancement for pml/ucx. Recommend keeping open (could be re-pinged), though it has been dormant since 2022. |
| [#9682](https://github.com/open-mpi/ompi/issues/9682) — TCP interface specification/selection | Design discussion proposing a single all-encompassing MCA parameter (e.g. ip_if_include/tcp_if_include) to set IP interface selection defaults across all OMPI/PRRTE/PMIx TCP subsystems instead of forcing users to set btl_tcp/oob_tcp/ptl_tcp separately. | not implemented | ✅ Keep open | Consensus was to add a unified IP-interface MCA param plus FAQ documentation, but no such all-encompassing parameter appears to have been implemented. Recommend keeping open as a still-valid enhancement, or closing if the team deems it stale. |
| [#10110](https://github.com/open-mpi/ompi/issues/10110) — Comm_spawn/accept using inefficient (and frail) rendezvous mechanism | RFC by rhc54 to rewrite the OMPI dpm comm_spawn/connect-accept rendezvous to use scalable PMIx group functions instead of the older publish/lookup mechanism. | not implemented | ✅ Keep open | ompi/dpm/dpm.c on main still uses the publish/lookup + PMIx_Connect/PMIx_Fence rendezvous (no PMIx_Group_construct), so the requested enhancement is not implemented; it is a still-valid architectural improvement request. |
| [#10438](https://github.com/open-mpi/ompi/issues/10438) — Improvements and fixes for coll/HAN | Proposes coll/HAN usability improvements: select sub-communicator components by name instead of numeric IDs, add global module MCA params, unify comm-create paths, and fix the dynamic path. | partially implemented on main | ✅ Keep open | An enhancement umbrella for coll/HAN; some pieces were addressed (e.g. PR 10456 and the 8248 fix) but the by-name selection, global params, and dynamic-path unification were not fully implemented. Keep open as a still-valid enhancement tracker unless maintainers consider it superseded. |
| [#10560](https://github.com/open-mpi/ompi/issues/10560) — Make `--enable-wrapper-rpath --disable-wrapper-runpath` pass `-Wl,--disable-new-dtags` when supported | Request that disabling wrapper runpath while enabling rpath cause the wrappers to emit -Wl,--disable-new-dtags to force RPATH instead of relying on the compiler default (RUNPATH). | not implemented | ✅ Keep open | config/opal_setup_wrappers.m4 on main only emits -Wl,--enable-new-dtags when runpath is enabled and never emits --disable-new-dtags, and the proposed --with-wrapper-runpath trinary design was never implemented, so the feature does not exist. Keep open as a valid, unimplemented enhancement. |
| [#10871](https://github.com/open-mpi/ompi/issues/10871) — silent performance degradation when cuda_ipc is not possible | When CUDA-aware MPI runs but cuda_ipc is silently disabled (e.g. due to CUDA_VISIBLE_DEVICES or managed memory), intra-node GPU performance can halve with no warning; reporter requests documentation, a warning, and/or an MPIX query for whether cuda_ipc is active. | not implemented | ✅ Keep open | An enhancement request (warning/docs/query API) with no comments or implementation; nothing in the tree provides this notification, so the request remains unaddressed and still reasonable, recommend keeping open as a usability/diagnostics feature. |
| [#11290](https://github.com/open-mpi/ompi/issues/11290) — v6.x: Remove internal hwloc and libevent | Plan to drop the bundled/internal copies of hwloc and libevent in Open MPI v6.x, since both are widely available inbox on Linux and via Homebrew/MacPorts on macOS. | not implemented | ✅ Keep open | On current main (VERSION major=6), 3rd-party/ still ships hwloc-2.7.1.tar.gz and libevent-2.1.12-stable-ompi.tar.gz and opal/mca/hwloc still has the internal component; the removal has not happened. Tagged Target: v6.0.x and still valid, so keep open. |
| [#11696](https://github.com/open-mpi/ompi/issues/11696) — Accelerator aware (OFI) NIC selection | Feature request to have Open MPI select the NIC nearest the chosen GPU (same PCIe root complex) for GPUDirect RDMA correctness and performance, ideally opt-in via an MCA parameter. | partially implemented; broader solution not implemented | ✅ Keep open | Extensive design discussion converged on an opt-in OFI NIC-selection approach (related PR #11687), but no clear evidence the general PCIe-root-aware solution was completed and merged; treat as still-open enhancement targeting main. |
| [#11715](https://github.com/open-mpi/ompi/issues/11715) — API Call to find OpenMPI IO Backend | Request for a runtime API to query which MPI-IO backend (ompio vs romio) is in use for a file, ideally via an MPI_File_get_info info key. | not implemented | ✅ Keep open | Maintainers agreed an Open MPI-specific info key on MPI_File (queryable via MPI_File_get_info) was the right approach and a maintainer said he could implement it, but there is no evidence it was merged. A dlopen/dladdr hack was offered as interim. Keep open as an unimplemented enhancement. |
| [#12078](https://github.com/open-mpi/ompi/issues/12078) — MPI 4.1 - recognizing mpi_assert_strict_persistent_collective_ordering would be nice | Tracking item to recognize the MPI 4.1 info assertion key mpi_assert_strict_persistent_collective_ordering for persistent collectives. | not implemented | ✅ Keep open | Grep of current main finds no reference to mpi_assert_strict_persistent_collective_ordering, so this MPI 4.1 enhancement is still unimplemented. Recommend Keep open as an MPI 4.1 tracking item. |
| [#12086](https://github.com/open-mpi/ompi/issues/12086) — MPI 4.1 - Allow MPI_WIN_SHARED_QUERY on created and allocated windows | Tracking item for the MPI 4.1 change that allows MPI_Win_shared_query on windows created by MPI_Win_create and MPI_Win_allocate, not only allocate_shared. | partially implemented on main (rdma/ubcl osc) | ✅ Keep open | The rdma osc component now supports shared_query for MPI_WIN_FLAVOR_ALLOCATE windows and ubcl notes the MPI 4.1 no-abort behavior, so this is partly done, but full coverage across components/flavors is unconfirmed. Recommend Keep open pending maintainer confirmation that the MPI 4.1 requirement is fully met. |
| [#12087](https://github.com/open-mpi/ompi/issues/12087) — MPI 4.1 - may want to recognize new RMA info key mpi_accumulate_granularity | Tracking item to recognize the MPI 4.1 RMA info key mpi_accumulate_granularity. | not implemented | ✅ Keep open | Grep of current main finds no reference to accumulate_granularity, so this MPI 4.1 info-key feature is still unimplemented. Recommend Keep open as an MPI 4.1 tracking item. |
| [#12279](https://github.com/open-mpi/ompi/issues/12279) — Expand CUDA support and fix documentation to account for all cuda dependent components | With CUDA-aware build using --enable-mca-dso, libmpi still depended on libcudart unless io-romio341 was also made a DSO; requests automatic handling plus doc updates and a CUDA-check note about needing MPI_Init. | not implemented (workaround exists; docs gap) | ✅ Keep open | Workaround (add io-romio341 to mca-dso list) confirmed working, but the requested automatic handling and documentation fix were left open and labeled enhancement/documentation; no PR merged, keep open as a valid enhancement/doc task. |
| [#12286](https://github.com/open-mpi/ompi/issues/12286) — Slow MPI_Group_difference | MPI_Group_difference (via ompi_group_dense_overlap) is O(n^2) and extremely slow for groups containing all processes of a >14000-rank job; requests a better algorithm e.g. using sorted lists. | not implemented (v5.0.x, main) | ✅ Keep open | Maintainer declined to backport to v4.1.x but the performance improvement itself was never implemented on main/v5.0.x; a legitimate, still-open enhancement request, so keep open. |
| [#12602](https://github.com/open-mpi/ompi/issues/12602) — coll tuned alltoall algorithm ignored after initialization | The coll_tuned_alltoall_algorithm MCA value is captured/cached at communicator-creation (module init) time, so it cannot be changed per-message via MPI_T based on message size as the user wants. | not implemented | ✅ Keep open | Confirmed on main: the forced algorithm is installed via COLL_TUNED_EXECUTE_IF_DYNAMIC at module init and the dynamic decision reads tuned_module->user_forced[ALLTOALL] rather than re-querying the MCA var, so runtime MPI_T changes are still ignored. Valid enhancement request, not implemented; keep open. |
| [#12646](https://github.com/open-mpi/ompi/issues/12646) — Reduce collectives latency by reusing temporary buffers | Proposal to add a reusable/cached temporary buffer pool for collectives to avoid repeated memory registration and malloc/free overhead on the critical path. | not implemented (partial PR #13782) | ✅ Keep open | A partial-fix PR (#13782) was referenced in 2026 but the general bufpool feature is not fully implemented; this is a still-valid enhancement targeted at main. |
| [#13383](https://github.com/open-mpi/ompi/issues/13383) — Allow use of OFI BTL with RXM | Requests re-evaluating the explicit exclusion of the libfabric RXM provider from the OFI BTL (excluded for lacking real FI_DELIVERY_COMPLETE), which currently prevents shared-memory use in RXM/verbs MTL setups. | not implemented | ✅ Keep open | The exclusion (commit 41acfee2) is a deliberate correctness guard; the request to reconsider it is a valid open enhancement/discussion with no maintainer response yet, so keep open pending a libfabric/OFI BTL decision. |
| [#13525](https://github.com/open-mpi/ompi/issues/13525) — Feature: Provide network hardware counter summary at completion time | Requests a feature (like Cray MPI) to sample NIC hardware performance counters at MPI_Init and report deltas/summaries at MPI_Finalize. | not yet merged (PR #13830 open) | ✅ Keep open | Reporter filed PR #13830 (2026-04-20) to implement this; the feature is not yet in main. Keep open until the PR is reviewed and merged (then close). |

## Feature now exists (9)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#39](https://github.com/open-mpi/ompi/issues/39) — mca_base_open() "none" and "all" options | RFC to add reserved keywords "none" and "all" (and "any") to MCA framework component selection so a framework can be told to open no components or any/all components. | exists on main | 🗑️ Close | opal/mca/base/mca_base_components_open.c now recognizes "all"/"none" keywords and the modern MCA selection supports negation/explicit include lists, so the requested capability is present; this 2008 RFC is effectively satisfied and can be closed. |
| [#475](https://github.com/open-mpi/ompi/issues/475) — No protection for opening plugins from wrong OMPI version | Loading an incompatible plugin .so from a different installed Open MPI version (e.g. via in-tree tests with an OS-installed Open MPI) caused a segfault in MCA var registration; the request was to skip/validate mismatched plugins. | v4.0.x+, main | 🗑️ Close | mca_base_component_repository.c now checks the MCA major/minor version of each loaded component and skips mismatched ones (lines 491-500), providing the requested protection; only a finer project-version check remains as a noted TODO. Recommend closing. |
| [#2168](https://github.com/open-mpi/ompi/issues/2168) — TCP BTL does not do a good job of determining if peer interfaces can connect | Request that the TCP BTL consult the OS routing table (e.g., libnl) instead of heuristics to decide whether peer interfaces can actually reach each other. | v4.0.x, v4.1.x, v5.0.x, v6.0.x, main | 🗑️ Close | An opal reachable framework with a netlink component (opal_reachable_netlink_rt_lookup) now exists, and btl/tcp_proc.c calls opal_reachable.reachable() for interface pairing, implementing the requested OS-routing-table consultation. The capability exists, so close (verify the noted multilink follow-up is acceptable). |
| [#3176](https://github.com/open-mpi/ompi/issues/3176) — Adjust rcache framework to allow hooking madvise properly | Placeholder to re-architect the rcache/memory framework so madvise(2) could be hooked safely, which was previously not possible. | v5.0.x, v6.0.x, main | 🗑️ Close | The memory patcher component now defines intercept_madvise and calls patch_symbol("madvise", ...), so madvise is hooked; the requested capability exists, recommend Close. |
| [#8928](https://github.com/open-mpi/ompi/issues/8928) — Open MPI OFI transport does not support all OFI transports | Request for the OFI (libfabric) transport to support the larger 64-byte FI_CONTEXT2 structure (used by providers like the IBM Blue Gene provider) in addition to the 32-byte FI_CONTEXT. | v5.0.x, v6.0.x, main | 🗑️ Close | The mtl/ofi component now declares struct fi_context2 ctx in mtl_ofi_request.h and sets hints->mode = FI_CONTEXT \| FI_CONTEXT2 in mtl_ofi_component.c, so FI_CONTEXT2 is supported on current branches and the request is implemented; close. |
| [#9194](https://github.com/open-mpi/ompi/issues/9194) — MPI 4.0 - Need to implement embiggening feature | Tracking issue to implement the MPI-4.0 large-count (embiggening) feature: the MPI_*_c C functions, mpi_f08 overloads, large-count callbacks, and MPI_ERR_VALUE_TOO_LARGE. | implemented on v5.0.x/v6.0.x/main | 🗑️ Close | MPI_ERR_VALUE_TOO_LARGE is defined in ompi/include/mpi.h.in and the bindings generator produces the large-count _c variants on main; the embiggening feature was implemented for the v5.0 release. Recommend closing as the feature now exists. |
| [#12010](https://github.com/open-mpi/ompi/issues/12010) — Implement the MPI_T_BIND_MPI_SESSION bind type for use in CV/PV/Event handles. | Request to add the MPI 4.0 MPI_T_BIND_MPI_SESSION binding-type enum value for MPI_T control/performance variable and event handles for standard compliance. | main (MPI_T_BIND_MPI_SESSION enum present in mpi.h.in) | 🗑️ Close | MPI_T_BIND_MPI_SESSION now appears in ompi/include/mpi.h.in's MPIT object-binding enum (added to compile the MPICH test suite), so the requested enum value exists; close, or verify a session-bound variable is actually registered if deeper compliance is desired. |
| [#13153](https://github.com/open-mpi/ompi/issues/13153) — ompi and opal datatype infrastructure should support MPI big count API | Tracking request to add MPI big count support (e.g. MPI_Type_vector_c) to the ompi_datatype/opal datatype infrastructure. | implemented on main/v6.0.x | 🗑️ Close | Local checkout shows datatype creation functions now use size_t counts and the 'Big count support for datatypes' commit (76b3db57fb, merged 2026-04-25) embiggened ompi_datatype_get_args and the create routines; the requested infrastructure now exists, so recommend Close. |
| [#13399](https://github.com/open-mpi/ompi/issues/13399) — Variable forwarding in case of spawn not working for children | Requested that env vars set via -x/--unset-env etc. be forwarded to MPI_Comm_spawn'd children, consistent with --fwd-environment. | exists in PRRTE master (future OMPI release) | 🗑️ Close | Fixed in PRRTE via PRs 2266/2268/2279 (and PMIx 3681) adding inherit/set-env support; reporter confirmed it works and said 'the ticket can be closed.' Remaining cmd-line ordering nit was declared 'good enough' by maintainer. Recommend close. |

## Obsolete / moot (6)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#146](https://github.com/open-mpi/ompi/issues/146) — Make builds with VT default to using stlport4 when compiled with Oracle Studio compilers | Request that configure auto-add -library=stlport4 to CXXFLAGS when building VampirTrace with Oracle Studio C++ compilers to avoid the broken-STL warning. | not implemented | 🗑️ Close | VampirTrace was removed from Open MPI entirely (no vampir/stlport references remain in the tree), so this enhancement is moot; recommend closing. |
| [#3470](https://github.com/open-mpi/ompi/issues/3470) — Extend SLURM integration | Request to extend the SLURM RAS component (via popen) to obtain ThreadsPerCore/core count, since only slot count was available from the environment on nativized SLURM/Cray. | not implemented | 🗑️ Close | RAS/SLURM allocation logic moved out of OMPI into the PRRTE submodule years ago, so this OMPI-side RAS enhancement is no longer actionable here; the KNL/Cray context is also long obsolete. Recommend Close. |
| [#5949](https://github.com/open-mpi/ompi/issues/5949) — Warn in configury that the TKR 'mpi' module is deprecated | Requests a configure/ompi_info deprecation warning for the old non-ignore-TKR Fortran 'mpi' module, since it could not yet be removed due to old compilers (NAG, gcc 4.8.5). | obsolete (non-ignore-TKR mpi module fully removed) | 🗑️ Close | Open MPI now requires ignore-TKR support (configury errors/warns and only builds use-mpi-ignore-tkr; the use-mpi-tkr directory is gone), so the legacy TKR module was removed outright rather than merely deprecated, making the warning request moot. Recommend closing. |
| [#7285](https://github.com/open-mpi/ompi/issues/7285) — Make ompi_info also show PRRTE MCA params | Enhancement to have ompi_info display PRRTE (and possibly PMIx) MCA parameters since post-split ompi_info only shows OMPI params. | not implemented | 🗑️ Close | ompi_info source on main does not display PRRTE/PMIx params; instead the v5 design provides separate prte_info/pmix_info tools and --prtemca/--pmixmca flags, so the original proposal is effectively superseded/moot and can be closed. |
| [#10259](https://github.com/open-mpi/ompi/issues/10259) — Make ompi_mpi_java_eager MCA var show up in ompi_info | Request to expose the ompi_mpi_java_eager MCA variable (registered in libmpi_java during Java MPI_Init) in ompi_info without adding a libmpi to libmpi_java dependency. | moot (Java removed) | 🗑️ Close | Java bindings have been removed from main (no ompi/mpi/java, no ompi_mpi_java_eager references), so this enhancement is moot; close. |
| [#13140](https://github.com/open-mpi/ompi/issues/13140) — Adding another example for testing connectivity | Contributor offered to add a Java version of the connectivity example program to examples/, and a maintainer agreed it would be a nice addition. | not implemented (Java bindings removed) | 🗑️ Close | The experimental Java MPI bindings were removed from the tree (ompi/mpi/java no longer exists; recent remove-java merge), so a Java connectivity example is no longer applicable. Recommend closing as moot. |

## Wontfix / out of scope (17)

| Issue | Summary | Relevance | Recommend | Why still open / notes |
|-------|---------|-----------|-----------|------------------------|
| [#182](https://github.com/open-mpi/ompi/issues/182) — incorporate collective debugger extension for padb? | Request to incorporate (a variant of) the padb --deadlock collective-debugger patch so users do not have to keep porting it to new Open MPI versions. | not implemented | 🗑️ Close | padb is an effectively abandoned tool and this 2013 enhancement was never picked up; the MPIR/debugger ecosystem has moved on. No interest in 12+ years; recommend closing as out of scope. |
| [#1544](https://github.com/open-mpi/ompi/issues/1544) — Have constants available in shared library | Request (from Julia MPI.jl maintainer) to export MPI constant values (like HDF5's *_g symbols) as data-section symbols in the shared library for easy run-time binding generation. | not implemented | 🗑️ Close | In Open MPI, C handles like MPI_INT are opaque pointers (not compile-time constants), so they cannot be trivially exposed as data symbols; maintainers suggested the existing configure-probe approach. Not implemented and effectively declined; recommend close. |
| [#3954](https://github.com/open-mpi/ompi/issues/3954) — Fortran/C PMPI profiling wrappers | Request/discussion to optionally have Fortran MPI bindings call the C MPI_ entry points (instead of PMPI_) so that profiling tools wrapping only C calls also intercept Fortran calls. | not implemented | 🗑️ Close | The maintainer consensus (multiple members) was strongly against this as the correct way to intercept Fortran is in Fortran; an MCA/configure scheme was floated but never implemented and no PR materialized. The bindings have since been completely regenerated by the Python generator, making the old design moot; recommend closing as out of scope. |
| [#7472](https://github.com/open-mpi/ompi/issues/7472) — Delete smcuda BTL and put CUDA functionality in vader BTL | Long-term plan to remove the separate smcuda BTL and fold CUDA support directly into the vader (now sm) BTL. | not implemented | 🗑️ Close | On main the BTL was renamed vader->sm, but smcuda still exists as a separate component and CUDA was not folded into sm; the project moved to the accelerator framework instead. The original RFC plan was not pursued, so close (or relabel) as superseded. |
| [#8967](https://github.com/open-mpi/ompi/issues/8967) — Open MPI on bonded interface issues | TCP BTL drops connections / fails at scale when nodes have multiple IPs/subnets on a single physical interface; reporter requests an enhancement so Open MPI's interface matching supports multiple IPs per interface. | not implemented | 🗑️ Close | Maintainer (jsquyres) explained this multi-IP-per-interface case is not well supported and would require reworking interface detection in several places; the discussion ended with a feature-enhancement request that nobody picked up. Workaround (one IP per interface) exists; recommend closing as out of scope unless someone champions the feature. |
| [#9575](https://github.com/open-mpi/ompi/issues/9575) — OFI MTL and BTL consume double endpoints / hardware resources | Using OFI for both the MTL (point-to-point) and BTL (one-sided) causes applications to allocate twice the endpoints/hardware resources because the two components manage independent OFI endpoints; proposal was to share endpoints. | not implemented | 🗑️ Close | Multiple maintainers (bwbarrett, hppritcha) explicitly opposed merging to a single shared endpoint due to conflicting completion-semantic/performance requirements between MTL and BTL, and a per-node workaround (increasing hfi1 contexts) was noted. The design decision is wontfix. Recommend closing. |
| [#9722](https://github.com/open-mpi/ompi/issues/9722) — Add simple opal atomic store/load? | Request to add simple opal_atomic_load/opal_atomic_store (relaxed-ordering) helpers to OPAL's atomics, which currently lacks dedicated load/store primitives. | not implemented | 🗑️ Close | No dedicated opal_atomic_load/store helpers exist on main; maintainers were open to a PR but saw no in-tree use case (synchronization is via locks/fences) and the reporter resolved his own use case locally and invited closing. Recommend closing as a no-current-use-case enhancement. |
| [#10017](https://github.com/open-mpi/ompi/issues/10017) — MPI_Datatype definitions cannot be used in constexpr statements | Predefined MPI handles (e.g. MPI_INT) use a void* cast to an external padded symbol, so they cannot be used in C++ constexpr expressions. | not implemented | 🗑️ Close | The OMPI_PREDEFINED_GLOBAL void* double-cast is still present on main and is required for ABI stability; per the MPI standard (named constants are only link-time, not compile-time constants, per jeffhammond), constexpr cannot be standardly supported, making this out of scope/wontfix. |
| [#10572](https://github.com/open-mpi/ompi/issues/10572) — Add generic MPI pkgconfig descriptors | Request to install generic mpi-c.pc/mpi-cxx.pc/mpi-fort.pc files (matching CMake's pkgconfig detection) in addition to the existing ompi-*.pc files. | not implemented | 🗑️ Close | Maintainers (ggouaillardet, jsquyres) consistently pushed back, preferring CMake or downstream distros to handle the generic names rather than Open MPI installing implementation-agnostic .pc filenames; discussion stalled in 2023 with no plan to add them. Close as out of scope / wontfix. |
| [#11207](https://github.com/open-mpi/ompi/issues/11207) — hwloc distance table | Future-consideration proposal to build a generalized object-distance table (NUMA/PCI/GPU) usable for dynamic affinity-based object selection, possibly in hwloc during topology init. | not implemented | 🗑️ Close | An open-ended design idea; rhc54 responded that PMIx already provides such a distance table and that NUMA-based distances are no longer well-defined, effectively pushing back on the proposal, after which the thread stalled in 2022. No implementation tracked in OMPI. Recommend closing as out of scope / superseded by existing PMIx capability. |
| [#12227](https://github.com/open-mpi/ompi/issues/12227) — Fault tolerance for node failure | User asks whether Open MPI 5.0.1 supports ULFM fault tolerance for whole-node failure (not just process failure), which fails because PRTE terminates the job when a daemon is lost. | not implemented | 🗑️ Close | Maintainer (rhc54) stated node-failure FT is not supported, with no committed plan or timetable; process-level FT works but node FT requires changes to PRTE's default fail-fast behavior. Recommend close as out-of-scope/not implemented (effectively wontfix for now). |
| [#12228](https://github.com/open-mpi/ompi/issues/12228) — Proposal: provide mpiacc wrapper script | Proposal to ship an 'mpiacc' compiler wrapper that makes compiling heterogeneous (CUDA/HIP/SYCL) MPI applications as simple as 'mpiacc hello.cu'. | not implemented | 🗑️ Close | Maintainers (jsquyres, bosilca) pushed back, arguing Open MPI cannot track other tools' evolving flags and that existing mechanisms (wrapper nesting, --showme, pkg-config) plus a user-local symlink workaround suffice; no implementation landed. Recommend close as out of scope (possibly redirect to a docs improvement). |
| [#12536](https://github.com/open-mpi/ompi/issues/12536) — Support zero-copy non-contiguous send | Request to add zero-copy intra-/inter-node sends for non-contiguous derived datatypes (e.g. structs of vectors), as MPICH does for some cases. | not implemented | 🗑️ Close | Members (bosilca) and UCX maintainer (yosefe) explained UCX has no zero-copy path for complex datatypes and XPMEM support would be moot once UCX is enabled; this is a large cross-project request gated on UCX. No work planned; recommend close as out of scope, though it could be kept as a long-term wishlist item. |
| [#13453](https://github.com/open-mpi/ompi/issues/13453) — Efficiency improvements for error code/class management | Proposes reusing NULL slots in the ompi_mpi_errcodes pointer array (instead of always growing it) when error codes/classes are added after removals. | not implemented | ❓ Needs info | errcode.c add functions still always increment lastused on main (unchanged), so not implemented. Maintainer is skeptical of value and notes a dense array would break detection of misuse (removed-class still referenced). Low priority; recommend pinging author/closing absent a refined design that preserves safety checks. |
| [#13523](https://github.com/open-mpi/ompi/issues/13523) — srun with Open MPI 5.0.3 unexpectedly launches MPI jobs as singletons without ERROR | When launched under Slurm srun without a reachable PMIx server (e.g. plain srun or --mpi=pmi2), Open MPI silently falls back to N singletons rather than warning/erroring that MPI wireup failed. | not implemented | ❓ Needs info | Maintainer responded that adding resource-manager-specific (Slurm) detection is a large, hard-to-maintain ask (Slurm envars churn constantly) and offered it only as a community contribution. No work done; effectively wontfix unless someone volunteers. Recommend pinging/closing if no contributor steps up. |
| [#7983](https://github.com/open-mpi/ompi/issues/7983) — Use MPI_FILE_WRITE_SHARED with stdout? | User wants to serialize multi-rank output by opening /dev/stdout with MPI_MODE_SEQUENTIAL and writing via shared file pointers; works on macOS but fails on Linux with 'Illegal seek'. | not implemented | ✅ Keep open | Maintainer (ompio author) explained MPI I/O was not designed for this and it would require new interfaces plus launcher-level stdout aggregation support; arguably not even well-defined in the MPI standard. Not implemented and essentially out of scope, but no decision was recorded, so leaving open as a low-priority enhancement is defensible. |
| [#9003](https://github.com/open-mpi/ompi/issues/9003) — OSHMEM should not require SPML, nor UCX | Requests rearchitecting the OSHMEM layer so it can be built without UCX/SPML (e.g., on top of MPI-3 RMA or shared-memory), since OpenSHMEM has no fundamental need for UCX. | not implemented | ✅ Keep open | Maintainers (rhc54, hjelmn) agreed there is no technical reason it cannot be done but no one has the interest/cycles to own it; OSHMEM still requires the UCX-based spml. A legitimate standing enhancement request with no owner; keep open as a tracked wishlist item or close as wontfix given no maintainer. |
